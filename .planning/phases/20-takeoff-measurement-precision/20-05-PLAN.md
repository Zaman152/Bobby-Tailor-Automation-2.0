---
phase: 20-takeoff-measurement-precision
plan: 05
type: execute
wave: 3
depends_on: ["20-03", "20-04"]
files_modified:
  - claude_analyzer.py
  - calculator.py
  - aggregator.py
  - tests/test_takeoff_generalization.py
autonomous: true

must_haves:
  truths:
    - "MEASURE pass extracts ALL linear run types (storm, gas, duct, conduit, striping, guard rail, trench)"
    - "ITEM_NAME_MAP covers full Masterv2 §C taxonomy (~70 entries), not only golden CSV items"
    - "Aggregator distinguishes door/frame/equipment types generically"
  artifacts:
    - path: aggregator.py
      provides: "Complete ITEM_NAME_MAP per Masterv2"
    - path: claude_analyzer.py
      contains: "MEASURE_ADDENDUM"
---

<objective>
Extend measurement extraction and aggregation for **all** quantity types found on construction drawings — linear runs (20+ types), areas, counts — aligned with Masterv2 §C taxonomy. Golden CSV items must emerge from general rules, not hardcoded map entries.
</objective>

<context>
@.planning/phases/20-takeoff-measurement-precision/20-RESEARCH.md (§11.8, §11.9)
@Masterv2.md (§C.3 estimation tables)
@aggregator.py
@calculator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: MEASURE_ADDENDUM — all linear run types + calculators</name>
  <files>claude_analyzer.py, calculator.py</files>
  <action>
**MEASURE_ADDENDUM** appended to EXTRACTION_PROMPT (generic, no project names):

Linear run arrays (extend pipe_runs[] semantics):
- Storm/sanitary: PVC, RCP, HDPE (existing)
- Gas: black steel, CSST, corrugated stainless
- Mechanical: duct LF, refrigerant lines, condensate
- Electrical: conduit, wireway, cable tray
- Site: striping, guard rail, hand rail, trench drain, curb/gutter
- Structural: lintels (new lintel_runs[]), steel angles

Rules:
- NEVER put run lengths in measurements[] when a dedicated array exists
- Include total_lf when annotated; else sum segments
- material=null → classify by description keywords in calculator

Calculator additions:
- `_calculate_from_lintel_runs()` (RESEARCH §5)
- Extend `_calculate_from_pipe_runs()`: null-guard material; route gas→gas_pipe, duct→new duct_lf table or generic linear
- Add missing ESTIMATION_TABLES: lintel, gas_pipe, duct_lf, conduit_lf, striping (verify existing)

Wire lintel_runs into apply_estimation_tables.
  </action>
  <verify>python3 -m pytest tests/test_calculator_accuracy.py tests/test_takeoff_generalization.py -q</verify>
  <done>Any linear annotation type has a calculator path</done>
</task>

<task type="auto">
  <name>Task 2: Full ITEM_NAME_MAP from Masterv2 §C</name>
  <files>aggregator.py, tests/test_takeoff_generalization.py</files>
  <action>
Expand ITEM_NAME_MAP to cover Masterv2 §C taxonomy (RESEARCH §11.9 ~70 entries):

**Civil/Site:** Bollards, Striping, Storm Pipe (by size), Trench Drain, Catch Basins, Manholes, Headwall, Curb, Asphalt, Concrete Pavement

**Structure:** Columns (H-spec), Sealed Concrete, Exposed Structure, Tilt Up Wall (ext/int), CMU Wall, Lintels, Guard Rail, Hand Rail

**Architectural:** Flooring, Ceiling Grid, Drywall, Paint, Doors-HM/WD/AL, Frame-HM, Windows, Canopy, EIFS, CMU Paint, Insulation, Stairs, Ladder, Lift, Mobilization

**MEP:** Gas Piping, Lighting Fixtures, Receptacles, Panels, Fan Coil, AHU, Exhaust Fans

Ordering rules:
- Specific patterns BEFORE generic (Doors-HM before Doors)
- Use word-boundary-safe regex (match aggregator normalize_item_name tests)

Add `tests/test_aggregator_generalization.py` or extend test_takeoff_generalization:
- 20+ description strings → expected canonical names (synthetic, no golden CSV)
  </action>
  <verify>python3 -m pytest tests/test_takeoff_generalization.py -q -k "aggregator"</verify>
  <done>ITEM_NAME_MAP complete; aggregator tests cover civil, structural, arch, MEP labels</done>
</task>

</tasks>

<success_criteria>
Any item type in Masterv2 §C maps through extraction → calculator → aggregator without project-specific code.
</success_criteria>

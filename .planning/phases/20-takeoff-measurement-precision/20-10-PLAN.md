---
phase: 20-takeoff-measurement-precision
plan: 10
type: execute
wave: 7
gap_closure: true
depends_on: ["20-08", "20-09"]
files_modified:
  - tests/test_golden_takeoff.py
  - README.md
  - .planning/phases/20-takeoff-measurement-precision/20-UAT.md
  - .planning/phases/20-takeoff-measurement-precision/20-VERIFICATION.md
autonomous: false
user_setup:
  - tool: manual
    why: "Golden PDFs are client files — copy from uploads/ or provide paths"
    docs: "20-CONTEXT.md Golden reference files"

must_haves:
  truths:
    - "GoldenValidator score ≥0.97 on Crow Cass when crow_cass_plans.pdf present"
    - "GoldenValidator score ≥0.97 on Bob's Discount when bobs_discount_plans.pdf present"
    - "Failures fixed only via general rules (prompt, profile, aggregator) — never hardcoded project quantities"
    - "20-UAT.md golden section updated with actual scores"
  artifacts:
    - path: tests/fixtures/crow_cass/crow_cass_plans.pdf
      provides: "Crow Cass golden PDF (optional in repo)"
      note: "May remain gitignored; test skipif when absent"
    - path: tests/fixtures/bobs_discount/bobs_discount_plans.pdf
      provides: "Bob's Discount golden PDF (optional in repo)"
      note: "May remain gitignored; test skipif when absent"
---

<objective>
Close human-verification gap: run golden PDF regression at ≥97% on Crow Cass and Bob's Discount. Document fixture setup and iterate on failures using general rules only.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@.planning/phases/20-takeoff-measurement-precision/20-VERIFICATION.md (Human Verification)
@.planning/phases/20-takeoff-measurement-precision/20-CONTEXT.md
</execution_context>

<context>
@tests/test_golden_takeoff.py
@tests/golden_validator.py
@pdf_analyzer.py
@.planning/phases/20-takeoff-measurement-precision/20-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Golden PDF fixture setup + README</name>
  <files>README.md</files>
  <action>
1. Document exact fixture paths (already in test_golden_takeoff.py):
   - `tests/fixtures/crow_cass/crow_cass_plans.pdf`
   - `tests/fixtures/bobs_discount/bobs_discount_plans.pdf`

2. Add README section **Golden regression fixtures** with copy commands from uploads/ per 20-CONTEXT.md:
   - `Crow - Cass White Road-Plans.pdf` → crow_cass_plans.pdf
   - `Bob's Discount Furniture - Kennesaw, GA-plans.pdf` → bobs_discount_plans.pdf

3. If PDFs exist under `uploads/` or project root, copy them into fixture dirs (do not commit if .gitignore blocks — document for operator).

4. Optional: add `scripts/setup_golden_fixtures.sh` that copies when source files exist (exit 0 with message if missing).
  </action>
  <verify>grep -q "Golden regression fixtures" README.md && grep -q "crow_cass_plans.pdf" README.md</verify>
  <done>Fixture paths documented; PDFs copied when sources available</done>
</task>

<task type="auto">
  <name>Task 2: Run golden tests and iterate to ≥97%</name>
  <files>tests/test_golden_takeoff.py, claude_analyzer.py, calculator.py, aggregator.py, sheet_pass_matrix.py</files>
  <action>
1. Run generalization first (must be 100%):
   `pytest tests/test_takeoff_generalization.py -v`

2. Run golden integration:
   `pytest tests/test_golden_takeoff.py -v -m golden`

3. If PDFs absent: record skip in SUMMARY and UAT; proceed to checkpoint with `blocked: missing PDFs`.

4. If PDFs present and score &lt; 0.97: diagnose per-item failures from GoldenValidator report.

**Allowed fixes:**
- Prompt rule improvements (COUNT_PROMPT, MEASURE_ADDENDUM, SCHEDULE_PROMPT)
- Profile / content-first logic
- Pass matrix adjustments
- Estimation table formulas
- ITEM_NAME_MAP patterns (generic)

**Forbidden fixes:**
- `if project_name == "Crow": qty = 28`
- Hardcoded golden quantities
- Skipping passes by filename

5. Re-run until both fixtures ≥97% OR document remaining gaps with root-cause in SUMMARY.

Note: Golden tests call `run_pdf_analysis` — requires `ANTHROPIC_API_KEY` in env.
  </action>
  <verify>pytest tests/test_golden_takeoff.py -v -m golden 2>&1 | tee /tmp/golden_run.log; grep -E 'passed|skipped|FAILED' /tmp/golden_run.log</verify>
  <done>≥97% on both golden projects OR documented blocker with per-item gap table</done>
</task>

<task type="auto">
  <name>Task 3: Update 20-UAT.md golden regression matrix</name>
  <files>.planning/phases/20-takeoff-measurement-precision/20-UAT.md</files>
  <action>
Update 20-UAT.md **Golden regression** section with:
- Actual GoldenValidator scores (or SKIPPED — PDF absent)
- Per-item PASS/FAIL table for Crow and Bob
- Date and commit hash of passing run

Update human-verify checklist item 2 with results.
  </action>
  <verify>grep -q "Golden regression" .planning/phases/20-takeoff-measurement-precision/20-UAT.md</verify>
  <done>UAT reflects measured golden scores, not placeholder checkmarks</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Golden regression run (or documented PDF-absent skip) + UAT updated</what-built>
  <how-to-verify>
1. Confirm PDFs at fixture paths (or accept documented skip)
2. `pytest tests/test_golden_takeoff.py -v -m golden` — both ≥97% if PDFs present
3. Review 20-UAT.md per-item table for any remaining FAIL rows
  </how-to-verify>
  <resume-signal>Type "approved" or list failing items to fix</resume-signal>
</task>

</tasks>

<verification>
- [ ] Generalization suite still 100% before golden run
- [ ] No project-specific quantity hacks in codebase
- [ ] UAT golden section has real scores or explicit SKIP reason
</verification>

<success_criteria>
Phase success criterion #2 satisfied when PDFs present: golden regression ≥97% Crow + Bob.
</success_criteria>

<output>
After completion, create `.planning/phases/20-takeoff-measurement-precision/20-10-SUMMARY.md`
</output>

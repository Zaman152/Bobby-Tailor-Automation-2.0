---
phase: 20-takeoff-measurement-precision
plan: 04
type: execute
wave: 2
depends_on: ["20-00"]
files_modified:
  - calculator.py
  - tests/test_takeoff_generalization.py
autonomous: true

must_haves:
  truths:
    - "Room notes 'sealed concrete' produce sealed_concrete even on retail profile"
    - "Room notes 'VCT' produce flooring even on industrial profile when explicitly tagged"
    - "Profiles exist for industrial, retail, office, civil, residential, institutional, mixed_use, auto"
    - "Civil profile never generates flooring/ceiling from site areas"
  artifacts:
    - path: calculator.py
      contains: "PROJECT_TYPE_PROFILES"
---

<objective>
Implement content-first room mapping and expanded project-type profiles so area calculations are correct for **any** building type — not only industrial vs retail.

Priority chain: room notes → room materials → project profile → auto fallback.
</objective>

<context>
@.planning/phases/20-takeoff-measurement-precision/20-RESEARCH.md (§11.3, §11.4)
@calculator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: ESTIMATION_TABLES + PROJECT_TYPE_PROFILES (7 types + auto)</name>
  <files>calculator.py</files>
  <action>
Add ESTIMATION_TABLES entries (if missing):
sealed_concrete, cmu_wall, internal_tilt_up_wall, canopy, eifs, cmu_paint, gas_pipe, lintel
(plus any Masterv2 §C items not yet present)

Add PROJECT_TYPE_PROFILES for: industrial, retail, office, civil, residential, institutional, mixed_use, auto
(RESEARCH §11.4 — use default_floor_items, skip_items, expect_items structure)

Add MATERIAL_NOTE_MAP — maps note keywords to item types:
```python
MATERIAL_NOTE_MAP = [
    (r"sealed\s*concrete|polished\s*concrete|SOG", "sealed_concrete"),
    (r"exposed\s*structure|exposed\s*deck", "exposed_structure"),
    (r"vct|lvt|carpet|tile|hardwood", "flooring"),
    (r"tilt[- ]?up|precast\s*panel", "tilt_up_wall"),
    (r"cmu|block\s*wall|masonry", "cmu_wall"),
    ...
]
```
  </action>
  <verify>python3 -c "from calculator import PROJECT_TYPE_PROFILES; assert set(PROJECT_TYPE_PROFILES) >= {'industrial','retail','office','civil','residential','institutional','mixed_use','auto'}"</verify>
  <done>All building-type profiles defined</done>
</task>

<task type="auto">
  <name>Task 2: Content-first _calculate_from_room + _detect_project_type</name>
  <files>calculator.py</files>
  <action>
Rewrite `_calculate_from_room(room, sheet_name, project_type="auto")`:

1. Parse `room.get("notes")` and `room.get("materials", [])` through MATERIAL_NOTE_MAP
2. For each matched item type, call `_apply_formula(item_type, area, 'sq_ft', str(area))`
3. If content matches skip_items from active profile, do NOT also add profile defaults (e.g. sealed concrete room skips flooring)
4. If NO content matches → use profile `default_floor_items` + wall_items (perimeter × height for paint/drywall/cmu)
5. `project_type="auto"` → detect via `_detect_project_type(all_pages)` using keyword heuristics (RESEARCH §11.4 table) — works for any project name

Update `apply_estimation_tables(extracted, project_type="auto")` to accept and pass project_type.

Add unit tests to test_takeoff_generalization.py verifying content-first overrides profile.
  </action>
  <verify>python3 -m pytest tests/test_takeoff_generalization.py tests/test_calculator_accuracy.py -q</verify>
  <done>Retail note on industrial project → flooring; sealed concrete note → sealed_concrete regardless of profile</done>
</task>

</tasks>

<success_criteria>
Calculator produces correct item types for any building category based on drawing content first, profile second.
</success_criteria>

---
phase: 20-takeoff-measurement-precision
plan: 04
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - calculator.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "_calculate_from_room produces sealed_concrete + exposed_structure for industrial projects"
    - "_calculate_from_room produces flooring + ceiling_grid for retail projects (existing behavior)"
    - "ESTIMATION_TABLES includes sealed_concrete, cmu_wall, internal_tilt_up_wall"
    - "Project type can be detected from extracted page data"
  artifacts:
    - path: calculator.py
      provides: "PROJECT_TYPE_PROFILES, _detect_project_type, profile-aware _calculate_from_room, new ESTIMATION_TABLES entries"
      contains: "PROJECT_TYPE_PROFILES"
  key_links:
    - from: calculator.py
      to: aggregator.py
      via: "item_type values fed to normalize_item_name"
      pattern: "normalize_item_name"
    - from: calculator.py
      to: pdf_analyzer.py
      via: "apply_estimation_tables called per sheet"
      pattern: "apply_estimation_tables"
---

<objective>
Add project-type profiles to `calculator.py` so that `_calculate_from_room()` generates the correct item types based on project type (industrial vs retail). Add missing ESTIMATION_TABLES entries for industrial items (sealed_concrete, cmu_wall, internal_tilt_up_wall). Add `_detect_project_type()` heuristic.

Purpose: The root cause of Crow Cass's worst error (RC-2) — 395K SF Sealed Concrete showing as 437K SF Flooring — is that `_calculate_from_room()` unconditionally generates flooring/ceiling/paint/drywall for all area types. Industrial buildings need sealed_concrete + exposed_structure instead.

Output: Profile-aware room calculations, new estimation tables, auto-detection function.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-takeoff-measurement-precision/20-RESEARCH.md (§3 Project-Type Profiles)
@.planning/phases/20-takeoff-measurement-precision/20-CONTEXT.md (RC-2)
@calculator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PROJECT_TYPE_PROFILES and new ESTIMATION_TABLES entries</name>
  <files>calculator.py</files>
  <action>
1. **Add `PROJECT_TYPE_PROFILES` dict** after `ESTIMATION_TABLES`:

```python
PROJECT_TYPE_PROFILES = {
    "industrial": {
        "area_items": ["sealed_concrete", "exposed_structure"],
        "wall_items": ["tilt_up_wall"],
        "skip_items": ["flooring", "ceiling_grid", "paint", "drywall"],
    },
    "retail": {
        "area_items": ["flooring", "ceiling_grid"],
        "wall_items": ["paint", "drywall"],
        "skip_items": [],
    },
    "office": {
        "area_items": ["flooring", "ceiling_grid"],
        "wall_items": ["paint", "drywall"],
        "skip_items": [],
    },
    "auto": None,
}
```

2. **Add new entries to ESTIMATION_TABLES** for industrial items:

- `"sealed_concrete"`: unit_out="sq_ft", waste_factor=1.0, formula="area", keywords=["sealed concrete", "polished concrete", "concrete floor", "slab on grade"]
- `"cmu_wall"`: unit_out="sq_ft", waste_factor=1.0, formula="area", keywords=["cmu", "masonry", "block wall", "concrete masonry"]
- `"internal_tilt_up_wall"`: unit_out="sq_ft", waste_factor=1.0, formula="area", keywords=["internal tilt", "interior tilt", "interior concrete wall"]
- `"canopy"`: unit_out="sq_ft", waste_factor=1.0, formula="area", keywords=["canopy", "entrance canopy", "overhead canopy"]
- `"eifs"`: unit_out="sq_ft", waste_factor=1.05, formula="area × 1.05", keywords=["eifs", "exterior insulation", "dryvit"]
- `"cmu_paint"`: unit_out="sq_ft", waste_factor=1.0, formula="area", keywords=["cmu paint", "masonry paint", "block paint"]
  </action>
  <verify>python3 -c "from calculator import PROJECT_TYPE_PROFILES, ESTIMATION_TABLES; assert 'sealed_concrete' in ESTIMATION_TABLES; assert 'industrial' in PROJECT_TYPE_PROFILES; print('OK')"</verify>
  <done>Profile dict and 6 new estimation table entries exist</done>
</task>

<task type="auto">
  <name>Task 2: Make _calculate_from_room profile-aware + add _detect_project_type</name>
  <files>calculator.py</files>
  <action>
1. **Update `_calculate_from_room` signature** to accept `project_type: str = "auto"`:
   - If `project_type` is in `PROJECT_TYPE_PROFILES` and not `"auto"`:
     - Generate items from `profile["area_items"]` using the room area (each gets `_room_calc` call)
     - Generate items from `profile["wall_items"]` using estimated wall area (perimeter × ceiling height)
     - Skip any items in `profile["skip_items"]`
   - If `project_type == "auto"` or not in profiles → use existing behavior (flooring, ceiling_grid, paint, drywall) — this preserves backward compatibility

2. **Update `apply_estimation_tables` signature** to accept `project_type: str = "auto"`:
   - Pass `project_type` through to `_calculate_from_room(room, sheet_name, project_type)`
   - All other calculation paths (measurements, components, schedules, pipe_runs, civil_structures) remain unchanged

3. **Add `_detect_project_type(extracted_pages: list) -> str`** function:
   - Iterates all extracted page dicts
   - Scores indicators: "warehouse", "tilt-up", "tilt up", "industrial", "sealed concrete" → +2 industrial; "retail", "store", "showroom", "sales floor" → +2 retail; "office", "tenant", "suites" → +1 office
   - Returns the type with highest score, or "retail" if all zero (safer default)
   - This function is called in `pdf_analyzer.run_pdf_analysis()` (wired in Plan 20-06)

Keep backward compatibility: `apply_estimation_tables(data)` without `project_type` defaults to `"auto"` which preserves the existing flooring/ceiling/paint/drywall behavior.
  </action>
  <verify>python3 -c "from calculator import apply_estimation_tables, _detect_project_type; print('imports OK')"</verify>
  <done>_calculate_from_room generates profile-appropriate items; apply_estimation_tables accepts project_type; _detect_project_type heuristic added</done>
</task>

</tasks>

<verification>
- [ ] `apply_estimation_tables(data)` without project_type still produces flooring/ceiling/paint/drywall (backward compat)
- [ ] `apply_estimation_tables(data, project_type="industrial")` produces sealed_concrete/exposed_structure instead
- [ ] Existing `tests/test_calculator_accuracy.py` still passes
</verification>

<success_criteria>
Industrial projects generate sealed_concrete + exposed_structure from room areas; retail projects keep flooring + ceiling_grid (existing behavior). Auto-detection heuristic available for pipeline integration. Backward compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/20-takeoff-measurement-precision/20-04-SUMMARY.md`
</output>

---
phase: 20-takeoff-measurement-precision
plan: 13
type: execute
wave: 9
gap_closure: true
depends_on: ["20-12"]
files_modified:
  - scripts/golden_convergence.py
  - tests/test_golden_takeoff.py
  - aggregator.py
  - calculator.py
autonomous: true

must_haves:
  truths:
    - "scripts/golden_convergence.py exits 0 only when all present golden fixtures score >= 0.97"
    - "Integer EA items aggregated with whole-number enforcement for count-type rows"
    - "Golden tests document companion takeoff PDF path beside plans PDF"
  artifacts:
    - path: scripts/golden_convergence.py
      provides: "automated accuracy gate for CI and auto-iteration"
---

<objective>
Automated convergence gate: one command runs generalization + golden regression and fails until ≥97% on every present fixture — enables full-auto iterate loop.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: golden_convergence.py</name>
  <files>scripts/golden_convergence.py</files>
  <action>
1. Run `pytest tests/test_takeoff_generalization.py -q` — must pass first

2. Run `pytest tests/test_golden_takeoff.py -m golden -v` — capture scores per fixture

3. Parse stdout / use GoldenValidator in-process for crow + bob paths

4. Exit 0 if all present fixtures >= 0.97; exit 1 with per-item failure table otherwise

5. Support `--min-score 0.97` and `--fixture crow|bob|all`

6. Print API credit / skip hints when tests skip or error
  </action>
  <verify>python3 scripts/golden_convergence.py --help</verify>
  <done>Script runnable and enforces threshold</done>
</task>

<task type="auto">
  <name>Task 2: EA whole-number aggregation + golden companion paths</name>
  <files>aggregator.py, tests/test_golden_takeoff.py, README.md</files>
  <action>
1. In `aggregate_takeoff`, for unit EA: round quantities to nearest int when item_type is count-class (bollard, columns, stairs, etc.)

2. Golden test: auto-detect `crow_cass_takeoff.pdf` / `bobs_discount_takeoff.pdf` sibling of plans (same as production)

3. README: document TAKEOFF_ACCURACY_MODE=high and companion PDF naming
  </action>
  <verify>pytest tests/test_golden_takeoff.py::test_golden_validator_logic -v</verify>
  <done>EA aggregation stable; golden docs updated</done>
</task>

</tasks>

<verification>
python3 scripts/golden_convergence.py 2>&1 | head -40
</verification>

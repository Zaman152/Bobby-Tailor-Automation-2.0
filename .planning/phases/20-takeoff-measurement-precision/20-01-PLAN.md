---
phase: 20-takeoff-measurement-precision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pdf_analyzer.py
  - tests/test_sheet_name.py
autonomous: true

must_haves:
  truths:
    - "Sheet ID extracted from title-block region (bottom-right), not full page text"
    - "Standard/code references (ASTM, NFPA, UL, IBC, ADA) never become sheet IDs"
    - "Works for A-101, A3.0, C-4, M3.1, S1.2, G0.1 naming conventions"
  artifacts:
    - path: pdf_analyzer.py
      provides: "_sheet_name_from_doc with SHEET_ID_NOISE_PATTERNS"
      contains: "SHEET_ID_NOISE_PATTERNS"
---

<objective>
Fix sheet identification for **any** construction PDF: parse the title block region, apply generic noise filters for building codes/standards (not a hardcoded E283 list), support multiple sheet ID formats (A-101, A3.0, C-4, etc.).

Purpose: Correct sheet IDs enable sheet-type classification and model routing for all disciplines — architectural, structural, civil, MEP.
</objective>

<context>
@.planning/phases/20-takeoff-measurement-precision/20-RESEARCH.md (§1, §11.6)
@pdf_analyzer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Title-block extraction + generic SHEET_ID_NOISE_PATTERNS</name>
  <files>pdf_analyzer.py</files>
  <action>
1. Add `SHEET_ID_NOISE_PATTERNS` list (RESEARCH §11.6) — regex patterns for ASTM, NFPA, UL, IBC, ADA, ANSI, ASCE, grid labels, keynote numbers. **Do NOT hardcode E283 or A156 as the only noise entries.**

2. Rewrite `_sheet_name_from_doc`:
   - Search title-block region: `y > height * 0.80 AND x > width * 0.55` via `page.get_text("words")`
   - Sheet ID regex priority: `[A-Z]\d+\.\d+`, `[A-Z]-\d+`, `[A-Z]\d{3}`, `[CMEPSG]\d+\.\d+` (discipline prefixes)
   - `_is_noise_sheet_candidate(text) -> bool` using SHEET_ID_NOISE_PATTERNS
   - Fallback: full-page search skipping noise matches
   - Final fallback: `Page_{n}`

3. Export `get_title_block_text(doc, page_num)` for use by `classify_sheet_type_from_text` in sheet_pass_matrix.py (20-00).

4. Wire `get_pdf_metadata()` to also return `sheet_type_hint` per page using classify function (import from sheet_pass_matrix if available, else skip).
  </action>
  <verify>python3 -m pytest tests/test_sheet_name.py -q</verify>
  <done>Generic noise filter; A4.0 wins over ASTM E283; C-4 and A-101 both parse</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests — discipline-agnostic sheet ID cases</name>
  <files>tests/test_sheet_name.py</files>
  <action>
Test cases (mock word bounding boxes or text helper):
- ASTM E283 + A4.0 in page → A4.0
- NFPA 13 + S1.2 in title block → S1.2
- C-4 civil sheet
- A-101 with hyphen
- M3.1 mechanical schedule
- Page with only ASTM references → Page_N fallback
- Title block in bottom-right only (words outside region ignored)

Optional integration test with any PDF in tests/fixtures/ using skipif.
  </action>
  <verify>python3 -m pytest tests/test_sheet_name.py -q</verify>
  <done>Tests cover architectural, civil, MEP, and noise cases — not Bob-specific only</done>
</task>

</tasks>

<success_criteria>
Sheet naming works across all discipline prefixes and rejects code-standard false positives generically.
</success_criteria>

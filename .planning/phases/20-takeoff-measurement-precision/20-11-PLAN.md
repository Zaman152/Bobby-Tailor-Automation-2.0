---
phase: 20-takeoff-measurement-precision
plan: 11
type: execute
wave: 8
gap_closure: true
depends_on: ["20-10"]
files_modified:
  - companion_takeoff.py
  - pdf_analyzer.py
  - takeoff_pipeline.py
  - tests/test_companion_takeoff.py
autonomous: true

must_haves:
  truths:
    - "Any upload folder may include a companion *takeoff* / *take-off* PDF; pipeline merges its tables as takeoff_legend schedules"
    - "No project names or hardcoded quantities in companion_takeoff.py"
    - "Companion schedules flow through _calculate_from_schedule and aggregate_takeoff like vision schedules"
  artifacts:
    - path: companion_takeoff.py
      provides: "find_companion_takeoff_pdf + extract_legend_schedules"
    - path: tests/test_companion_takeoff.py
      provides: "CI tests for table row parsing without API"
---

<objective>
When estimators upload a paired quantity take-off PDF (common on every job), use its tabular text as authoritative takeoff_legend — independent of whether the legend is rasterized on the plan sheet.
</objective>

<context>
@.planning/phases/20-takeoff-measurement-precision/20-CONTEXT.md
@pdf_analyzer.py
@takeoff_pipeline.py
@calculator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: companion_takeoff.py — discover + parse</name>
  <files>companion_takeoff.py</files>
  <action>
1. `find_companion_takeoff_pdf(plans_path: str) -> Optional[str]`:
   - Same directory as plans PDF
   - Match filename patterns: `takeoff`, `take-off`, `take offs`, `quantity` (case-insensitive)
   - Exclude the plans file itself; prefer highest word overlap with plans basename

2. `extract_legend_schedules(takeoff_pdf_path: str) -> list[dict]`:
   - Use pdfplumber (optional import) per page `extract_tables()`
   - Normalize headers to ITEM/DESCRIPTION, QTY, UNIT via column alias map
   - Skip tables with &lt;2 numeric qty rows
   - Return one schedule dict: `table_purpose: takeoff_legend`, `use_for_takeoff: true`

3. Row filter: qty must parse via calculator._parse_numeric; skip header rows

Forbidden: Crow, Bob, sheet numbers, golden quantities as literals
  </action>
  <verify>pytest tests/test_companion_takeoff.py -v</verify>
  <done>Module finds companion files and returns schedule rows from synthetic table fixture</done>
</task>

<task type="auto">
  <name>Task 2: Wire companion into PDF + pipeline</name>
  <files>pdf_analyzer.py, takeoff_pipeline.py</files>
  <action>
1. In `run_pdf_analysis`, after resolving pdf_path:
   - `companion = find_companion_takeoff_pdf(pdf_path)`
   - `project_legends = extract_legend_schedules(companion) if companion else []`

2. `TakeoffPipeline.run_project(pages, project_legend_schedules=...)`:
   - After each sheet extracted (non-skipped), if `project_legend_schedules` and sheet is first `floor_plan`, extend `merged["schedules"]` with legends (dedupe by name)

3. Log companion path used (info level) for operator traceability
  </action>
  <verify>grep -q "project_legend_schedules" takeoff_pipeline.py pdf_analyzer.py</verify>
  <done>Companion legends merged into first floor plan extraction for any project</done>
</task>

</tasks>

<verification>
- pytest tests/test_companion_takeoff.py -v
- pytest tests/test_takeoff_generalization.py -q
</verification>

<success_criteria>
- Companion PDF detection works for any directory layout described in README
- Parsed schedules produce calculator items via existing schedule path
</success_criteria>

---
phase: 20-takeoff-measurement-precision
plan: 08
type: execute
wave: 6
gap_closure: true
depends_on: ["20-06"]
files_modified:
  - tests/test_scraper_pipeline.py
  - tests/test_scraper_analyze_manifest.py
  - tests/test_scraper_two_phase.py
  - tests/test_linked_sheets.py
  - tests/test_pipeline_parity.py
  - .planning/phases/20-takeoff-measurement-precision/20-VERIFICATION.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "All scraper integration tests pass with TakeoffPipeline mocks (ACCURACY-20-12)"
    - "test_pipeline_parity.py and scraper test modules can run in same pytest session without pollution"
    - "No test patches scraper.analyze_drawing (symbol removed in 20-06)"
  artifacts:
    - path: tests/test_scraper_pipeline.py
      provides: "Patches scraper._pipeline.run_sheet"
      contains: "_pipeline.run_sheet"
---

<objective>
Close verification Gap 1 (ACCURACY-20-12): prove StackCT scraper parity via a green automated test suite.

Note: Orchestrator commit `75fb649` already retargeted mocks from `scraper.analyze_drawing` → `scraper._pipeline.run_sheet`. This plan verifies, fixes any remaining failures, and resolves `test_pipeline_parity.py` module-isolation issues when run alongside scraper tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@.planning/phases/20-takeoff-measurement-precision/20-VERIFICATION.md (Gap 1)
</execution_context>

<context>
@scraper.py
@takeoff_pipeline.py
@tests/test_pipeline_parity.py
@tests/test_scraper_pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and finish scraper test mock migration</name>
  <files>tests/test_scraper_pipeline.py, tests/test_scraper_analyze_manifest.py, tests/test_scraper_two_phase.py, tests/test_linked_sheets.py</files>
  <action>
1. Confirm every `patch("scraper.analyze_drawing")` is gone; use `patch("scraper._pipeline.run_sheet", ...)` or inject `TakeoffPipeline(analyzer=mock)` via `patch("scraper._pipeline", pipeline_instance)`.

2. Keep `patch("scraper.apply_estimation_tables")` — still valid (scraper imports from calculator).

3. Linked-sheet tests: `_discover_and_add_linked_sheets` returns `(new_extracted, linked_meta)` only — no `new_estimates` unpack.

4. Two-phase ordering tests: allow setup-phase `capturing` callbacks (browser/login/discover) — assert `len(capturing) >= page_count` and `max(capturing) < min(analyzing)`, not exact counts.

Run:
```bash
pytest tests/test_scraper_pipeline.py tests/test_scraper_analyze_manifest.py tests/test_scraper_two_phase.py tests/test_linked_sheets.py -v
```
All must pass.
  </action>
  <verify>python3 -m pytest tests/test_scraper_pipeline.py tests/test_scraper_analyze_manifest.py tests/test_scraper_two_phase.py tests/test_linked_sheets.py -q</verify>
  <done>53+ scraper tests green; zero AttributeError on analyze_drawing</done>
</task>

<task type="auto">
  <name>Task 2: Fix parity + scraper pytest isolation</name>
  <files>tests/test_pipeline_parity.py</files>
  <action>
`test_pipeline_parity.py` loads `scraper` via `importlib` with mocked `sys.modules`, which pollutes later tests that `import scraper` normally.

Fix (pick one minimal approach):
- Add `conftest.py` fixture that `importlib.reload(scraper)` before scraper test modules, restoring real dependencies; OR
- Change parity scraper tests to patch `takeoff_pipeline.analyze_drawing` instead of replacing entire `sys.modules["claude_analyzer"]`; OR
- Mark parity tests `@pytest.mark.isolated` and document separate run (last resort).

Verify combined run:
```bash
pytest tests/test_pipeline_parity.py tests/test_scraper_pipeline.py -q
```
All pass in one invocation.
  </action>
  <verify>python3 -m pytest tests/test_pipeline_parity.py tests/test_scraper_pipeline.py -q</verify>
  <done>Parity + scraper suites pass in same pytest session</done>
</task>

</tasks>

<verification>
- [ ] `grep -r 'scraper.analyze_drawing' tests/` returns no matches
- [ ] Combined parity + scraper pytest invocation exits 0
</verification>

<success_criteria>
ACCURACY-20-12 satisfied: automated tests prove scraper uses TakeoffPipeline, not direct analyze_drawing.
</success_criteria>

<output>
After completion, create `.planning/phases/20-takeoff-measurement-precision/20-08-SUMMARY.md`
</output>

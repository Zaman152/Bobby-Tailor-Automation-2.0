---
phase: 20-takeoff-measurement-precision
plan: 12
type: execute
wave: 8
gap_closure: true
depends_on: ["20-11"]
files_modified:
  - claude_analyzer.py
  - sheet_pass_matrix.py
  - takeoff_pipeline.py
  - accuracy_config.py
autonomous: true

must_haves:
  truths:
    - "TAKEOFF_ACCURACY_MODE=high routes Sonnet for floor_plan count/measure/schedule/legend"
    - "Dedicated LEGEND_PROMPT pass on floor_plan reads graphic quantity tables full-page"
    - "VERIFY_PROMPT reconciles legend vs count vs measure; drops conflicting room flooring when legend has slab SF"
  artifacts:
    - path: accuracy_config.py
      provides: "is_high_accuracy_mode + model_for_takeoff_pass"
    - path: claude_analyzer.py
      provides: "LEGEND_PROMPT, VERIFY_PROMPT, pass_type legend|verify"
---

<objective>
Advanced multi-pass vision: legend-focused pass + verify pass under high-accuracy mode so graphic takeoff legends on plan sheets are read even when not in the PDF text layer.
</objective>

<context>
@claude_analyzer.py
@sheet_pass_matrix.py
@takeoff_pipeline.py
@.planning/phases/20-takeoff-measurement-precision/20-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: accuracy_config + model routing</name>
  <files>accuracy_config.py, sheet_pass_matrix.py</files>
  <action>
1. `accuracy_config.py`:
   - `TAKEOFF_ACCURACY_MODE` env: `standard` | `high` (default `high` for production takeoff)
   - `is_high_accuracy_mode() -> bool`
   - When high: floor_plan count/measure/schedule/legend → CLAUDE_MODEL_SCHEDULES (Sonnet)

2. Extend PASS_MATRIX `floor_plan`: add `legend` pass after `schedule`

3. MODEL_ROUTING: `("floor_plan", "legend")`, `("floor_plan", "verify")` → Sonnet

4. `plan_passes`: in high mode include `legend` and final `verify` on floor_plan only
  </action>
  <verify>pytest tests/test_sheet_pass_matrix.py -v -k "floor_plan"</verify>
  <done>High-accuracy mode enables legend + verify passes</done>
</task>

<task type="auto">
  <name>Task 2: LEGEND_PROMPT + VERIFY_PROMPT + merge</name>
  <files>claude_analyzer.py, takeoff_pipeline.py</files>
  <action>
1. `LEGEND_PROMPT` — extract ONLY quantity takeoff legend tables; return schedules[] only; require ITEM+QTY+UNIT per row

2. `VERIFY_PROMPT` — input in user message: summary of schedules/components/rooms counts; output corrections[] with action keep|drop|replace (applied in merge_verify_pass)

3. `merge_verify_pass(merged, verify_result)` — drop flooring rows when legend has sealed_concrete/exposed_structure; prefer legend qty for matching item types

4. `_run_pass` handles pass_type `legend` and `verify`; legend uses full-page render (not margin crop only)

5. Schedule pass on floor_plan: use full page image (disable margin-only crop for primary pass)
  </action>
  <verify>grep -q "LEGEND_PROMPT" claude_analyzer.py && grep -q "VERIFY_PROMPT" claude_analyzer.py</verify>
  <done>Legend and verify passes integrated in pipeline</done>
</task>

</tasks>

<verification>
pytest tests/test_sheet_pass_matrix.py tests/test_takeoff_pipeline.py -q
</verification>

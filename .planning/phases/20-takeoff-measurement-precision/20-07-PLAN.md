---
phase: 20-takeoff-measurement-precision
plan: 07
type: execute
wave: 5
depends_on: ["20-06"]
files_modified:
  - tests/test_takeoff_generalization.py
  - tests/test_golden_takeoff.py
  - tests/fixtures/sheet_coverage.json
  - .planning/phases/20-takeoff-measurement-precision/20-UAT.md
  - README.md
autonomous: false

must_haves:
  truths:
    - "tests/test_takeoff_generalization.py passes (all sheet types, no API)"
    - "Golden regression ≥97% on Crow Cass AND Bob's Discount when PDFs present"
    - "20-UAT.md documents generalization coverage + golden regression separately"
    - "No wrong item types on either golden project"
  artifacts:
    - path: .planning/phases/20-takeoff-measurement-precision/20-UAT.md
      contains: "Generalization"
---

<objective>
Convergence and sign-off: iterate until **both** test layers pass —
1. Generalization suite (all plan types, synthetic, CI-always)
2. Golden regression (Crow + Bob client references, skipif without PDFs)

Tune prompts/thresholds only through general rules, never project-specific hacks.
</objective>

<context>
@.planning/phases/20-takeoff-measurement-precision/20-CONTEXT.md
@.planning/phases/20-takeoff-measurement-precision/20-RESEARCH.md (§11.1, §13)
</context>

<tasks>

<task type="auto">
  <name>Task 0: sheet_coverage.json — document fixture scope (not product scope)</name>
  <files>tests/fixtures/sheet_coverage.json</files>
  <action>
Create sheet_coverage.json with header:
```json
{
  "_note": "Documents golden REGRESSION fixtures only. Product supports all sheet types per PASS_MATRIX.",
  "supported_sheet_types": ["floor_plan","elevation","civil_site","schedule","detail","title_sheet","roof_plan","mep_plan"],
  "golden_regression_fixtures": { "crow_cass": {...}, "bobs_discount": {...} }
}
```
Map golden items to source sheets for debugging — NOT as exhaustive product capability list.
  </action>
  <verify>python3 -c "import json; d=json.load(open('tests/fixtures/sheet_coverage.json')); assert 'supported_sheet_types' in d"</verify>
  <done>Fixture scope documented; product scope is PASS_MATRIX</done>
</task>

<task type="auto">
  <name>Task 1: Run both test layers and iterate</name>
  <files>tests/test_takeoff_generalization.py, tests/test_golden_takeoff.py</files>
  <action>
1. **Always run:** `pytest tests/test_takeoff_generalization.py -v` — must pass 100% before golden
2. **Copy golden PDFs** from uploads/ to tests/fixtures/ if present
3. **Run:** `pytest tests/test_golden_takeoff.py -v -m golden`
4. **On failure:** fix via general rules (prompt, profile, aggregator) — NEVER hardcode golden quantities
5. **Iterate** until generalization 100% + golden ≥97%

Forbidden fixes:
- if project_name == "Crow": qty = 28
- Hardcoding ITEM_NAME_MAP entries that only match one project's wording
- Skipping passes based on filename

Allowed fixes:
- Prompt rule improvements
- Profile/content-first logic
- Pass matrix adjustments
- Estimation table formula corrections
  </action>
  <verify>pytest tests/test_takeoff_generalization.py tests/test_golden_takeoff.py -v -m "golden or generalization"</verify>
  <done>Both test layers pass</done>
</task>

<task type="auto">
  <name>Task 2: 20-UAT.md + README</name>
  <files>.planning/phases/20-takeoff-measurement-precision/20-UAT.md, README.md</files>
  <action>
20-UAT.md sections:
1. **Generalization matrix** — each sheet_type fixture pass/fail
2. **Golden regression** — Crow + Bob per-item accuracy
3. **StackCT parity** — confirm pipeline_parity test passes
4. **Sign-off checklist**

README:
```bash
# All plan types (no API, CI-safe)
pytest tests/test_takeoff_generalization.py -v

# Client regression (requires golden PDFs)
pytest tests/test_golden_takeoff.py -v -m golden
```
  </action>
  <verify>test -f .planning/phases/20-takeoff-measurement-precision/20-UAT.md</verify>
  <done>UAT documents both layers</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Plan-type-agnostic takeoff pipeline with multi-pass extraction, 7 project profiles, full Masterv2 item taxonomy, StackCT/PDF parity, two-layer accuracy tests</what-built>
  <how-to-verify>
1. `pytest tests/test_takeoff_generalization.py -v` — all sheet types green
2. `pytest tests/test_golden_takeoff.py -v -m golden` — ≥97% on Crow + Bob
3. Upload a NEW plan (not Crow/Bob) and verify takeoff_summary produces sensible trade items
4. Confirm industrial PDF gets sealed_concrete not flooring; retail gets flooring; civil gets pipe LF
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<success_criteria>
Generalization tests prove engine works for all sheet types. Golden tests prove ≥97% on client references. No project-specific hardcoding anywhere in codebase.
</success_criteria>

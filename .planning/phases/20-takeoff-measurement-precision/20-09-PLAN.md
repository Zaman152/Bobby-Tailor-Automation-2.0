---
phase: 20-takeoff-measurement-precision
plan: 09
type: execute
wave: 6
gap_closure: true
depends_on: ["20-05"]
files_modified:
  - aggregator.py
  - tests/test_takeoff_generalization.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "ITEM_NAME_MAP has ≥68 entries (Masterv2 §C target ~70)"
    - "New patterns cover fire suppression, site utilities, HVAC subtypes, storefront, fence — not Crow/Bob-only wording"
    - "Specific patterns ordered before generic (Doors-HM before Doors, etc.)"
  artifacts:
    - path: aggregator.py
      provides: "ITEM_NAME_MAP ≥68 entries"
      min_lines: 78
---

<objective>
Close verification Gap 2 (ACCURACY-20-11): expand `ITEM_NAME_MAP` from 52 to ≥68 entries by cross-referencing Masterv2 §C taxonomy (RESEARCH §11.9).

Golden and generalization fixtures must still pass; add tests for each new canonical name.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@.planning/phases/20-takeoff-measurement-precision/20-RESEARCH.md (§11.9)
@Masterv2.md (§C / ITEM_NAME_MAP reference)
</execution_context>

<context>
@aggregator.py
@tests/test_takeoff_generalization.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit Masterv2 §C vs ITEM_NAME_MAP — add missing entries</name>
  <files>aggregator.py</files>
  <action>
1. Count current `ITEM_NAME_MAP` tuples; document baseline in SUMMARY.

2. Add **~18 missing** categories (generic regex, no Crow/Bob strings):

**Civil/Site (if absent):**
- Sanitary Pipe / Sewer (`sanitary.*pipe|sewer.*pipe|ss pipe`)
- Water Main (`water.*main|water.*line|domestic.*water`)
- Fire Hydrants (`fire.*hydrant|\bfh\b`)
- Area Drain / Cleanout (`area.*drain|floor.*drain|cleanout`)
- Stormwater basin (`detention|retention.*pond|bioswale`)
- Fence (`\bfence\b|chain.*link|ornamental.*fence`)
- Grading/earthwork (`grading|cut.*fill|mass.*grade`) — EA or CY as appropriate

**Structure/Site:**
- Dock Doors (`dock.*door|overhead.*door|coiling.*door`)
- Dock Leveler (`dock.*leveler|dock.*seal|dock.*bumper`)

**Architectural:**
- Storefront/Curtain Wall (`storefront|curtain.*wall|window.*wall`)
- Drywall Ceiling (`suspended.*ceil|gyp.*ceil|acoustical.*ceil.*grid`)
- Doors-GL (`glass.*door|aluminum.*storefront.*door`) — before generic Doors

**MEP / Fire:**
- Sprinkler Heads (`sprinkler.*head|fire.*sprinkler|pendent`)
- Fire Alarm (`smoke.*detector|fire.*alarm|horn.*strobe`)
- VAV (`\bvav\b|variable.*air.*volume`)
- RTU (`\brtu\b|rooftop.*unit|packaged.*unit`)
- Unit Heater (`unit.*heater|cabinet.*heater`)
- Refrigerant (`refrigerant.*line|ref.*line|line.*set`)
- Thermostat (`thermostat|t-stat`) — optional if needed for count

3. Preserve ordering: more specific patterns BEFORE broader ones.

4. Do NOT remove or weaken existing 52 entries.
  </action>
  <verify>python3 -c "from aggregator import ITEM_NAME_MAP; n=len(ITEM_NAME_MAP); assert n>=68, f'only {n} entries'"</verify>
  <done>≥68 ITEM_NAME_MAP entries; Masterv2 gaps filled</done>
</task>

<task type="auto">
  <name>Task 2: Aggregator tests for new canonical names</name>
  <files>tests/test_takeoff_generalization.py</files>
  <action>
Extend `test_takeoff_generalization.py` § aggregator tests (or add parametrized block):

Add ≥12 new cases mapping description strings → canonical names, e.g.:
- "18\" RCP storm sewer" → Storm Pipe
- "fire hydrant type B" → Fire Hydrants
- "VAV box size 6" → VAV Boxes
- "sprinkler head pendent" → Sprinkler Heads
- "storefront glazing system" → Storefront/Curtain Wall
- "chain link fence 6'" → Fence

No golden CSV coupling — synthetic description strings only.

Name new test functions/classes with `aggregator` in the identifier (e.g. `test_aggregator_mep_fire_suppression`) so `-k aggregator` collects them.
  </action>
  <verify>python3 -m pytest tests/test_takeoff_generalization.py -q -k aggregator</verify>
  <done>New patterns covered by unit tests; full generalization suite still passes</done>
</task>

</tasks>

<verification>
- [ ] `len(ITEM_NAME_MAP) >= 68`
- [ ] `pytest tests/test_takeoff_generalization.py -q` — 100% pass
- [ ] No "Crow" or "Bob" in aggregator.py
</verification>

<success_criteria>
ACCURACY-20-11 satisfied: ITEM_NAME_MAP covers full Masterv2 §C taxonomy within ±2 of ~70 target.
</success_criteria>

<output>
After completion, create `.planning/phases/20-takeoff-measurement-precision/20-09-SUMMARY.md`
</output>

---
phase: 20-takeoff-measurement-precision
plan: 14
type: execute
wave: 9
gap_closure: true
depends_on: ["20-11", "20-12", "20-13"]
files_modified:
  - .planning/phases/20-takeoff-measurement-precision/20-UAT.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true

must_haves:
  truths:
    - "GoldenValidator score >= 0.97 on Crow Cass when plans PDF present"
    - "GoldenValidator score >= 0.97 on Bob's Discount when plans PDF present"
    - "20-UAT.md and STATE.md reflect measured scores and auto-iteration outcome"
  artifacts:
    - path: .planning/phases/20-takeoff-measurement-precision/20-14-SUMMARY.md
      provides: "convergence run log and final scores"
---

<objective>
Auto-iteration wave: run golden_convergence, fix failures via general rules only, re-run until ≥97% on both fixtures or document blocking root cause (API credits, missing companion PDF).
</objective>

<execution_context>
@scripts/golden_convergence.py
@.planning/phases/20-takeoff-measurement-precision/20-CONTEXT.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Convergence loop until >= 97%</name>
  <files>claude_analyzer.py, calculator.py, companion_takeoff.py, aggregator.py</files>
  <action>
1. `export TAKEOFF_ACCURACY_MODE=high`

2. Run `python3 scripts/golden_convergence.py` 

3. If score &lt; 0.97: read failure report; apply allowed fixes (prompts, companion, legend, verify, profiles, ITEM_NAME_MAP)

4. Re-run up to 5 iterations in this task session

5. Forbidden: project-specific quantity constants

If API returns insufficient credits: stop and record blocker in SUMMARY (not a code failure)
  </action>
  <verify>python3 scripts/golden_convergence.py; echo exit=$?</verify>
  <done>Both fixtures >= 97% OR documented blocker with last scores</done>
</task>

<task type="auto">
  <name>Task 2: Update UAT, STATE, ROADMAP, SUMMARY</name>
  <files>.planning/phases/20-takeoff-measurement-precision/20-UAT.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
Record final golden scores, iteration count, companion PDF used (yes/no), accuracy mode.
Mark phase 20 human_verify complete if golden passes.
  </action>
  <verify>grep -q "Golden regression" .planning/phases/20-takeoff-measurement-precision/20-UAT.md</verify>
  <done>Planning docs match measured reality</done>
</task>

</tasks>

<verification>
python3 scripts/golden_convergence.py
pytest tests/test_takeoff_generalization.py tests/test_companion_takeoff.py -q
</verification>

---
phase: 20-takeoff-measurement-precision
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/crow_cass/crow_cass_golden.csv
  - tests/fixtures/bobs_discount/bobs_discount_golden.csv
  - tests/fixtures/generalization/
  - tests/golden_validator.py
  - tests/test_golden_takeoff.py
  - tests/test_takeoff_generalization.py
autonomous: true

must_haves:
  truths:
    - "GoldenValidator is reusable for ANY golden CSV — not Crow/Bob-specific"
    - "Synthetic JSON fixtures test calculator+aggregator for every sheet_type without PDFs"
    - "Golden PDF tests are regression fixtures; generalization tests run in CI without API keys"
  artifacts:
    - path: tests/golden_validator.py
      provides: "GoldenValidator with fuzzy match + tolerance modes"
    - path: tests/test_takeoff_generalization.py
      provides: "Sheet-type synthetic fixture tests (no PDF, no API)"
    - path: tests/fixtures/generalization/
      provides: "JSON extraction fixtures per sheet_type"
---

<objective>
Build the accuracy test harness in two layers:
1. **Generalization suite** — synthetic extraction JSON per sheet_type tests calculator + aggregator for ALL plan types (runs in CI, no API).
2. **Golden regression suite** — Crow Cass + Bob's Discount client reference CSVs validate end-to-end PDF pipeline (optional PDFs, skipif when absent).

Purpose: Golden files prove regression on known projects; generalization tests prove the engine works for any plan type.
</objective>

<context>
@.planning/phases/20-takeoff-measurement-precision/20-RESEARCH.md (§9, §11.10)
@tests/test_calculator_accuracy.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: GoldenValidator + golden CSVs (regression fixtures)</name>
  <files>tests/golden_validator.py, tests/fixtures/crow_cass/crow_cass_golden.csv, tests/fixtures/bobs_discount/bobs_discount_golden.csv</files>
  <action>
Create `GoldenValidator` (RESEARCH §9) — fully generic:
- Accepts any golden CSV path; no hardcoded item names
- `validate(takeoff_summary) -> {pass, score, items, missing, extra}`
- Fuzzy name match via difflib; tolerance modes `exact_or_within_1` and `pct`
- Pass threshold configurable (default 0.97)

Create golden CSVs from 20-CONTEXT.md (Crow 10 items, Bob 12 items) — these are **regression fixtures only**.

Create `tests/test_golden_takeoff.py`:
- `@pytest.mark.golden` integration tests calling `run_pdf_analysis`
- skipif when PDF absent
- `test_golden_validator_logic` unit test (always runs)
  </action>
  <verify>python3 -m pytest tests/test_golden_takeoff.py -q -k "validator_logic"</verify>
  <done>Validator is project-agnostic; golden CSVs exist as regression data</done>
</task>

<task type="auto">
  <name>Task 2: Generalization synthetic fixtures + test_takeoff_generalization.py</name>
  <files>tests/fixtures/generalization/, tests/test_takeoff_generalization.py</files>
  <action>
Create synthetic extraction JSON fixtures (RESEARCH §11.10) — **no PDF, no Claude API**:

```
tests/fixtures/generalization/
  floor_plan_retail.json      # rooms with VCT notes → Flooring
  floor_plan_industrial.json  # room 397556 SF, notes "sealed concrete" → Sealed Concrete
  elevation_commercial.json   # lintel_runs, eifs areas
  civil_site.json             # pipe_runs storm, catch_basins, striping LF
  schedule_doors.json         # door schedule HM/WD rows
  mep_roof_gas.json           # pipe_runs black steel gas
  detail_ladder.json          # component ladder qty=1
  expected/                   # expected takeoff_summary rows per fixture
```

Create `tests/test_takeoff_generalization.py`:
- For each fixture: `apply_estimation_tables(json)` → `aggregate_takeoff()` → compare to expected/
- Tests verify:
  - Industrial room with sealed concrete notes does NOT produce Flooring
  - Civil pipe run produces storm_pipe LF, not storm from GL elevation
  - Door schedule produces Doors-HM / Doors-WD separately
  - Component with null qty produces nothing (until count pass fills it — tested after 20-03)
  - Schedule spec_reference tables produce zero calculation rows

These tests run on every CI build without API keys.
  </action>
  <verify>python3 -m pytest tests/test_takeoff_generalization.py -q</verify>
  <done>Generalization tests pass for 6+ sheet_type scenarios</done>
</task>

</tasks>

<success_criteria>
Two-layer test strategy: generalization (always-on, synthetic) + golden regression (PDF, skipif). Neither layer hardcodes business logic — only validates outputs.
</success_criteria>

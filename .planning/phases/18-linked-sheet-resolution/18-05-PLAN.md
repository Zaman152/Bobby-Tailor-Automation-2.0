---
phase: 18-linked-sheet-resolution
plan: 05
type: execute
wave: 5
depends_on:
  - 18-04
files_modified:
  - tests/test_linked_sheets.py
  - README.md
  - .planning/phases/18-linked-sheet-resolution/18-UAT.md
autonomous: false

must_haves:
  truths:
    - "Integration test: 2 selected pages + 1 unresolved ref → linked page captured, cross-ref resolves to 'resolved'"
    - "Integration test: MAX_LINKED_SHEETS=1 with 2 refs → only 1 linked page added, warning logged"
    - "Integration test: AUTO_INCLUDE_LINKED_SHEETS=false → linked_sheets_suggested populated, none captured"
    - "README documents linked sheet behavior under Production takeoff runs"
    - "18-UAT.md checklist signed off by operator"
  artifacts:
    - path: "tests/test_linked_sheets.py"
      provides: "Integration-level tests for linked sheet pipeline"
      contains: "test_integration"
    - path: "README.md"
      provides: "Linked sheet section"
      contains: "AUTO_INCLUDE_LINKED_SHEETS"
    - path: ".planning/phases/18-linked-sheet-resolution/18-UAT.md"
      provides: "Human-acceptance checklist"
      contains: "PASS/FAIL"
  key_links:
    - from: "tests/test_linked_sheets.py integration tests"
      to: "scraper._discover_and_add_linked_sheets"
      via: "mock browser, mock stackct_store.get_plans"
      pattern: "_discover_and_add_linked_sheets"
---

<objective>
Integration tests, README documentation, and UAT checklist for Phase 18.

Purpose: Verify the full linked-sheet pipeline end-to-end with mocked browser and
catalog; document behavior for operators; provide a UAT sign-off gate before this
phase is declared complete.

Output: Extended `tests/test_linked_sheets.py` with integration tests, README section,
and `18-UAT.md` with human-verify checkpoint.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-linked-sheet-resolution/18-RESEARCH.md
@.planning/phases/18-linked-sheet-resolution/18-04-SUMMARY.md
@tests/test_linked_sheets.py
@scraper.py
@linked_sheets.py
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration tests for linked sheet pipeline</name>
  <files>tests/test_linked_sheets.py</files>
  <action>
Append integration-level tests to the existing `tests/test_linked_sheets.py`.

These tests mock the browser and `stackct_store.get_plans` to test the full
`_discover_and_add_linked_sheets` function synchronously (use `pytest-asyncio` or
`asyncio.run`).

**Setup:** Add imports:
```python
import asyncio
import pytest
from unittest.mock import AsyncMock, MagicMock, patch
from pathlib import Path
import tempfile
from scraper import _discover_and_add_linked_sheets
from capture_manifest import PageEntry, RunManifest
```

**`test_integration_linked_page_captured_and_analyzed`:**
- Mock catalog: `[{"page_id": 201, "sheet_name": "C-4 - CIVIL SITE PLAN", "folder_id": 1}]`
- Mock all_extracted: 1 extraction with `cross_references: [{"ref_sheet": "C-4", ...}]`
  and `_source_sheet: "A-1 - ARCH PLAN"`
- Manifest: 1 page (page_id=101) already in manifest, capture_status="ok"
- Mock `stackct_store.get_plans` to return catalog
- Mock `_capture_sheet_screenshot` to return (True, None)
- Mock `analyze_drawing` to return `{"measurements": [], "components": [], "_source_sheet": "C-4 - CIVIL SITE PLAN"}`
- Mock `browser.start`, `browser.login` as AsyncMocks returning True
- Mock `browser.close` as AsyncMock
- Call `_discover_and_add_linked_sheets(...)` with `AUTO_INCLUDE_LINKED_SHEETS=True, MAX_LINKED_SHEETS=10`
- Assert: `linked_meta` has 1 entry with `page_id=201`
- Assert: `new_extracted` has 1 entry

**`test_integration_max_linked_sheets_truncates`:**
- Catalog: 3 pages (C-4, C-5, C-6)
- all_extracted: 3 refs pointing to those 3 sheets
- `MAX_LINKED_SHEETS=1` (patch config)
- Assert: only 1 page captured; `linked_meta` has 1 entry

**`test_integration_auto_include_false_suggests_only`:**
- Same setup as first test but `AUTO_INCLUDE_LINKED_SHEETS=False`
- Assert: `new_extracted` is empty (no capture)
- Assert: `linked_meta[0]["suggested_only"] == True`

**`test_integration_empty_catalog_returns_empty`:**
- Mock `stackct_store.get_plans` returns `[]`
- Assert: all return values are empty lists

Use `pytest.mark.asyncio` if available, otherwise `asyncio.run()` inside sync test.
  </action>
  <verify>
    pytest tests/test_linked_sheets.py -v -k "integration"
  </verify>
  <done>
    All 4 integration tests pass (or are properly skipped if AsyncMock setup requires
    pytest-asyncio which may need to be installed — note in SUMMARY if so).
  </done>
</task>

<task type="auto">
  <name>Task 2: README section + 18-UAT.md</name>
  <files>README.md, .planning/phases/18-linked-sheet-resolution/18-UAT.md</files>
  <action>
**README.md** — Find the "Production takeoff runs" section (or similar section about
running scrapes). Add a new subsection after it:

```markdown
### Linked Sheet Auto-Follow (Phase 18)

When a drawing sheet references a detail on another sheet (e.g., detail bubble "17 / C-4"),
the scraper automatically discovers and captures the linked sheet before resolving
cross-references.

**Configuration:**

| Variable | Default | Description |
|---|---|---|
| `AUTO_INCLUDE_LINKED_SHEETS` | `true` | Capture and analyze linked sheets automatically |
| `MAX_LINKED_SHEETS` | `10` | Maximum linked sheets added per run (cost guard) |
| `MAX_LINKED_DEPTH` | `1` | Recursion depth (v1: no recursive follow) |

**Behavior when `AUTO_INCLUDE_LINKED_SHEETS=true`:**
- After analyzing selected sheets, unresolved `ref_sheet` codes are collected
- Linked page_ids are matched from the StackCT catalog (same folder only)
- Linked pages are captured and analyzed in a second pass
- `takeoff.json` includes `linked_sheets_added[]` with `{page_id, sheet_name, ref_from}`
- Cross-references previously marked `target_sheet_not_found` may now resolve

**Behavior when `AUTO_INCLUDE_LINKED_SHEETS=false`:**
- No extra capture; `takeoff.json` includes `linked_sheets_suggested[]` for manual follow-up

**Limits:**
- Only sheets in the same `folder_id` are followed
- Recursive link-following (depth > 1) is not supported in v1
```

**`.planning/phases/18-linked-sheet-resolution/18-UAT.md`** — Create UAT checklist:

```markdown
# Phase 18 UAT: Linked Sheet Auto-Follow

## Pre-conditions
- Phase 17 pipeline working (manifest, two-phase capture)
- SQLite catalog populated for test project (run sync first)
- Test project has at least one sheet with a detail bubble referencing another sheet

## Checklist

### LINK-01: Catalog Matcher
- [ ] Run `python -c "from linked_sheets import match_ref_to_page; ..."` with real sheet name → correct page_id
- [ ] Ambiguous match logs WARNING with near-misses
- [ ] No-match returns None (no crash)

### LINK-02: Ref Collection
- [ ] `collect_unresolved_refs` finds refs in `cross_references[]`
- [ ] `collect_unresolved_refs` finds refs in `civil_structures[].detail_ref_sheet`
- [ ] Duplicates deduplicated

### LINK-03: AUTO_INCLUDE_LINKED_SHEETS=false
- [ ] Run with `AUTO_INCLUDE_LINKED_SHEETS=false`
- [ ] `takeoff.json` has non-empty `linked_sheets_suggested[]`
- [ ] `linked_sheets_added[]` is empty
- [ ] No extra browser session opened

### LINK-04: Full Pipeline (AUTO_INCLUDE=true)
- [ ] Select 2-3 sheets that contain cross-reference bubbles
- [ ] Job completes; job monitor shows "Adding N linked detail sheets" phase
- [ ] `takeoff.json` `linked_sheets_added` lists the auto-added page(s)
- [ ] Cross-reference entry status changes from `target_sheet_not_found` to `resolved` or `target_found_detail_missing`
- [ ] `linked_sheets_count` visible in job monitor UI

### LINK-05: MAX_LINKED_SHEETS cap
- [ ] Set `MAX_LINKED_SHEETS=1` with project having 3+ refs
- [ ] Only 1 linked sheet added; WARNING logged about truncation

### LINK-06: Partial + Cancel safety
- [ ] Cancel job during linked capture phase → partial report generated, no crash
- [ ] Failed linked capture → main report still generated from original sheets

## Sign-off
- [ ] All checklist items confirmed PASS
- Signed off by: _______________
- Date: _______________
```
  </action>
  <verify>
    grep -n "AUTO_INCLUDE_LINKED_SHEETS" README.md
    test -f .planning/phases/18-linked-sheet-resolution/18-UAT.md && echo "UAT file exists"
  </verify>
  <done>
    README has linked-sheet section with config table; 18-UAT.md exists with full checklist.
  </done>
</task>

</tasks>

<verification>
1. `pytest tests/test_linked_sheets.py -v` — all unit + integration tests pass
2. `grep "AUTO_INCLUDE_LINKED_SHEETS" README.md` — section present
3. `test -f .planning/phases/18-linked-sheet-resolution/18-UAT.md && echo OK`
4. `ruff check tests/test_linked_sheets.py` — zero errors
</verification>

<success_criteria>
- All 4 integration tests pass (or documented as requiring pytest-asyncio install)
- README has linked-sheet configuration table and behavior description
- 18-UAT.md has all 6 scenario groups with checkboxes
</success_criteria>

<checkpoint type="human-verify" gate="blocking">
  <what-built>
    Phase 18 complete: linked_sheets.py matcher, scraper Pass 2a/2b/2c, config vars,
    reporter JSON fields, /api/status linked_sheets_count, monitor UI notice, tests, README, UAT.
  </what-built>
  <how-to-verify>
    1. Run `pytest tests/test_linked_sheets.py -v` — confirm all tests pass
    2. Start Flask: `python app.py`
    3. Select a project that has cross-reference bubbles in sheets, run a job
    4. Watch monitor: confirm "Adding linked detail sheets" phase appears
    5. After job completes, open takeoff.json — confirm `linked_sheets_added` array
    6. Confirm previously `target_sheet_not_found` refs now show `resolved`
    7. Run with `AUTO_INCLUDE_LINKED_SHEETS=false` — confirm `linked_sheets_suggested` populated instead
    8. Sign off 18-UAT.md
  </how-to-verify>
  <resume-signal>
    Type "approved" when UAT passes, or describe specific failures for follow-up.
  </resume-signal>
</checkpoint>

<output>
After completion, create `.planning/phases/18-linked-sheet-resolution/18-05-SUMMARY.md`
</output>

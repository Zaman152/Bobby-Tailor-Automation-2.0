---
phase: 01-config-and-safe-operations
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "App loads .env from project root via Path(__file__).parent — no machine-specific paths"
    - "Missing STACKCT_EMAIL, STACKCT_PASSWORD, or ANTHROPIC_API_KEY fails at import/startup with a clear message"
    - "No real email or secret is hardcoded as a default in config.py"
  artifacts:
    - path: config.py
      provides: "Validated environment constants"
      contains: "validate_required_env"
  key_links:
    - from: config.py
      to: .env
      via: "load_dotenv + Path(__file__).parent"
      pattern: "load_dotenv"
---

<objective>
Harden `config.py` so environment configuration is portable, fail-fast, and free of unsafe defaults.

Purpose: FOUND-01 (verify/complete) — relative `.env` already works; this plan removes the hardcoded email default and adds startup validation.

Output: `config.py` with `validate_required_env()`, no sensitive defaults, optional cwd fallback for `.env`.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-config-and-safe-operations/01-RESEARCH.md
@config.py
@Master.md (Section 7 Feature 1.3)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and complete relative .env loading</name>
  <files>config.py</files>
  <action>
Confirm `_env_path = Path(__file__).resolve().parent / ".env"` is used. Add cwd fallback per Master.md if project-root `.env` missing: `Path.cwd() / ".env"`. Keep `load_dotenv(..., override=True)`. Do not reintroduce any absolute Mac path.
  </action>
  <verify>grep shows no `/Users/` paths in config.py; `python3 -c "import config"` succeeds when `.env` exists in project root</verify>
  <done>FOUND-01 path loading is explicit and portable</done>
</task>

<task type="auto">
  <name>Task 2: Remove unsafe defaults and add validate_required_env()</name>
  <files>config.py</files>
  <action>
Remove default `STACKCT_EMAIL = os.getenv(..., "muhammad@klouded.com")` — use empty string or None then validate. Add `REQUIRED_ENV_VARS = ["STACKCT_EMAIL", "STACKCT_PASSWORD", "ANTHROPIC_API_KEY"]` and `validate_required_env()` that raises `ValueError` listing missing vars. Call `validate_required_env()` at end of config module (after all getenv calls). Keep sensible defaults only for non-secrets: CLAUDE_MODEL, HEADLESS, OUTPUT_DIR, RUN_SCHEDULE.
  </action>
  <verify>`python3 -c "import config"` fails with clear message when a required var is unset (test by temporarily renaming a key in a subprocess); succeeds with full `.env`</verify>
  <done>App cannot start silently with missing credentials</done>
</task>

</tasks>

<verification>
Fresh clone + `.env` from `.env.example` → `import config` works. Missing any required var → immediate ValueError with variable names listed.
</verification>

<success_criteria>
FOUND-01 satisfied; no hardcoded credentials; fail-fast validation on startup.
</success_criteria>

<output>
After completion, create `.planning/phases/01-config-and-safe-operations/01-01-SUMMARY.md`
</output>

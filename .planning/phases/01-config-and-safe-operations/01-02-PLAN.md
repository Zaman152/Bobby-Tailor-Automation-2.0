---
phase: 01-config-and-safe-operations
plan: 2
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app.py
  - project_cache.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "HTTP 500 responses from Flask routes return generic JSON, never raw exception strings or tracebacks"
    - "Job status API returns user-safe error messages; full details only in server logs"
    - "/api/projects never returns str(e) from a failed StackCT fetch"
  artifacts:
    - path: app.py
      provides: "Global error handlers and sanitized job errors"
      contains: "errorhandler"
  key_links:
    - from: app.py
      to: project_cache.py
      via: "get_projects() returns safe error key"
      pattern: "error"
---

<objective>
Sanitize all user-facing API error paths so internal exception details never reach the browser.

Purpose: FOUND-02 — fix `jobs[job_id]["error"] = str(e)`, `project_cache` `str(e)` responses, and add Flask global handlers for route exceptions.

Output: Centralized error handling in `app.py`; generic messages for job failures and project fetch failures.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-config-and-safe-operations/01-RESEARCH.md
@app.py
@project_cache.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Flask global error handlers</name>
  <files>app.py</files>
  <action>
Import `HTTPException` from werkzeug.exceptions. Register `@app.errorhandler(HTTPException)` returning `jsonify({"error": e.name, "message": e.description}), e.code`. Register `@app.errorhandler(Exception)` logging with `logger.error(..., exc_info=True)` and returning `jsonify({"error": "Internal server error", "message": "An unexpected error occurred"}), 500`. Do not enable debug mode changes — handlers work in production.
  </action>
  <verify>Add a temporary test route or trigger 404 — response JSON has no file paths. Unhandled exception in a route returns generic 500 JSON.</verify>
  <done>All Flask route exceptions sanitized at the edge</done>
</task>

<task type="auto">
  <name>Task 2: Sanitize background job and project-cache errors</name>
  <files>app.py, project_cache.py</files>
  <action>
In `_stackct_job` and `_pdf_job`: keep `logger.exception(...)` but set `jobs[job_id]["error"]` to a fixed user-safe string (e.g. "The job failed. Check server logs for details."). Optionally map known failures (login_failed, no_pages) to specific short messages without exposing library names.

In `project_cache.get_projects()`: replace `return {"error": str(e), ...}` with `return {"error": "Could not fetch projects from StackCT. Try again or check credentials.", "from_cache": False}` while keeping `logger.error(f"Live fetch failed: {e}")`.

Do NOT change `claude_analyzer.py` pipeline error dicts in this plan — those are internal to scrape flow, not HTTP responses (scraper logs warnings only).
  </action>
  <verify>Simulate job failure — `/api/status/<id>` error field has no `playwright`, `Traceback`, or file paths. Force project fetch failure — `/api/projects` error is generic string.</verify>
  <done>FOUND-02 satisfied for all HTTP-facing error paths</done>
</task>

</tasks>

<verification>
curl job status after forced failure — no stack trace in JSON. Server log contains full exception.
</verification>

<success_criteria>
FOUND-02 complete; operators see helpful generic errors; developers see full logs server-side.
</success_criteria>

<output>
After completion, create `.planning/phases/01-config-and-safe-operations/01-02-SUMMARY.md`
</output>

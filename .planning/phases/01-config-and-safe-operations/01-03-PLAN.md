---
phase: 01-config-and-safe-operations
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - requirements.txt
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Every os.getenv() in config.py has a documented counterpart in .env.example"
    - "requirements.txt lists all runtime deps including Pillow with minimum versions"
  artifacts:
    - path: .env.example
      provides: "Complete env var template for new installs"
      contains: "ANTHROPIC_API_KEY"
    - path: requirements.txt
      provides: "Pinned runtime dependencies"
      contains: "pillow"
  key_links:
    - from: .env.example
      to: config.py
      via: "variable name parity"
      pattern: "STACKCT_EMAIL"
---

<objective>
Complete deployment documentation artifacts so a new developer can clone, copy `.env.example` → `.env`, and install without guesswork.

Purpose: DEPLOY-01 and DEPLOY-03 — audit and complete `.env.example`; verify `requirements.txt` is complete.

Output: Updated `.env.example` with all config vars and comments; verified `requirements.txt`.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@config.py
@.env.example
@requirements.txt
@Master.md (Section 11.3)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and complete .env.example</name>
  <files>.env.example</files>
  <action>
Compare every `os.getenv()` in config.py against `.env.example`. Add any missing vars: RUN_SCHEDULE (with cron comment), PAGE_LOAD_TIMEOUT if exposed (skip if not in config). Ensure each var has a one-line description. Use placeholder values only (never real credentials). Group sections: StackCT, Anthropic, Browser, Output, Schedule.
  </action>
  <verify>Manual checklist: STACKCT_EMAIL, STACKCT_PASSWORD, ANTHROPIC_API_KEY, CLAUDE_MODEL, CLAUDE_MODEL_SCHEDULES, HEADLESS, OUTPUT_DIR, RUN_SCHEDULE all present with comments</verify>
  <done>DEPLOY-01 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Verify requirements.txt completeness</name>
  <files>requirements.txt</files>
  <action>
Confirm runtime deps match imports across app: playwright, anthropic, apscheduler, python-dotenv, flask, pymupdf, pillow. Keep `>=` minimum versions. Do not add dev-only tools. If Pillow already present, ensure version `>=10.0.0` documented in comment at top of file (optional one-line comment).
  </action>
  <verify>`pip install -r requirements.txt` in clean venv succeeds; `pip show Pillow` shows installed package</verify>
  <done>DEPLOY-03 satisfied</done>
</task>

</tasks>

<verification>
New developer flow: `cp .env.example .env` → fill values → `pip install -r requirements.txt` → no missing-var surprises beyond validate_required_env from plan 01-01.
</verification>

<success_criteria>
.env.example is complete; requirements.txt installs full runtime stack including Pillow.
</success_criteria>

<output>
After completion, create `.planning/phases/01-config-and-safe-operations/01-03-SUMMARY.md`
</output>

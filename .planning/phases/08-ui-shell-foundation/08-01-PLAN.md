# Plan 08-01: Base Layout Template + Sidebar

**Phase:** 08 — UI Shell Foundation
**Requirement:** UI-01 (partial)
**Reference:** Master.md §8.2

---

## Objective

Replace tab-based horizontal navigation with fixed sidebar + main content layout matching Master.md specification.

---

## Current State

```html
<header>BT Logo + Title</header>
<div class="container">
  <div class="tabs">[StackCT][PDF][Reports]</div>
  <div id="tab-stackct">...</div>
  <div id="tab-pdf">...</div>
  <div id="tab-reports">...</div>
  <div id="progressCard">...</div>
</div>
```

## Target State

```html
<div class="app-shell">
  <aside class="sidebar">
    <div class="sidebar-brand">BT Logo + Bobby Tailor</div>
    <nav class="sidebar-nav">
      <a class="nav-item active" data-page="projects">Projects</a>
      <a class="nav-item" data-page="pdf">PDF Upload</a>
      <a class="nav-item" data-page="reports">Reports</a>
      <a class="nav-item" data-page="settings">Settings</a>
    </nav>
    <div class="sidebar-footer">
      <div class="job-mini-card" id="sidebarJobCard"><!-- JOB-04 slot --></div>
    </div>
  </aside>
  <main class="main-content">
    <header class="page-header">
      <h1 id="pageTitle">StackCT Projects</h1>
    </header>
    <div class="page-body">
      <section id="page-projects" class="page-section active">...</section>
      <section id="page-pdf" class="page-section">...</section>
      <section id="page-reports" class="page-section">...</section>
      <section id="page-settings" class="page-section">...</section>
      <div id="progressCard">...</div>
    </div>
  </main>
</div>
```

---

## Tasks

### T1: Add shell container structure

**File:** `templates/index.html`

1. Wrap entire `<body>` content in `<div class="app-shell">`
2. Create `<aside class="sidebar">` before main content
3. Move logo/branding from `<header>` into sidebar

**Estimate:** Small

---

### T2: Build sidebar navigation

**File:** `templates/index.html`

1. Add `<nav class="sidebar-nav">` with 4 nav items:
   - Projects (data-page="projects") — icon: 📋
   - PDF Upload (data-page="pdf") — icon: 📄
   - Reports (data-page="reports") — icon: 📊
   - Settings (data-page="settings") — icon: ⚙️
2. Add `active` class to Projects by default
3. Add divider before Settings item

**Estimate:** Small

---

### T3: Add job mini-card slot in sidebar footer

**File:** `templates/index.html`

1. Add `<div class="sidebar-footer">` at bottom of sidebar
2. Include empty `<div class="job-mini-card" id="sidebarJobCard"></div>`
3. Placeholder for JOB-04 (Phase 7) to populate later

**Estimate:** Trivial

---

### T4: Create main content structure

**File:** `templates/index.html`

1. Create `<main class="main-content">`
2. Add `<header class="page-header">` with dynamic `<h1 id="pageTitle">`
3. Add `<div class="page-body">` containing page sections

**Estimate:** Small

---

### T5: Convert tab divs to page sections

**File:** `templates/index.html`

1. Rename `id="tab-stackct"` → `id="page-projects"`, add `class="page-section active"`
2. Rename `id="tab-pdf"` → `id="page-pdf"`, add `class="page-section"`
3. Rename `id="tab-reports"` → `id="page-reports"`, add `class="page-section"`
4. Create empty `id="page-settings"` section (placeholder)
5. Keep `progressCard` in page-body (shared across pages)

**Estimate:** Small

---

### T6: Remove old header and tabs

**File:** `templates/index.html`

1. Remove standalone `<header>` element (branding moved to sidebar)
2. Remove `.tabs` container entirely
3. Remove `.container` wrapper (no longer needed)

**Estimate:** Trivial

---

### T7: Add sidebar CSS layout

**File:** `templates/index.html` (inline CSS for now; extracted in 08-03)

```css
.app-shell {
  display: flex;
  min-height: 100vh;
}

.sidebar {
  width: 240px;
  flex-shrink: 0;
  background: var(--bg-surface);
  border-right: 1px solid var(--border-subtle);
  display: flex;
  flex-direction: column;
}

.sidebar-brand {
  padding: 20px;
  border-bottom: 1px solid var(--border-subtle);
}

.sidebar-nav {
  flex: 1;
  padding: 16px 12px;
}

.nav-item {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 10px 14px;
  border-radius: 8px;
  color: var(--text-secondary);
  text-decoration: none;
  font-size: 14px;
  cursor: pointer;
  transition: all 0.15s;
}

.nav-item:hover {
  background: var(--bg-elevated);
  color: var(--text-primary);
}

.nav-item.active {
  background: rgba(59, 130, 246, 0.15);
  color: var(--accent-primary);
}

.sidebar-footer {
  padding: 16px;
  border-top: 1px solid var(--border-subtle);
}

.main-content {
  flex: 1;
  display: flex;
  flex-direction: column;
  overflow-y: auto;
}

.page-header {
  padding: 24px 32px 0;
}

.page-body {
  flex: 1;
  padding: 24px 32px;
  max-width: 960px;
}

.page-section {
  display: none;
}

.page-section.active {
  display: block;
}
```

**Estimate:** Medium

---

### T8: Update navigation JavaScript

**File:** `templates/index.html` (inline JS for now; extracted in 08-03)

Replace `switchTab()` with:

```javascript
function navigateTo(pageName) {
  // Hide all sections
  document.querySelectorAll('.page-section').forEach(s => s.classList.remove('active'));
  // Show target section
  const target = document.getElementById('page-' + pageName);
  if (target) target.classList.add('active');
  
  // Update nav active state
  document.querySelectorAll('.nav-item').forEach(n => n.classList.remove('active'));
  document.querySelector(`.nav-item[data-page="${pageName}"]`)?.classList.add('active');
  
  // Update page title
  const titles = { projects: 'StackCT Projects', pdf: 'Upload PDF', reports: 'Reports', settings: 'Settings' };
  document.getElementById('pageTitle').textContent = titles[pageName] || pageName;
  
  // Trigger data loads
  if (pageName === 'reports') loadReports();
  if (pageName === 'projects' && !projectsLoaded) loadProjects();
}

// Bind nav clicks
document.querySelectorAll('.nav-item').forEach(item => {
  item.addEventListener('click', () => navigateTo(item.dataset.page));
});
```

**Estimate:** Small

---

### T9: Add Settings placeholder page

**File:** `templates/index.html`

```html
<section id="page-settings" class="page-section">
  <div class="card">
    <h2>Settings</h2>
    <p class="empty">Settings will be available in Phase 6.</p>
  </div>
</section>
```

**Estimate:** Trivial

---

## Verification

| Check | Method |
|-------|--------|
| Sidebar renders at 240px fixed width | Browser inspect |
| Nav items highlight on click | Click each nav item |
| Page sections toggle correctly | Navigate between all 4 pages |
| Progress card remains visible when job starts | Start a job, check visibility |
| No console JS errors | DevTools console |
| Existing project/PDF/reports functionality intact | Run quick smoke test |

---

## Dependencies

- None (Phase 1 complete, no other blockers)

## Blocked By

- None

## Blocks

- 08-02 (theme tokens can apply to new layout)
- 08-03 (CSS extraction includes new layout styles)

---

*Plan created: 2026-05-26*

---
phase: 14-stackct-plan-sets
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - browser.py
  - tests/test_plan_sets.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "browser.get_plan_sets returns deduplicated folder list per project"
    - "browser.get_page_ids_in_folder returns sheets only after clicking that folder"
    - "Projects with no folder cards get a single direct-grid fallback set"
  artifacts:
    - path: browser.py
      provides: "get_plan_sets, get_page_ids_in_folder"
      contains: "def get_plan_sets"
    - path: tests/test_plan_sets.py
      provides: "dedupe unit tests from 14-DISCOVERY audit fixtures"
  key_links:
    - from: browser.py
      to: "StackCT [data-folder-id]"
      via: "click folder then query [data-page-id]"
      pattern: "data-folder-id"
---

<objective>
Implement StackCT plan-set discovery and folder-scoped page listing in `browser.py`, with dedupe rules validated against the 7-project audit.

Purpose: Foundation for Phase 14 — correct hierarchy before DB/UI changes.

Output: `get_plan_sets(project_id)`, `get_page_ids_in_folder(project_id, folder_id)`, deprecate flat `get_all_page_ids` to call fallback path only.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-stackct-plan-sets/14-RESEARCH.md
@.planning/phases/14-stackct-plan-sets/14-DISCOVERY.md
@scripts/audit_plan_sets.py
@browser.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plan-set discovery and dedupe</name>
  <files>browser.py</files>
  <action>
1. Add `normalize_plan_sets(raw: list[dict]) -> list[dict]` (module-level or static) implementing dedupe rules from `14-DISCOVERY.md`:
   - Skip Bookmarks / Supporting Documents / bare "Plans"
   - Drop "Plans X" parent when child "X" exists with same sheet_count
   - Drop aggregate names containing multiple issue labels (heuristic: both "v1" and "v2" in one name)

2. Add `async def get_plan_sets(self, project_id: int) -> list[dict]`:
   - Navigate `#/Takeoff/{project_id}`, wait for DOM
   - `evaluate` all `[data-folder-id]` candidates with short names
   - `normalize_plan_sets`
   - For each set: click folder, count `[data-page-id]`, attach `sheet_count`
   - Return `[{folder_id, name, sheet_count}, ...]`

3. **Direct-grid fallback** (ATL 081 pattern): if zero sets after dedupe but `[data-page-id]` exists on page (retry wait up to 30s), return single synthetic set:
   `{folder_id: 0, name: "All drawing sheets", sheet_count: N}`

4. Keep `get_all_page_ids` as thin wrapper: `get_plan_sets` → if one set, return its pages; if multiple, return [] and log warning (callers must use folder API). Or: if only set folder_id==0, scrape landing grid without click.
  </action>
  <verify>python3 -c "from browser import StackCTBrowser; print('import ok')" && python3 -m pytest tests/test_plan_sets.py -q</verify>
  <done>get_plan_sets exists; dedupe tests pass; manual script on 7416168 returns 2 sets (v1=120, v2=180)</done>
</task>

<task type="auto">
  <name>Task 2: Folder-scoped page IDs + tests</name>
  <files>browser.py, tests/test_plan_sets.py</files>
  <action>
1. Add `async def get_page_ids_in_folder(self, project_id: int, folder_id: int) -> list[dict]`:
   - Goto project takeoff URL
   - If `folder_id == 0`: scrape landing grid (direct-grid fallback)
   - Else: click `[data-folder-id="{folder_id}"]`, wait, run existing `[data-page-id]` evaluate logic (reuse from current `get_all_page_ids`)

2. Add `tests/test_plan_sets.py` with fixtures from `14-DISCOVERY.json` (Morehouse 3 raw → 2 deduped; Baking Social duplicate parent dropped). No live browser in unit tests.

3. Update `scripts/discover_plan_sets.py` to call new APIs (optional cleanup).
  </action>
  <verify>python3 -m pytest tests/test_plan_sets.py -q</verify>
  <done>get_page_ids_in_folder returns page list; tests cover dedupe without Playwright</done>
</task>

</tasks>

<verification>
- [ ] Morehouse: `get_plan_sets` → 2 sets with correct IDs and counts
- [ ] Bid for Baking Social: 1–2 deduped sets (not duplicate "Plans …" row)
- [ ] ATL 081: fallback single set OR real folders after longer wait (document in SUMMARY)
</verification>

<success_criteria>
- Plan-set discovery matches StackCT folder cards user sees
- Page scrape is folder-scoped, not project-wide merge
</success_criteria>

<output>
After completion, create `.planning/phases/14-stackct-plan-sets/14-01-SUMMARY.md`
</output>

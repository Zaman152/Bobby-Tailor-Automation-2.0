---
phase: 14-stackct-plan-sets
plan: 3
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - app.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "GET /api/projects/<id>/plan-sets returns folder list with counts"
    - "GET /api/projects/<id>/plan-sets/<folder_id>/plans returns sheets for one set only"
    - "POST run/stackct accepts folder_id and validates page_ids belong to that folder"
  artifacts:
    - path: app.py
      provides: "plan-sets routes"
      contains: "/plan-sets"
  key_links:
    - from: app.py
      to: project_cache.get_project_plan_sets
      via: "jsonify"
      pattern: "get_project_plan_sets"
---

<objective>
Expose folder-first StackCT catalog via REST APIs; enforce folder_id on plan fetch and run jobs.

Purpose: Frontend and scraper consume plan sets before sheets.

Output: New routes; deprecate flat `/plans` to require `folder_id` query param with 400 if missing.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-stackct-plan-sets/14-02-SUMMARY.md
@app.py
@scraper.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plan-set and folder-scoped plans APIs</name>
  <files>app.py</files>
  <action>
1. `GET /api/projects/<int:project_id>/plan-sets` — `get_project_plan_sets`; support `?refresh=1`

2. `GET /api/projects/<int:project_id>/plan-sets/<int:folder_id>/plans` — `get_project_plans(project_id, folder_id)`; `?refresh=1`, background sync + `syncing` flag (reuse Phase 13 pattern)

3. `POST /api/projects/<int:project_id>/sync-plan-sets` — warm plan set index

4. Update `POST /api/projects/<int:project_id>/sync-plans` → require `folder_id` in JSON body or query

5. **Backward compat:** `GET /api/projects/<id>/plans`:
   - If `folder_id` query present → delegate to folder route
   - Else return `400` with message: `folder_id required; call /plan-sets first` (or auto-return plan-sets list if `?legacy=1` — prefer strict)

6. Update `GET /api/projects/sheet-counts` → return `plan_set_counts: {project_id: N}` and optional per-folder breakdown from DB
  </action>
  <verify>curl -s http://127.0.0.1:5050/api/projects/7416168/plan-sets | head -c 200</verify>
  <done>New routes registered; old /plans requires folder_id</done>
</task>

<task type="auto">
  <name>Task 2: Run API folder validation</name>
  <files>app.py, scraper.py</files>
  <action>
1. `/api/run/stackct` body: add optional `folder_id` (required when `mode=specific` and using page_ids)

2. `_stackct_job` / `scraper.run_project_scrape`: pass `folder_id`; when discovering pages use `get_page_ids_in_folder` not flat get_all

3. Validate submitted `page_ids` ⊆ plans for that folder in DB (or re-fetch if stale)

4. Store `folder_id` and plan set `name` in job metadata / `takeoff.json` report root
  </action>
  <verify>grep -n folder_id app.py scraper.py</verify>
  <done>Runs scoped to folder; page_ids validated</done>
</task>

</tasks>

<verification>
- [ ] curl plan-sets for Morehouse returns 2 entries
- [ ] curl plans for folder 35240700 returns 120 plans only
- [ ] run with wrong folder page_id rejected
</verification>

<success_criteria>
- API surface matches two-step UX contract
- Run pipeline cannot mix folders silently
</success_criteria>

<output>
After completion, create `.planning/phases/14-stackct-plan-sets/14-03-SUMMARY.md`
</output>

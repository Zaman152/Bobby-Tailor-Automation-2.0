---
phase: 14-stackct-plan-sets
plan: 4
type: execute
wave: 4
depends_on: ["14-03"]
files_modified:
  - static/app.js
  - templates/index.html
  - static/style.css
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Preview Plans shows plan set picker before sheet checklist"
    - "Project list shows plan set count (e.g. 2 sets) not misleading single sheet total"
    - "Run Selected sends folder_id and page_ids to API"
  artifacts:
    - path: static/app.js
      provides: "two-step preview flow"
      contains: "fetchPlanSets"
    - path: templates/index.html
      provides: "plan set panel markup"
      contains: "planSetList"
  key_links:
    - from: static/app.js
      to: "/api/projects/*/plan-sets"
      via: "fetch"
      pattern: "plan-sets"
---

<objective>
Rebuild Projects preview UX: plan sets → sheets → run. Align UI copy with StackCT ("plan set" not "takeoff").

Purpose: Deliver user-visible fix for Morehouse and all multi-set projects.

Output: Two-panel flow, updated sheet-count display, human-verify checklist in SUMMARY.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-stackct-plan-sets/14-03-SUMMARY.md
@static/app.js
@templates/index.html
@Master.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plan set selection UI</name>
  <files>templates/index.html, static/style.css, static/app.js</files>
  <action>
1. Add `#planSetPanel` between project picker and `#planSelectionPanel`:
   - Heading: "Plan sets" with count
   - Radio list: set name + sheet count (folder cards style)
   - Hidden until Preview clicked

2. `fetchPlanSets(projectId)`:
   - GET `/api/projects/{id}/plan-sets`
   - Poll while `syncing` (same as plans)
   - On select set → store `selectedPlanSet = {folder_id, name}` → enable "Load sheets" or auto-fetch plans

3. Change Preview flow:
   - Click Preview → show plan set panel first
   - If `plan_sets.length === 1` → auto-select and load sheets
   - If `> 1` → user must pick set then continue
   - If `0` and error → show message

4. `fetchPlans(projectId, folderId)` — call `/plan-sets/{folderId}/plans` only

5. Update `projectMeta`: show "Plan set: {name}" when sheets visible
  </action>
  <verify>Manual: Morehouse shows 2 sets before 120 checkboxes</verify>
  <done>Two-step UI wired to new APIs</done>
</task>

<task type="auto">
  <name>Task 2: Project list counts + run payload</name>
  <files>static/app.js</files>
  <action>
1. `loadSheetCounts` / project list: display `{n} sets` from `plan_set_counts` API; if one set synced show `{sheets} sheets · 1 set`

2. `runStackCT` / `runSelectedPlans`: include `folder_id: selectedPlanSet.folder_id` in POST body

3. On project select: optional `POST sync-plan-sets` background warm (replace flat sync-plans)

4. Update help/error strings for network vs login (keep Phase 13 hints)

5. Document human verification in SUMMARY: Morehouse v1+v2, Baking Social, one single-set project
  </action>
  <verify>grep folder_id static/app.js</verify>
  <done>Run sends folder_id; list shows set counts</done>
</task>

</tasks>

<verification>
- [ ] Morehouse: user sees MSP3 v1 and v2 before sheets
- [ ] Selecting v2 loads 180 sheets not 120
- [ ] Run uses only selected set's page_ids
</verification>

<success_criteria>
- UX matches StackCT folder cards at go.stackct.com/app/#/Takeoff/7416168
- No flat 120-sheet dump before set selection on multi-set projects
</success_criteria>

<output>
After completion, create `.planning/phases/14-stackct-plan-sets/14-04-SUMMARY.md`
</output>

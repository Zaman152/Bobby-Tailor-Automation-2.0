---
phase: 14-stackct-plan-sets
plan: 2
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - stackct_store.py
  - stackct_sync.py
  - project_cache.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "SQLite stores plan sets per project with folder_id and sheet_count"
    - "project_plans rows include folder_id; primary key is (stackct_id, folder_id, page_id)"
    - "sync_project_plans requires folder_id; old flat cache invalidated via schema_version bump"
  artifacts:
    - path: stackct_store.py
      provides: "project_plan_sets table, folder-scoped upsert_plans"
      contains: "project_plan_sets"
  key_links:
    - from: stackct_sync.py
      to: browser.get_plan_sets
      via: "sync_plan_sets_for_project"
      pattern: "get_plan_sets"
---

<objective>
Extend SQLite catalog for plan sets (folders) and folder-scoped sheets; update sync service to populate both layers.

Purpose: DB-first APIs can serve plan-set list without browser; sheets keyed by folder.

Output: Schema v2 migration, `upsert_plan_sets`, `get_plan_sets`, `get_plans(project_id, folder_id)`.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-stackct-plan-sets/14-01-SUMMARY.md
@stackct_store.py
@stackct_sync.py
@project_cache.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema v2 and store CRUD</name>
  <files>stackct_store.py</files>
  <action>
1. Bump `cache_metadata.schema_version` to `"2"` with migration on init:
   - `CREATE TABLE project_plan_sets (stackct_id, folder_id, name, sheet_count, synced_at, PRIMARY KEY (stackct_id, folder_id))`
   - Alter/rebuild `project_plans`: add `folder_id INTEGER NOT NULL DEFAULT 0`, change PK to `(stackct_id, folder_id, page_id)`
   - Safe migration: backup old plans to temp table or `DELETE FROM project_plans` + require re-sync (document in README)

2. CRUD:
   - `upsert_plan_sets(stackct_id, sets, synced_at)`
   - `get_plan_sets(stackct_id) -> list[dict]`
   - `get_plans(stackct_id, folder_id) -> list[dict]`
   - `is_plan_sets_fresh(stackct_id)`, `get_plan_sets_synced_at(stackct_id)`
   - Update `get_sheet_counts()` → return `{project_id: {plan_set_count, total_sheets?}}` or keep counts per folder in plan_sets table only

3. `projects.sheet_count` → deprecate or set to NULL until folder chosen; add `plan_set_count` column optional.
  </action>
  <verify>python3 -c "import stackct_store as s; s.init_db(); print(s.get_plan_sets(7416168))"</verify>
  <done>Schema v2 applies; CRUD functions work on empty DB</done>
</task>

<task type="auto">
  <name>Task 2: Sync layer folder-aware</name>
  <files>stackct_sync.py, project_cache.py</files>
  <action>
1. Add `sync_project_plan_sets(project_id, force=False)` — browser login once, `get_plan_sets`, upsert, return `{plan_sets, from_cache, syncing?}`

2. Change `sync_project_plans(project_id, folder_id, force=False)` — call `get_page_ids_in_folder(project_id, folder_id)`, upsert with folder_id

3. `project_cache.py`:
   - `get_project_plan_sets(project_id)` — DB-first + stale background sync (mirror plans pattern)
   - `get_project_plans(project_id, folder_id, ...)` — require folder_id; remove flat cache-miss sync without folder

4. Record `sync_runs` with `sync_type` `plan_sets` | `plans` and optional `folder_id` in metadata or new column.
  </action>
  <verify>python3 -c "from stackct_sync import sync_project_plan_sets; print('ok')"</verify>
  <done>Sync functions accept folder_id; project_cache exposes plan set + folder plans getters</done>
</task>

</tasks>

<verification>
- [ ] After sync Morehouse: 2 rows in `project_plan_sets`, plans only when folder synced
- [ ] Re-sync folder 35240694 stores 180 pages distinct from v1
</verification>

<success_criteria>
- Database models match StackCT folder hierarchy
- No code path writes folder-less plans without migration shim
</success_criteria>

<output>
After completion, create `.planning/phases/14-stackct-plan-sets/14-02-SUMMARY.md`
</output>

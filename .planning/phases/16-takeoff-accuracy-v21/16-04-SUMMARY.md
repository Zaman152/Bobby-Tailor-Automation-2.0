# 16-04 Summary — Aggregator + reporter outputs

**Status:** Complete  
**Files:** `aggregator.py`, `reporter.py`, `tests/test_aggregator.py`, `tests/test_reporter_accuracy.py`

New outputs: `takeoff_summary.csv`, `takeoff_summary` in JSON, `specification_tables`, consolidated section in `summary.txt`, optional `spec_tables.json`.

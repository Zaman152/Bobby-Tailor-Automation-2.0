---
phase: 16-takeoff-accuracy-v21
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - claude_analyzer.py
  - tests/test_extraction_prompt.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "EXTRACTION_PROMPT includes table_purpose, cross_references, pipe_runs, civil_structures"
    - "Parsed JSON tolerates new keys without breaking existing scraper flow"
  artifacts:
    - path: claude_analyzer.py
      provides: "EXTRACTION_PROMPT v2.1"
      contains: "cross_references"
    - path: tests/test_extraction_prompt.py
      provides: "prompt structure smoke tests"
  key_links:
    - from: claude_analyzer.py
      to: scraper.py
      via: "analyze_drawing return dict"
---

<objective>
Replace `EXTRACTION_PROMPT` in `claude_analyzer.py` with Masterv2 §C.8 complete prompt (Addendum v2.1).

Purpose: Foundation for table classification, cross-refs, civil structures, and pipe runs — all downstream accuracy work depends on structured extraction.

Output: Updated prompt + minimal parser adjustments so new top-level keys pass through to scraper unchanged.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@Masterv2.md (§C.8 EXTRACTION_PROMPT)
@.planning/phases/16-takeoff-accuracy-v21/16-RESEARCH.md
@claude_analyzer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace EXTRACTION_PROMPT</name>
  <files>claude_analyzer.py</files>
  <action>
1. Replace `EXTRACTION_PROMPT` with the full prompt from `Masterv2.md` §C.8 (lines ~2434–2589). Preserve existing model-selection and image-encoding logic unchanged.

2. Ensure prompt documents:
   - `table_purpose` + `use_for_takeoff` on schedules
   - `cross_references[]` with ref_number, ref_sheet, item_described
   - `pipe_runs[]` with length_lf, diameter_in, slope_pct (not in measurements)
   - `civil_structures[]` for GL/INV on structures (not measurements)
   - `approximate` on measurements with ± prefix
   - RULE 6 exclusions (scale, temps, GL/INV, slope %)

3. In JSON parse/validate helper (if any), allow unknown keys; do not strip `cross_references`, `pipe_runs`, `civil_structures` from returned dict.
  </action>
  <verify>python3 -c "from claude_analyzer import EXTRACTION_PROMPT; assert 'cross_references' in EXTRACTION_PROMPT and 'table_purpose' in EXTRACTION_PROMPT"</verify>
  <done>Prompt deployed; import succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Prompt smoke tests</name>
  <files>tests/test_extraction_prompt.py</files>
  <action>
Add unit tests (no API calls):
- Prompt contains required JSON schema keys
- Prompt contains TABLE CLASSIFICATION and CROSS-REFERENCE rules
- Sample fixture JSON with `table_purpose: specification_reference` parses through any normalization function
  </action>
  <verify>python3 -m pytest tests/test_extraction_prompt.py -q</verify>
  <done>Tests pass</done>
</task>

</tasks>

<verification>
- [ ] GitNexus impact run on `analyze_drawing` before edit (log in SUMMARY)
- [ ] No regression in existing `tests/` that mock Claude responses
</verification>

---
After completion, create `.planning/phases/16-takeoff-accuracy-v21/16-01-SUMMARY.md`

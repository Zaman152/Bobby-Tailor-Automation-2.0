---
phase: 16-takeoff-accuracy-v21
plan: 5
type: execute
wave: 5
depends_on: ["16-04"]
files_modified:
  - app.py
  - static/app.js
  - static/js/reports-workspace.js
  - .planning/phases/16-takeoff-accuracy-v21/16-UAT.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can preview consolidated takeoff summary in browser"
    - "16-UAT.md documents accuracy checklist from Masterv2 §12 + §C.7"
  artifacts:
    - path: .planning/phases/16-takeoff-accuracy-v21/16-UAT.md
    - path: app.py
      contains: "takeoff_summary"
  key_links:
    - from: static/app.js
      to: "/api/reports/.../preview/takeoff_summary.csv"
      via: "preview tab or workspace Summary sub-view"
---

<objective>
Expose consolidated takeoff in UI and document accuracy UAT (ACCURACY-12).

Purpose: Estimators see StackCT-format totals without downloading; integrates with Phase 15 preview workspace when available, else extends current preview tabs.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-premium-ui-ux-revamp/15-03-PLAN.md
@.planning/phases/16-takeoff-accuracy-v21/16-RESEARCH.md
@Masterv2.md (§12 Testing checklist, §C.7 table)
@app.py
@static/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preview API for takeoff_summary</name>
  <files>app.py</files>
  <action>
1. Allow `takeoff_summary.csv` in preview allowlist (path validation unchanged).

2. Return JSON `{type: "csv", rows: [...]}` or dedicated `{type: "summary", items: [...]}` from takeoff.json `takeoff_summary` if CSV missing (backward compat).

3. `list_reports()` includes `takeoff_summary.csv` in files when present.
  </action>
  <verify>python3 -m pytest tests/test_preview_routes.py -q -k takeoff_summary 2>/dev/null || python3 -c "
from pathlib import Path
import app
# smoke: allowlist includes takeoff_summary.csv if constant exists
assert hasattr(app, 'app')
print('app ok')
"</verify>
  <done>Preview route accepts takeoff_summary.csv</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Accuracy UAT sign-off</name>
  <files>.planning/phases/16-takeoff-accuracy-v21/16-UAT.md</files>
  <action>
Walk through every row in `16-UAT.md` (Masterv2 §C.7 + §12). Mark pass/fail. Block phase completion if any CRITICAL row fails.
  </action>
  <verify>16-UAT.md has signed section with date and tester initials</verify>
  <done>Human sign-off recorded in 16-UAT.md</done>
</task>

<task type="auto">
  <name>Task 2: UI tab + UAT doc</name>
  <files>static/app.js, static/js/reports-workspace.js, .planning/phases/16-takeoff-accuracy-v21/16-UAT.md</files>
  <action>
1. Add **Takeoff Summary** tab (or promote Summary tab to show consolidated table first when `takeoff_summary` exists):
   - Table: item | quantity | unit | source_sheets
   - Link to full calculations tab for audit

2. If `reports-workspace.js` exists from Phase 15, wire tab there; else extend inline preview in `app.js`.

3. Write `16-UAT.md` with Masterv2 §C.7 verification matrix + §12 quality checks (panel schedules, no GL in calculations, spec tables not in calc CSV).
  </action>
  <verify>test -f .planning/phases/16-takeoff-accuracy-v21/16-UAT.md</verify>
  <done>UAT doc exists; manual smoke: open report → see consolidated table</done>
</task>

</tasks>

<verification>
- [ ] Preview vs Export still distinct if Phase 15 landed
- [ ] Auth + CSRF unchanged on new preview paths
</verification>

---
After completion, create `.planning/phases/16-takeoff-accuracy-v21/16-05-SUMMARY.md`

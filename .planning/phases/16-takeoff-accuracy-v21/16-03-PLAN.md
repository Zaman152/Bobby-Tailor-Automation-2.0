---
phase: 16-takeoff-accuracy-v21
plan: 3
type: execute
wave: 3
depends_on: ["16-01", "16-02"]
files_modified:
  - scraper.py
  - pdf_analyzer.py
  - tests/test_cross_references.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "After all sheets analyzed, cross_references are collected and resolved when target sheet exists in run"
    - "Unresolved refs have resolution_status in report payload"
  artifacts:
    - path: scraper.py
      contains: "_find_detail_in_extraction"
    - path: tests/test_cross_references.py
  key_links:
    - from: scraper.py
      to: takeoff.json
      via: "report cross_references key"
---

<objective>
Implement cross-reference resolution pass in `scraper.py` (Masterv2 Gap A).

Purpose: Enrich civil/structure items with detail from referenced sheets when those sheets were analyzed in the same job.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@Masterv2.md (§C.2 Gap A)
@scraper.py
@.planning/phases/16-takeoff-accuracy-v21/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-ref collector and resolver</name>
  <files>scraper.py</files>
  <action>
1. Add `_find_detail_in_extraction(extracted, detail_number) -> dict` per Masterv2 (search schedules rows + components).

2. After all pages processed in `run_project_scrape` (or equivalent orchestration):
   - Collect cross_refs from each sheet's `cross_references[]` with `from_sheet`, `ref_sheet`, `ref_number`, `item_described`, `partial_data`
   - Match target sheet by normalized sheet name/code (case-insensitive substring in `_source_sheet` or sheet_title)
   - Set `resolved_spec` or `resolution_status: target_sheet_not_found`

3. Return resolved list from a new `resolve_cross_references(all_extracted) -> list` function (do not write JSON here).

4. **Scraper/pdf_analyzer:** After all pages, call `resolved_refs = resolve_cross_references(all_extracted)` and pass to `generate_report(..., cross_references=resolved_refs)`. Document: enrichment is audit/JSON-first; optional post-resolution recalc for matched civil_structures is out of scope unless time permits (note in SUMMARY).

5. Mirror the same post-loop call in `pdf_analyzer.py` so PDF runs get identical behavior.
  </action>
  <verify>python3 -m pytest tests/test_cross_references.py -q</verify>
  <done>Unit tests with fixture extractions pass</done>
</task>

<task type="auto">
  <name>Task 2: Cross-ref unit tests</name>
  <files>tests/test_cross_references.py</files>
  <action>
Fixtures:
- Sheet A refs C-4 detail 17; Sheet C-4 has schedule row with "17" → resolved_spec populated
- Sheet A refs Z-9 not in run → target_sheet_not_found
- _find_detail_in_extraction finds component by detail number
  </action>
  <verify>python3 -m pytest tests/test_cross_references.py -q</verify>
  <done>Tests pass without Playwright</done>
</task>

</tasks>

<verification>
- [ ] gitnexus_impact on `run_project_scrape` before edit
- [ ] Job thread still uses primitive callbacks only (no Flask context in callbacks)
</verification>

---
After completion, create `.planning/phases/16-takeoff-accuracy-v21/16-03-SUMMARY.md`

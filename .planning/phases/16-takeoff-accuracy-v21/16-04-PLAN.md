---
phase: 16-takeoff-accuracy-v21
plan: 4
type: execute
wave: 4
depends_on: ["16-02", "16-03"]
files_modified:
  - aggregator.py
  - reporter.py
  - scraper.py
  - pdf_analyzer.py
  - tests/test_aggregator.py
  - tests/test_reporter_accuracy.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "takeoff_summary.csv exists per run with consolidated item totals"
    - "specification_tables stored in takeoff.json (not in calculations.csv)"
    - "summary.txt includes PROJECT TAKEOFF SUMMARY section"
  artifacts:
    - path: aggregator.py
      provides: "aggregate_takeoff"
    - path: reporter.py
      contains: "takeoff_summary"
  key_links:
    - from: reporter.py
      to: aggregator.py
      via: "aggregate_takeoff(calculated_items)"
---

<objective>
Add consolidation layer and new outputs (Masterv2 Gap C + spec library).

Purpose: Deliver StackCT-style project totals while keeping per-sheet calculations.csv as audit trail.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@Masterv2.md (§C.2 Gap C, spec_tables in reporter)
@reporter.py
@.planning/phases/16-takeoff-accuracy-v21/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: aggregator.py</name>
  <files>aggregator.py, tests/test_aggregator.py</files>
  <action>
1. Create `aggregator.py` with `ITEM_NAME_MAP`, `normalize_item_name()`, `aggregate_takeoff(calculated_items)` per Masterv2 §C.2 (sorted output, quantity_fmt, source_sheets, detail audit rows).

2. Tests:
   - Two flooring rows on different sheets → one Striping/Flooring bucket with summed qty
   - normalize_item_name("guard rail along parking", ...) → Guard Rail, LF
   - Empty input → []
  </action>
  <verify>python3 -m pytest tests/test_aggregator.py -q</verify>
  <done>Aggregator tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Reporter integration</name>
  <files>reporter.py</files>
  <action>
1. Extend `generate_report(..., cross_references=None)` signature:
   - Set `report["cross_references"] = cross_references or []` **before** `json.dump` to takeoff.json
   - Build `specification_tables` from all extracted schedules where `table_purpose == specification_reference` → `report["specification_tables"]`
   - After all per-sheet estimates collected: `estimates = resolve_spec_lookups(all_extracted, estimates)` (ACCURACY-11)
   - Call `aggregate_takeoff(calculated_items)` → `report["takeoff_summary"]`
   - Write `takeoff_summary.csv` with columns: item, quantity, unit, source_sheets, line_count

2. **scraper.py / pdf_analyzer.py:** After page loop, call `resolve_cross_references(all_extracted)` then `generate_report(..., cross_references=resolved)`.

3. Append consolidated section to `summary.txt` (format per Masterv2 §C.2).

4. Write `spec_tables.json` in run folder when specification_tables non-empty.

5. Ensure `list_reports()` exposes `takeoff_summary.csv` in files dict.
  </action>
  <verify>python3 -m pytest tests/test_aggregator.py tests/test_reporter_accuracy.py -q</verify>
  <done>Reporter produces new files on fixture run</done>
</task>

<task type="auto">
  <name>Task 3: Reporter accuracy tests</name>
  <files>tests/test_reporter_accuracy.py</files>
  <action>
Fixture test with mocked `all_extracted` + `calculated_items`:
- `generate_report` writes takeoff.json containing `cross_references`, `specification_tables`, `takeoff_summary`
- Run folder contains `takeoff_summary.csv`
- Spec-reference-only schedule produces zero calc rows but appears in specification_tables
  </action>
  <verify>python3 -m pytest tests/test_reporter_accuracy.py -q</verify>
  <done>Reporter wiring tests pass</done>
</task>

</tasks>

<verification>
- [ ] gitnexus_impact on `generate_report`
- [ ] takeoff.json size reasonable (detail[] may be large — acceptable for audit)
</verification>

---
After completion, create `.planning/phases/16-takeoff-accuracy-v21/16-04-SUMMARY.md`

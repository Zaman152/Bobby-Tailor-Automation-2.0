---
phase: 16-takeoff-accuracy-v21
plan: 2
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - calculator.py
  - scraper.py
  - pdf_analyzer.py
  - tests/test_calculator_accuracy.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "specification_reference schedules produce zero calculation rows"
    - "GL/INV and slope % patterns rejected in _parse_numeric"
    - "Civil/site item types calculate with correct units"
  artifacts:
    - path: calculator.py
      contains: "table_purpose"
    - path: tests/test_calculator_accuracy.py
      provides: "guards and civil table unit tests"
  key_links:
    - from: calculator.py
      to: claude_analyzer.py
      via: "schedules[].table_purpose"
---

<objective>
Harden `calculator.py` for v2.1 extraction: skip non-takeoff tables, add civil/site estimation tables, filter elevation/slope noise, propagate approximate flag.

Purpose: Stop wrong quantities from spec tables and survey data (Gaps B, D, E, F, G).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@Masterv2.md (§C.2 Gap B, §C.3–C.6, §C.4 civil tables)
@calculator.py
@.planning/phases/16-takeoff-accuracy-v21/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schedule guards + spec lookup stub</name>
  <files>calculator.py</files>
  <action>
1. At top of `_calculate_from_schedule()`, read `table_purpose` (default `takeoff_schedule`). Return `[]` if purpose in `specification_reference`, `general_notes`, OR `use_for_takeoff` is explicitly false.

2. Add `resolve_spec_lookups(all_extracted, estimates)` per Masterv2 §C.2 — index spec tables by lookup_key; enrich estimates with `spec_reference` when pipe size matches. **Do not call inside per-sheet loop** — scraper/pdf_analyzer will call after all sheets (wired in 16-04).

3. Wire `pipe_runs[]` and `civil_structures[]` from extraction into calculation:
   - `pipe_runs` → storm_pipe LF with waste factor
   - `civil_structures` → catch_basin/manhole/headwall as EA count
   - Do not double-count if same item already in components[]
  </action>
  <verify>python3 -m pytest tests/test_calculator_accuracy.py -q -k "schedule_guard or spec"</verify>
  <done>Guards and pipe/civil paths covered by tests</done>
</task>

<task type="auto">
  <name>Task 2: Civil tables + numeric filters</name>
  <files>calculator.py, tests/test_calculator_accuracy.py</files>
  <action>
1. Add ESTIMATION_TABLES entries from Masterv2 §C.3: storm_pipe, trench_drain, catch_basin, manhole, headwall, bollard, guard_rail, hand_rail, striping, concrete_pavement, asphalt, tilt_up_wall, exposed_structure, exterior_soffit (keywords as in doc).

2. Extend `_apply_formula()` branches for LF/SF/EA per Masterv2 §C.3 code block.

3. In `_parse_numeric()`, return None for:
   - GL/INV/ELEV patterns (`GL=845.0`, `INV. IN=843.3`)
   - Pure percentages (`4.81%`)

4. In `_calculate_from_measurement()`, pass `approximate` through; append ` [FIELD VERIFY ±]` to formula when true.

5. Tests: spec table schedule → []; `GL=845.0` → None; `38` with approximate → flag on result; storm_pipe 25 lf → correct output unit.
  </action>
  <verify>python3 -m pytest tests/test_calculator_accuracy.py -q</verify>
  <done>All new tests pass</done>
</task>

</tasks>

<verification>
- [ ] gitnexus_impact on `apply_estimation_tables` / `_calculate_from_schedule` logged in SUMMARY
- [ ] Existing calculator tests still pass
</verification>

---
After completion, create `.planning/phases/16-takeoff-accuracy-v21/16-02-SUMMARY.md`

---
phase: 21-accuracy-learning-engine
plan: 05
type: execute
wave: 2
depends_on: ["21-01"]
requirements: [V3-ACC-04]
files_modified:
  - src/bobbytailor/config.py
  - src/bobbytailor/pipeline/sheet_pass_matrix.py
  - src/bobbytailor/vision/claude_analyzer.py
  - src/bobbytailor/pipeline/pdf_analyzer.py
  - tests/conftest.py
  - tests/test_sheet_pass_matrix.py
  - .env.example
autonomous: true

must_haves:
  truths:
    - "Model routing sources every slug from config/env — no hardcoded model strings in routing logic"
    - "PRICING reflects the July 2026 table (Sonnet 5 $2/$10, Opus 4.8 $5/$25, Haiku 4.5 $1/$5, Fable 5 $10/$50)"
    - "Whole-sheet renders target ≈1568px long edge instead of the wasteful 7900px clamp; a crop helper renders clip regions at high DPI"
  artifacts:
    - path: "src/bobbytailor/config.py"
      provides: "CLAUDE_MODEL_SEMANTIC / _DENSE / _ESCALATION env-driven slugs"
      contains: "CLAUDE_MODEL_SEMANTIC"
    - path: "src/bobbytailor/pipeline/sheet_pass_matrix.py"
      provides: "MODEL_ROUTING v2 per RESEARCH §5 table"
      contains: "MODEL_ROUTING"
    - path: "src/bobbytailor/pipeline/pdf_analyzer.py"
      provides: "1568px-target whole-sheet render + render_clip_crop helper"
      contains: "def render_clip_crop("
  key_links:
    - from: "sheet_pass_matrix.MODEL_ROUTING"
      to: "config.CLAUDE_MODEL_*"
      via: "import, no literals"
      pattern: "CLAUDE_MODEL_"
---

<objective>
Upgrade model routing and render fidelity (V3-ACC-04): config-driven slugs for the July 2026 Anthropic line-up, MODEL_ROUTING v2 per the RESEARCH §5 routing table, PRICING refresh, and adaptive fidelity — whole-sheet renders at ≈1568px long edge with a `render_clip_crop()` helper for native-detail zooms.

Purpose: later plans (vision prompts 21-08, ensemble 21-11, verify-retry 21-14) consume these routing keys and render helpers; pricing must be correct for the budget guard.
Output: config slugs, MODEL_ROUTING v2, PRICING update, render retarget + crop helper.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-accuracy-learning-engine/21-RESEARCH.md  (§5 model line-up, routing table, image constraints — verified July 2026)
@.planning/phases/21-accuracy-learning-engine/21-PATTERNS.md  (sheet_pass_matrix assignment, conftest env pinning note)
@src/bobbytailor/pipeline/sheet_pass_matrix.py
@src/bobbytailor/config.py
</context>

## Artifacts this phase produces

New symbols created by this plan:
- **Config constants** in `config.py` (all `os.getenv`-driven): `CLAUDE_MODEL_SEMANTIC` (default `claude-sonnet-5`), `CLAUDE_MODEL_DENSE` (default `claude-opus-4-8`), `CLAUDE_MODEL_ESCALATION` (default `claude-fable-5`), `CLAUDE_MODEL_CLASSIFY` (default `claude-haiku-4-5`), `ESCALATION_ENABLED` (default false), `RENDER_TARGET_PX` (default 1568)
- **Updated dict** `MODEL_ROUTING` in sheet_pass_matrix.py — new (sheet_type, pass) keys for `semantic`, `dense`/schedule, `verify_retry` routes per RESEARCH §5 table
- **Updated dict** `PRICING` in claude_analyzer.py — July 2026 rates keyed by the four new slugs (keep old slugs for backward compat on cached reports)
- **Function** `render_clip_crop(page, rect_pt, target_px: int = 1568) -> PIL.Image` in pdf_analyzer.py — renders a `fitz.Rect` clip at DPI targeting `target_px` long edge
- **Changed behavior**: `_page_to_image` targets `RENDER_TARGET_PX` long edge (replacing the 7900px clamp logic)
- **Env vars** documented in `.env.example`: `CLAUDE_MODEL_SEMANTIC`, `CLAUDE_MODEL_DENSE`, `CLAUDE_MODEL_ESCALATION`, `CLAUDE_MODEL_CLASSIFY`, `ESCALATION_ENABLED`, `RENDER_TARGET_PX`

<threat_model>
ASVS L1:
- **API key handling (MEDIUM):** no change to ANTHROPIC_API_KEY sourcing; new config MUST NOT log slugs together with the key or echo the key in errors. Task 1 acceptance greps for `ANTHROPIC_API_KEY` appearing in any new log/format string — none allowed.
- **Cost abuse via env manipulation (LOW):** ESCALATION_ENABLED defaults to false (Fable 5 is budget-gated off per RESEARCH §5); the per-run budget guard lands in 21-11.
- No new input parsing or endpoints.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Config slugs + PRICING July 2026</name>
  <files>src/bobbytailor/config.py, src/bobbytailor/vision/claude_analyzer.py, .env.example</files>
  <read_first>
    - src/bobbytailor/config.py (lines 16-25: existing CLAUDE_MODEL/CLAUDE_MODEL_SCHEDULES env pattern to replicate)
    - src/bobbytailor/vision/claude_analyzer.py (lines ~20-24: PRICING dict; lines 458-532: where PRICING is consumed)
    - .planning/phases/21-accuracy-learning-engine/21-RESEARCH.md §5 (rates: claude-fable-5 $10/$50, claude-opus-4-8 $5/$25, claude-sonnet-5 $2/$10 intro, claude-haiku-4-5 $1/$5; note Sonnet 5 → $3/$15 on 2026-09-01)
  </read_first>
  <action>
    1. Add the six config constants listed in Artifacts using the exact `os.getenv("NAME", default)` pattern of CLAUDE_MODEL_SCHEDULES. Defaults: semantic=claude-sonnet-5, dense=claude-opus-4-8, escalation=claude-fable-5, classify=claude-haiku-4-5, ESCALATION_ENABLED=false-parsed like REUSE_SCREENSHOTS, RENDER_TARGET_PX=1568 int.
    2. Update PRICING in claude_analyzer.py: add entries for the four new slugs with the RESEARCH §5 rates; KEEP existing entries (old runs reference old slugs); add a `# Sonnet 5 price rises to $3/$15 on 2026-09-01 — update then` comment; default-for-unknown stays Sonnet-rate.
    3. Document all new env vars in .env.example with the default values and one-line comments.
  </action>
  <verify>python3 -c "from bobbytailor.config import CLAUDE_MODEL_SEMANTIC, CLAUDE_MODEL_DENSE, ESCALATION_ENABLED, RENDER_TARGET_PX; assert CLAUDE_MODEL_SEMANTIC == 'claude-sonnet-5' and RENDER_TARGET_PX == 1568 and ESCALATION_ENABLED is False" exits 0</verify>
  <done>Slugs and pricing config-driven, documented</done>
  <acceptance_criteria>
    - .env.example contains `CLAUDE_MODEL_SEMANTIC=` and `RENDER_TARGET_PX=`
    - PRICING contains key `"claude-sonnet-5"` with in=2.0 out=10.0 and `"claude-opus-4-8"` with in=5.0 out=25.0
    - `rg "ANTHROPIC_API_KEY" src/bobbytailor/config.py src/bobbytailor/vision/claude_analyzer.py | rg "log|format|print"` returns no matches
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: MODEL_ROUTING v2 + conftest env pinning</name>
  <files>src/bobbytailor/pipeline/sheet_pass_matrix.py, tests/conftest.py, tests/test_sheet_pass_matrix.py</files>
  <read_first>
    - src/bobbytailor/pipeline/sheet_pass_matrix.py (lines 54-69: MODEL_ROUTING shape; PASS_MATRIX keys — new routing keys must use existing sheet_type/pass_type vocabulary)
    - tests/conftest.py (lines 25-33: env pinning — new model env vars need os.environ.setdefault here BEFORE config import)
    - tests/test_sheet_pass_matrix.py (existing routing assertions to extend, not break)
    - .planning/phases/21-accuracy-learning-engine/21-RESEARCH.md §5 routing table (classify→Haiku; semantic labeling floor/roof/MEP→Sonnet 5; legend/schedule structuring + verify-retry arbitration→Opus 4.8; escalation→Fable 5 gated)
  </read_first>
  <action>
    1. Extend MODEL_ROUTING per the RESEARCH §5 table: schedule/legend passes route to `CLAUDE_MODEL_DENSE`; add entries for new pass types the vision package will register — `("*", "semantic")` → CLAUDE_MODEL_SEMANTIC, `("*", "verify_retry")` → CLAUDE_MODEL_DENSE (use the existing wildcard/fallback convention if one exists, else explicit per-sheet-type rows). Keep the dict shape `dict[tuple[str, str], str]` intact — `_pick_model` consumers must not change.
    2. Add `pick_model_for_pass(sheet_type, pass_type) -> str` helper if lookups need fallback logic (exact key → ("*", pass) → CLAUDE_MODEL default), so 21-08/21-11/21-14 all use one resolution function.
    3. conftest.py: `os.environ.setdefault` for CLAUDE_MODEL_SEMANTIC/DENSE/ESCALATION/CLASSIFY with distinct throwaway values (mirroring the existing haiku/sonnet pinning) so routing tests are deterministic under any collection order.
    4. Extend tests/test_sheet_pass_matrix.py: assert schedule pass resolves to the DENSE slug, semantic to SEMANTIC slug, unknown combos fall back to CLAUDE_MODEL, and `rg`-style source assertion that no `claude-` literal appears in MODEL_ROUTING values (all names come from config).
  </action>
  <verify>python3 -m pytest tests/test_sheet_pass_matrix.py -q exits 0; python3 -m pytest tests/ -q -m "not golden" green</verify>
  <done>Routing v2 config-driven with a single resolution helper; tests pin behavior</done>
  <acceptance_criteria>
    - sheet_pass_matrix.py contains `def pick_model_for_pass(` and no string literal starting with `"claude-"` inside MODEL_ROUTING values
    - tests/conftest.py contains `CLAUDE_MODEL_SEMANTIC`
    - `python3 -m pytest tests/test_sheet_pass_matrix.py -q` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Render retarget (1568px) + render_clip_crop helper</name>
  <files>src/bobbytailor/pipeline/pdf_analyzer.py</files>
  <read_first>
    - src/bobbytailor/pipeline/pdf_analyzer.py (lines 22-31: the 7900px clamp; `_page_to_image` 2× Matrix scaling from 20-06 decision)
    - .planning/phases/21-accuracy-learning-engine/21-RESEARCH.md §5 image constraints (model downscales to ~1568px standard — oversized renders waste tokens; adaptive fidelity = clip crops, not bigger full-page rasters; 8000px/10MB API caps)
  </read_first>
  <action>
    1. Rework `_page_to_image` (and the clamp constants at lines 22-31): compute zoom so the long edge lands at `config.RENDER_TARGET_PX` (default 1568) instead of clamping at 7900. Keep an absolute safety cap at 8000px and the existing base64 size guard.
    2. Add `render_clip_crop(page, rect_pt, target_px=1568)`: `page.get_pixmap(matrix=..., clip=fitz.Rect(rect_pt))` with zoom = target_px / max(rect width, height in pt) capped at 8000px, returning a PIL Image — the shared zoom primitive for verify-retry crops (21-14), crop galleries (21-08), and tiles (21-11).
    3. Whole-sheet callers keep working unchanged (only resolution changes); update any test asserting the 7900 constant.
  </action>
  <verify>python3 -c probe: render page 0 of tests/fixtures/crow_cass/crow_cass_plans.pdf via _page_to_image → long edge within [1500, 1600] px; render_clip_crop on a 100×100pt rect at target 800 → long edge within [780, 820] px</verify>
  <done>Renders sized for the model's actual input resolution; crop zoom primitive available</done>
  <acceptance_criteria>
    - pdf_analyzer.py contains `def render_clip_crop(` and `RENDER_TARGET_PX`
    - pdf_analyzer.py no longer contains the `7900` clamp constant
    - `python3 -m pytest tests/ -q -m "not golden"` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Routing tests green; full suite green
- Fixture render probe shows ≈1568px long edge
</verification>

<success_criteria>
- Strongest-model routing is config-driven and matches the RESEARCH §5 table (V3-ACC-04 routing half)
- Adaptive fidelity primitives (`RENDER_TARGET_PX`, `render_clip_crop`) exist for downstream vision plans (V3-ACC-04 fidelity half)
</success_criteria>

<output>
After completion, create `.planning/phases/21-accuracy-learning-engine/21-05-SUMMARY.md`
</output>

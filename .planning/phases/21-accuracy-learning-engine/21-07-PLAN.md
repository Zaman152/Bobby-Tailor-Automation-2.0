---
phase: 21-accuracy-learning-engine
plan: 07
type: execute
wave: 3
depends_on: ["21-03"]
requirements: [V3-ACC-02]
files_modified:
  - src/bobbytailor/deterministic/viewports.py
  - src/bobbytailor/deterministic/scale_solver.py
  - tests/test_scale_solver.py
  - tests/fixtures/crow_cass/scale_truth.json
  - tests/fixtures/bobs_discount/scale_truth.json
autonomous: true

must_haves:
  truths:
    - "Scale is solved per viewport with zero calibration: RANSAC consensus over DimensionSamples, printed notation demoted to cross-check"
    - "Crow Cass main-plan viewport solves to 1\"=20' (±2%) — not 24', not a detail scale"
    - "No HIGH-confidence wrong scales on any truth-table sheet (the worst failure class is structurally prevented)"
    - "Main-plan selection is by real area after per-viewport scale — frequency of detail callouts can no longer win"
  artifacts:
    - path: "src/bobbytailor/deterministic/viewports.py"
      provides: "viewport segmentation: clip rects > title anchors > density gutters > nearest-anchor fallback"
      exports: ["Viewport", "segment_viewports"]
      min_lines: 120
    - path: "src/bobbytailor/deterministic/scale_solver.py"
      provides: "RANSAC per-viewport solve + resolve_scale_v2 priority chain"
      exports: ["ViewportScale", "solve_viewport_scale", "resolve_scale_v2"]
      min_lines: 120
    - path: "tests/fixtures/crow_cass/scale_truth.json"
      provides: "hand-recorded page→viewport→fpi truth table"
  key_links:
    - from: "scale_solver.solve_viewport_scale"
      to: "dimensions.extract_dimension_samples"
      via: "samples filtered to viewport bounds"
      pattern: "extract_dimension_samples"
    - from: "resolve_scale_v2"
      to: "scale_extraction.snap_fpi"
      via: "ladder snap as cross-check only"
      pattern: "snap_fpi"
---

<objective>
Implement zero-calibration scale solving (V3-ACC-02): viewport segmentation (clip rects → title anchors → whitespace gutters → fallback) and per-viewport RANSAC consensus over DimensionSamples, with the new priority chain override > dimension-solved > printed (cross-check) > learned prior, per-viewport confidence records, and truth-table gate tests.

Purpose: the root fix for the multi-scale failure (Crow 1"=24' vs true 20'). Walls/rooms (21-10) and pipeline integration (21-13) consume `ViewportScale.fpp`.
Output: `viewports.py`, `scale_solver.py`, scale_truth.json tables, layer-2 gate tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-accuracy-learning-engine/21-RESEARCH.md  (§2 Zero-Calibration Scale Solving — segmentation signals, RANSAC estimator, confidence scoring, main-plan selection; Pitfalls 5, 11)
@.planning/phases/21-accuracy-learning-engine/21-PATTERNS.md  (scale_solver + viewports assignments — ScaleResult contract, snap_fpi, callout anchors, viewport record shape)
@src/bobbytailor/deterministic/geometry_takeoff.py
@src/bobbytailor/deterministic/scale_extraction.py
</context>

## Artifacts this phase produces

New symbols created by this plan:
- **Dataclass** `Viewport` — `region_id: str` ("R1"...), `bbox_pt: tuple`, `kind: str` ('main_plan'|'detail'|'title_block'|'legend'|'schedule'|'unknown')`, `detection_method: str` ('clip_rect'|'title_anchor'|'density_gutter'|'fallback')`, `title_text: str`, `to_dict()`
- **Function** `segment_viewports(page, table_bboxes: list = None) -> list[Viewport]`
- **Dataclass** `ViewportScale` — `fpp: Optional[float]`, `fpi: Optional[float]`, `confidence: str`, `n_inliers: int`, `inlier_share: float`, `printed_agreement: Optional[bool]`, `door_check: Optional[bool]`, `method: str`, `to_dict()`
- **Function** `solve_viewport_scale(samples: list[DimensionSample], viewport: Viewport, printed_fpi: Optional[float] = None) -> ViewportScale` — RANSAC consensus
- **Function** `resolve_scale_v2(page, viewports_with_scales, override_feet_per_inch=None, learned_prior=None) -> ScaleResult` — priority chain returning the EXISTING `ScaleResult` dict contract (method values extended with `dimension_solve`, `learned_prior`)
- **Function** `select_main_plan(viewports_with_scales) -> Optional[str]` — largest real area + floor-plan text labels
- **Files** `tests/fixtures/{crow_cass,bobs_discount}/scale_truth.json` — `{"pages": [{"page": N, "viewports": [{"approx_bbox_pt": [...], "fpi": F, "is_main": bool}]}]}`

<threat_model>
ASVS L1 — PDF parsing of untrusted files:
- **Wrong-but-confident output (design-level HIGH, mitigated):** a HIGH-confidence wrong scale silently corrupts every downstream quantity. Structural mitigation is the confidence rubric itself — HIGH requires ≥8 inlier dims AND ≥80% inlier share AND printed-scale agreement within 1% (RESEARCH §2); Task 3's gate test asserts zero HIGH-confidence wrong scales on truth tables. This rubric is a blocking acceptance criterion.
- **Degenerate input:** pages with zero dimension samples return confidence "none" and fall through the priority chain — never a guessed value (flag-don't-guess shared pattern).
- No network/SQL/writes.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: viewports.py — segmentation signal cascade</name>
  <files>src/bobbytailor/deterministic/viewports.py</files>
  <read_first>
    - .planning/phases/21-accuracy-learning-engine/21-RESEARCH.md §2 per-viewport segmentation (priority: clip rects via get_drawings(extended=True) > title-text anchors ("N / SCALE: x" under drawing, extract_scale_callouts anchors) > whitespace gutters/density projection + border rects + detail bubbles > nearest-anchor fallback; title block = bottom/right dense-small-text strip; legend/schedule regions masked from measurement)
    - src/bobbytailor/deterministic/geometry_takeoff.py (lines 259-331 `_measure_viewports` — the nearest-anchor bucketing that becomes the fallback tier; viewport record shape lines 286-295)
    - src/bobbytailor/deterministic/scale_extraction.py (extract_scale_callouts lines 162-188 — positioned {feet_per_inch, x, y} anchors)
  </read_first>
  <action>
    Implement `segment_viewports(page, table_bboxes=None)` as the four-tier cascade:
    1. Clip rects: `page.get_drawings(extended=True)` clip hierarchy — when ≥2 substantial clip rects exist (each > 5% page area), they are authoritative viewport regions (`detection_method="clip_rect"`).
    2. Title anchors: bind each `extract_scale_callouts` anchor to the geometry mass above/near it bounded by whitespace gutters; also detect boxed detail borders (large stroked rects) and detail bubbles (circle + section number).
    3. Density gutters: project segment density onto x/y axes; low-density bands (< 5% of median density, ≥ 20pt wide) split regions.
    4. Fallback: nearest-anchor bucketing equivalent to `_measure_viewports` current behavior.
    Always: classify the title block (bottom/right strip, high small-text density), and mark `table_bboxes` (passed from tables.py output) as kind='schedule'. Every page returns ≥1 viewport (whole page as 'unknown' if nothing detected). Assign region_id "R1","R2",... deterministic by area desc.
  </action>
  <verify>python3 -c probe: segment_viewports on crow_cass_plans.pdf page 0 returns ≥ 2 viewports and exactly one title_block; each has detection_method set</verify>
  <done>Viewport regions with detection provenance; masks for title/legend/schedule regions</done>
  <acceptance_criteria>
    - viewports.py contains `def segment_viewports(` and the string `extended=True`
    - viewports.py contains all four detection_method literals: clip_rect, title_anchor, density_gutter, fallback
    - Probe returns Viewport objects whose to_dict includes region_id and bbox_pt
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: scale_solver.py — RANSAC solve + resolve_scale_v2 chain</name>
  <files>src/bobbytailor/deterministic/scale_solver.py</files>
  <read_first>
    - .planning/phases/21-accuracy-learning-engine/21-RESEARCH.md §2 (RANSAC: sample one r_i, count inliers within ±2% relative, largest consensus median; ladder snap as cross-check — solved wins if >2% apart but flag; cross-validations: printed callout, 3'-0" door gaps ±2", grid spacing; confidence rubric HIGH/MEDIUM/LOW; main-plan = largest real area + floor-plan labels; Pitfall 11 priority: solved > printed > learned)
    - src/bobbytailor/deterministic/scale_extraction.py (snap_fpi lines 35-58 — import, keep intact)
    - src/bobbytailor/deterministic/geometry_takeoff.py (ScaleResult dataclass lines 39-47 and resolve_scale lines 75-111 — the dict contract downstream consumes; DO NOT change field names)
    - src/bobbytailor/deterministic/dimensions.py (DimensionSample fields)
  </read_first>
  <action>
    1. `solve_viewport_scale(samples, viewport, printed_fpi=None)`: filter samples to viewport bbox; RANSAC per RESEARCH §2 (k=min(len(samples), 50) iterations, inlier tol ±2% relative, consensus median). Compute `fpi = fpp * 72`. Cross-checks: `printed_agreement` = solved vs snap_fpi(printed_fpi) within 1%; `door_check` populated later by walls (param optional, default None). Confidence: HIGH = n_inliers ≥ 8 AND inlier_share ≥ 0.8 AND printed_agreement is True; MEDIUM = n_inliers ≥ 4 OR (printed only); LOW = fewer/conflict; none = no samples.
    2. `resolve_scale_v2(...)` priority chain returning `ScaleResult` (import from geometry_takeoff): (1) override → high/override; (2) dimension-solved viewport scale (method="dimension_solve", detail includes n_inliers/share); (3) printed scale via existing extraction (method="printed_scale", demoted, confidence capped at medium when unaccompanied); (4) learned prior (method="learned_prior", confidence low, ONLY when tiers 2-3 unavailable — anti-amplification: a learned prior NEVER outranks a solved or printed scale); (5) none.
    3. `select_main_plan(...)`: real area = viewport bbox area × fpp² for its OWN solved scale; require floor-plan-ish text labels (room words/grid bubbles) as tiebreak; return region_id.
    4. Persist per-viewport `{fpp, n_inliers, share, printed_agreement, door_check}` in ViewportScale.to_dict — the record the report/UI surfaces (V3-ACC-02 "confidence recorded and surfaced"; consumed by 21-13).
  </action>
  <verify>python3 -c probe: full solve on crow page 0 — main-plan viewport ViewportScale.fpi within [19.6, 20.4]</verify>
  <done>Zero-calibration solve with honest confidence; priority chain reordered per Pitfall 11</done>
  <acceptance_criteria>
    - `solve_viewport_scale()` on crow_cass page with truth 20 fpi returns fpi within ±2% of 20.0
    - scale_solver.py contains `def resolve_scale_v2(` importing ScaleResult from geometry_takeoff (contract preserved)
    - The chain source order is override → dimension_solve → printed_scale → learned_prior (assert via test in Task 3 feeding synthetic tiers)
    - HIGH confidence requires printed_agreement: source contains the three-condition conjunction
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: scale_truth.json tables + layer-2 gate tests</name>
  <files>tests/fixtures/crow_cass/scale_truth.json, tests/fixtures/bobs_discount/scale_truth.json, tests/test_scale_solver.py</files>
  <read_first>
    - tests/test_takeoff_generalization.py (expected-constraints fixture pattern)
    - .planning/phases/21-accuracy-learning-engine/21-VALIDATION.md (layer 2 gate: main-plan viewport correct on 100% of truth-table sheets; no HIGH-confidence wrong scales)
    - .planning/phases/21-accuracy-learning-engine/21-CONTEXT.md (Crow truth ~1"=20'; footprint 1136'×350')
  </read_first>
  <action>
    1. Build truth tables by probing: run the solver on every page of crow_cass_plans.pdf and bobs_discount_plans.pdf, hand-verify the main-plan fpi against printed scale text + the known Crow 20 fpi truth, and record `scale_truth.json` per the schema in Artifacts (minimum: all Crow pages, all Bob pages with a plan viewport; record fpi=null for pages with no solvable viewport).
    2. tests/test_scale_solver.py:
       - Gate A: for every truth-table entry, the solver's main-plan fpi within ±2% of truth
       - Gate B (blocking): NO viewport anywhere returns confidence=="high" with fpi off-truth by >2% — iterate all pages, assert not (high AND wrong)
       - Crow acceptance: page with 1136'×350' dims solves 20.0 ±2%, NOT within 2% of 24.0
       - resolve_scale_v2 tier tests with synthetic inputs: override wins; solved beats printed when >2% apart (flag set); learned prior only used when tiers 2-3 empty
       - RANSAC contamination test: synthetic sample set with 40% detail-scale contamination still recovers the majority scale with confidence ≤ medium if share < 0.8 (Pitfall 5)
  </action>
  <verify>python3 -m pytest tests/test_scale_solver.py -q exits 0; full suite green</verify>
  <done>Layer-2 gate green: 100% main-plan truth, zero high-confidence-wrong</done>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_scale_solver.py -q` exits 0 with ≥ 8 tests
    - tests/fixtures/crow_cass/scale_truth.json exists and contains `"fpi": 20`
    - The no-high-confidence-wrong assertion exists: test file contains `confidence` and a combined wrongness check
    - `python3 -m pytest tests/ -q -m "not golden"` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Layer-2 gate tests green on both hard-gate fixtures
- Crow resolves 1"=20' (roadmap success criterion 3 proven at module level)
</verification>

<success_criteria>
- Per-viewport zero-calibration scale solving with recorded confidence (V3-ACC-02 complete at module level; pipeline surfacing in 21-13)
- Priority chain: override > dimension-solved > printed cross-check > learned prior
</success_criteria>

<output>
After completion, create `.planning/phases/21-accuracy-learning-engine/21-07-SUMMARY.md`
</output>

---
phase: 21-accuracy-learning-engine
plan: 18
type: execute
wave: 10
depends_on: ["21-17"]
requirements: [V3-PROD-02, V3-STRUCT-01]
files_modified:
  - "root import shims (DELETE all 30)"
  - tests/** (import migration to bobbytailor package paths)
  - scripts/** (import migration)
  - main.py
  - README.md
  - docs/DEPLOYMENT.md
  - .planning/phases/21-accuracy-learning-engine/21-UAT.md
autonomous: false

must_haves:
  truths:
    - "Root shims are deleted; tests and scripts import bobbytailor package paths directly; the suite is green without any legacy path"
    - "The reviewed-items workflow is documented and demonstrable: a human verifies only the flagged subset surfaced by the report"
    - "Deployment docs cover the new layout, env vars, learning DB, budget guard, and structured error recovery; 21-UAT.md exists for live-testing sign-off"
  artifacts:
    - path: "README.md"
      provides: "new package layout + run instructions"
      contains: "src/bobbytailor"
    - path: "docs/DEPLOYMENT.md"
      provides: "deployment + env + recovery documentation"
    - path: ".planning/phases/21-accuracy-learning-engine/21-UAT.md"
      provides: "live-testing UAT script"
  key_links:
    - from: "tests/ + scripts/"
      to: "bobbytailor.* imports"
      via: "batch import migration"
      pattern: "from bobbytailor"
---

<objective>
Finish the restructure (delete shims, migrate imports — V3-STRUCT-01 closing step) and ship live-testing readiness (V3-PROD-02): reviewed-items workflow documentation, error-recovery notes, deployment docs, README, and the 21-UAT.md sign-off script.

Purpose: the phase ends "ready for live testing" per CONTEXT — docs, recovery, and a UAT checklist a human can execute.
Output: shim-free package, docs, UAT script, final green suite.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-accuracy-learning-engine/21-RESEARCH.md  (§7 step 3 — migrate tests in batches, delete shims in a closing plan; Pitfall 9 restart-required note)
@.planning/phases/21-accuracy-learning-engine/21-CONTEXT.md  (Process LOCKED — live-testing readiness, GitHub pushes at milestones)
@.planning/ROADMAP.md  (Phase 21 success criteria 6-7)
</context>

## Artifacts this phase produces

New symbols created by this plan:
- **Deleted files**: the 30 root shim modules (geometry_takeoff.py, app.py shim, etc. — root `app.py` is replaced by a 2-line gunicorn entry `from bobbytailor.web.app import create_app; app = create_app()` kept as the ONLY root Python entry beside main.py)
- **Changed imports**: all `tests/*.py`, `scripts/*.py`, `main.py` use `bobbytailor.*` package paths; conftest sys.path fallback removed
- **Docs**: README.md (layout diagram, quickstart, plans-only contract, learning loop overview), docs/DEPLOYMENT.md (gunicorn `app:app`, env var table incl. LEARNING_DB_PATH/API_BUDGET_PER_RUN_USD/CLAUDE_MODEL_*, restart-required-on-deploy note per Pitfall 9, error recovery: per-sheet degradation, budget_exhausted semantics, learning DB backup = copy learning.db)
- **File** `.planning/phases/21-accuracy-learning-engine/21-UAT.md` — UAT script covering roadmap success criteria 1-7 (plans-only upload run, flagged-subset review, correction → repeat-run application, StackCT parity run, reproducibility spot check)

<threat_model>
ASVS L1:
- **Regression risk (MEDIUM):** shim deletion is a wide but mechanical change — mitigation is batch migration with suite runs between batches (RESEARCH §7 step 3) and a final grep proving no legacy imports remain.
- **Docs must not leak secrets:** env tables show names + sources, never values; .env.example is the only value template.
- No code-behavior changes in this plan beyond import paths.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Migrate test/script imports in batches; delete shims</name>
  <files>tests/*.py, scripts/*.py, main.py, tests/conftest.py, 30 root shims (delete), root app.py (2-line entry)</files>
  <read_first>
    - tests/conftest.py (the sys.path fallback + _ensure_real names that need package-path updates)
    - tests/test_pipeline_parity.py (sys.modules.setdefault stubs reference OLD module names — update stub keys to package paths carefully)
    - main.py (scraper/config imports)
  </read_first>
  <action>
    1. Batch 1: migrate tests/ imports to `bobbytailor.*` (incl. conftest `_ensure_real` names and parity-stub sys.modules keys); run suite. Batch 2: scripts/ + main.py; run suite. Batch 3: delete all 30 shims, reduce root app.py to the 2-line gunicorn entry; remove the conftest sys.path fallback; run suite.
    2. Final audit: `rg "^from (geometry_takeoff|scale_extraction|takeoff_pipeline|claude_analyzer|scraper|aggregator|calculator|reporter|stackct_store|job_store)|^import (geometry_takeoff|takeoff_pipeline)" tests/ scripts/ main.py` returns nothing; `pip install -e .` still resolves.
  </action>
  <verify>python3 -m pytest tests/ -q -m "not golden" exits 0 after EACH batch and at the end; python3 scripts/vision_only_benchmark.py --help exits 0</verify>
  <done>Package-native imports everywhere; shims gone; suite green</done>
  <acceptance_criteria>
    - `ls geometry_takeoff.py scale_extraction.py takeoff_pipeline.py 2>/dev/null` finds no files at root
    - Root app.py is ≤ 5 lines and contains `create_app`
    - `python3 -m pytest tests/ -q -m "not golden"` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: README + DEPLOYMENT docs + 21-UAT.md</name>
  <files>README.md, docs/DEPLOYMENT.md, .planning/phases/21-accuracy-learning-engine/21-UAT.md</files>
  <read_first>
    - README.md (current state — update, don't rewrite history)
    - .env.example (the full env var inventory to document)
    - .planning/ROADMAP.md Phase 21 success criteria (the 7 truths the UAT must walk through)
    - .planning/phases/21-accuracy-learning-engine/21-VALIDATION.md (Manual-Only Verifications table — the two rows become UAT sections)
  </read_first>
  <action>
    1. README: package layout tree, quickstart (`pip install -e .`, `flask --app app run` / gunicorn `app:app`), the plans-only input contract (production takes plan PDFs only; benchmark ground truth via --takeoff-pdf), learning-loop overview (verify → store → repeat-run application), accuracy gate usage.
    2. docs/DEPLOYMENT.md per Artifacts (env table, restart-on-deploy, error recovery semantics: per-sheet error dicts, budget_exhausted flags, learning.db backup, raster fallback behavior).
    3. 21-UAT.md: numbered UAT walking the 7 roadmap success criteria + the two VALIDATION manual rows — each step with exact commands/URLs and expected observations (e.g. "upload Crow plans PDF alone → report shows scale confidence HIGH 1\"=20', provenance breakdown ≥90% geometry/text"; "correct an item name, re-run, see the learned name applied"). Sign-off checklist at the end.
  </action>
  <verify>Files exist with the required content anchors; markdown lints clean (no broken code fences)</verify>
  <done>Live-testing documentation complete</done>
  <acceptance_criteria>
    - README.md contains `src/bobbytailor` and `--takeoff-pdf`
    - docs/DEPLOYMENT.md contains `LEARNING_DB_PATH` and `API_BUDGET_PER_RUN_USD`
    - 21-UAT.md contains ≥ 7 numbered UAT sections mapping to the roadmap success criteria
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-testing readiness sign-off</name>
  <what-built>The complete Phase 21 system: plans-only deterministic-first pipeline, learning loop, restructured package, accuracy gate, docs.</what-built>
  <how-to-verify>
    1. Execute 21-UAT.md end-to-end (upload run, flagged review, correction persistence, repeat-run application, StackCT parity run)
    2. Confirm README/DEPLOYMENT docs match reality (boot via gunicorn per docs)
    3. Push to GitHub (`Zaman152/Bobby-Tailor-Automation-2.0`) at this milestone per the locked process decision
  </how-to-verify>
  <resume-signal>Type "UAT signed off" or list failing UAT steps</resume-signal>
</task>

</tasks>

<verification>
- Suite green shim-free; docs anchors present; UAT executed by human
</verification>

<success_criteria>
- All modules in the structured package with no legacy import paths; pytest green; app boots via blueprints; README documents the layout (roadmap criterion 6)
- 21-UAT.md signed off for live testing (roadmap criterion 7; V3-PROD-02 complete)
</success_criteria>

<output>
After completion, create `.planning/phases/21-accuracy-learning-engine/21-18-SUMMARY.md`
</output>

---
phase: 21-accuracy-learning-engine
plan: 17
type: execute
wave: 9
depends_on: ["21-15", "21-16"]
requirements: [V3-PROD-01]
files_modified:
  - scripts/vision_only_benchmark.py
  - scripts/accuracy_gate.sh
  - tests/test_benchmark_scoring.py
autonomous: false

must_haves:
  truths:
    - "The benchmark runs the pipeline on the plans PDF alone and scores against an EXPLICITLY passed ground-truth take-off reference — the runtime can never discover it"
    - "The gate asserts: geometry/text-layer-provenance quantities ≥97% on Crow+Bob; overall quantity accuracy ≥90%; item-found ≥95%; zero silent misses; two-run reproducibility ±5%; Chelsea+Moxy scored and tracked (non-gating)"
    - "The gate is CI-invocable with one command and budget-guarded; cost regression warns at >2× baseline"
    - "Gate runs are learning-isolated: LEARNING_DB_PATH is pinned to a fresh temp DB so scores measure the cold pipeline, never learned hints from the dev learning.db"
  artifacts:
    - path: "scripts/vision_only_benchmark.py"
      provides: "provenance-split scoring + reproducibility + explicit ground-truth arg"
      contains: "--takeoff-pdf"
    - path: "scripts/accuracy_gate.sh"
      provides: "one-command CI gate over the four uploads/ projects"
  key_links:
    - from: "scripts/vision_only_benchmark.py"
      to: "takeoff.json provenance fields (21-13)"
      via: "per-provenance accuracy split"
      pattern: "provenance"
    - from: "scripts/accuracy_gate.sh"
      to: "uploads/ four project pairs"
      via: "explicit plans + takeoff path arguments"
      pattern: "uploads"
---

<objective>
Upgrade the plans-only benchmark into the CI-invocable accuracy gate (V3-PROD-01): explicit ground-truth arguments (no discovery), per-provenance accuracy splits, zero-silent-miss assertion, two-run reproducibility check, cost regression warning, and a one-command gate script covering all four uploads/ projects with Crow+Bob as the hard gate.

Purpose: this is the phase's success-criteria measuring instrument — layers 1-4 are free; this layer certifies integration and spends budget deliberately.
Output: upgraded benchmark, `accuracy_gate.sh`, offline scoring tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-accuracy-learning-engine/21-CONTEXT.md  (Benchmark Protocol LOCKED — uploads/ table, Crow+Bob hard gate, Chelsea+Moxy tracked)
@.planning/phases/21-accuracy-learning-engine/21-RESEARCH.md  (§8 layer 5 — gate assertions; cost regression; raster smoke)
@scripts/vision_only_benchmark.py
</context>

## Artifacts this phase produces

New symbols created by this plan:
- **CLI flags** on vision_only_benchmark.py: `--takeoff-pdf PATH` (explicit ground truth, replaces auto-discovery), `--plans-pdf PATH`, `--repro` (run twice, assert per-item ±5%), `--min-geometry-acc 0.97 --min-overall-acc 0.90 --min-item-found 0.95` (gate thresholds as args with the RESEARCH defaults), `--budget USD`, `--report-json PATH`
- **Function** `score_with_provenance(result, golden) -> dict` — extends the existing score() dict with `geometry_text_qty_acc`, `vision_qty_share`, `silent_misses: list` (golden categories neither present nor flagged), per-item table
- **Function** `check_reproducibility(result_a, result_b) -> dict` — per-item relative deltas, max/mean, pass if all ≤5%
- **Script** `scripts/accuracy_gate.sh` — runs the four uploads/ pairs (Crow, Bob gating with hard thresholds; Chelsea, Moxy scored with `--tracking-only`), prints a summary table, exit code reflects gating projects only; includes the cost-regression warning (>2× recorded baseline in `scripts/.cost_baseline.json`); exports `LEARNING_DB_PATH` to a fresh temp DB (`mktemp -d`) for every gate run so accumulated corrections in the dev learning.db can never inject learned hints into scores — gate numbers are cold-pipeline, machine-independent measurements
- **Assertion**: `vision_qty_share ≈ 0` on vector fixtures (the "vision-provenance quantities ≈ 0" gate from RESEARCH §3)

<threat_model>
ASVS L1:
- **Ground-truth leakage (HIGH — blocking):** the benchmark keeps the tempdir isolation AND the existing `assert companion is None` (now against the devtools finder) so a regression re-introducing discovery fails loudly; ground truth enters only through --takeoff-pdf.
- **Budget (MEDIUM):** the gate passes `--budget` through to RunSettings; two-run repro doubles cost — document expected spend in the script header (RESEARCH §4 cost model: low single-digit USD per fixture set).
- Scripts are dev-invoked; no new prod surface.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Benchmark upgrade — explicit ground truth + provenance scoring</name>
  <files>scripts/vision_only_benchmark.py</files>
  <read_first>
    - scripts/vision_only_benchmark.py (FULL — tempdir isolation lines 221-246, score() lines 103-172, cost read line 243, golden CSV loading)
    - .planning/phases/21-accuracy-learning-engine/21-CONTEXT.md Benchmark Protocol (uploads/ filenames incl. the "Take offds" typo; plans-only rule)
    - src/bobbytailor/devtools/companion_takeoff.py (the parser that reads take-off PDFs into scoreable rows — reuse for --takeoff-pdf ingestion)
  </read_first>
  <action>
    1. Replace companion auto-discovery with `--takeoff-pdf` (required unless a golden CSV fixture is used via existing paths); keep the tempdir isolation and strengthen the no-discovery assertion (sibling takeoff planted in the tempdir must NOT be read).
    2. `score_with_provenance`: split accuracy by item provenance from takeoff.json; compute geometry_text_qty_acc over items with provenance in {geometry, text_layer, schedule_table}; vision_qty_share = fraction of quantity-bearing items with provenance vision_ensemble; silent_misses = golden categories with no matching item AND no needs_review flag covering that category.
    3. `--repro`: second run in a fresh tempdir; check_reproducibility per Artifacts.
    4. Threshold flags with defaults; `--report-json` writes the full scoring dict (CI artifact). Exit non-zero on any gate failure.
  </action>
  <verify>python3 -m pytest tests/test_benchmark_scoring.py -q exits 0 (Task 2); python3 scripts/vision_only_benchmark.py --help shows --takeoff-pdf and --repro</verify>
  <done>Benchmark measures exactly the roadmap success criteria, plans-only by construction</done>
  <acceptance_criteria>
    - `python3 scripts/vision_only_benchmark.py --help` output contains `--takeoff-pdf`, `--repro`, `--min-geometry-acc`
    - Script contains `silent_misses` and `vision_qty_share`
    - The planted-sibling isolation assertion exists (source contains the tempdir plant + not-read check)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: accuracy_gate.sh + offline scoring tests</name>
  <files>scripts/accuracy_gate.sh, tests/test_benchmark_scoring.py</files>
  <read_first>
    - scripts/vision_only_benchmark.py (final CLI)
    - .planning/phases/21-accuracy-learning-engine/21-CONTEXT.md (exact uploads/ filenames for the four pairs)
  </read_first>
  <action>
    1. accuracy_gate.sh: invoke the benchmark for Crow + Bob with gate thresholds and --repro (hard gate — non-zero exit fails the script) and Chelsea + Moxy with --tracking-only (scores recorded, never fail). Use the exact uploads/ filenames (quote them — spaces and the "Take offds" typo). Learning isolation: at script start `export LEARNING_DB_PATH="$(mktemp -d)/gate_learning.db"` (a fresh empty DB per gate run) with a header comment explaining WHY — prior corrections in the dev learning.db would otherwise inject learned hints and make "plans-only" scores learning-assisted and machine-dependent; optionally add a `--with-learning` flag that skips the export and labels its summary column "learned-assisted" (reported separately, never gating). Cost baseline: read/write scripts/.cost_baseline.json; warn (not fail) when current run cost > 2× baseline. Print a final summary table.
    2. tests/test_benchmark_scoring.py (offline — canned takeoff.json + golden rows, no API): score_with_provenance math (accuracy splits, vision share, silent-miss detection incl. the flagged-counts-as-covered rule); check_reproducibility pass/fail cases at the 5% boundary; threshold flag plumbing (gate fails when geometry acc 0.96 < 0.97).
  </action>
  <verify>bash -n scripts/accuracy_gate.sh exits 0; python3 -m pytest tests/test_benchmark_scoring.py -q exits 0; full suite green</verify>
  <done>One-command CI gate; scoring math pinned offline</done>
  <acceptance_criteria>
    - scripts/accuracy_gate.sh contains all four uploads/ plan filenames and `--tracking-only` for Chelsea/Moxy only
    - scripts/accuracy_gate.sh exports `LEARNING_DB_PATH` to a temp path before any benchmark invocation (grep: `export LEARNING_DB_PATH`)
    - `python3 -m pytest tests/test_benchmark_scoring.py -q` exits 0 with ≥ 6 tests
    - `python3 -m pytest tests/ -q -m "not golden"` exits 0
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the budget-guarded accuracy gate (spends API credits)</name>
  <what-built>The full plans-only pipeline (waves 1-8) plus the upgraded benchmark and gate script. Layers 1-4 are green offline; this run certifies integration on real API calls.</what-built>
  <how-to-verify>
    1. Ensure ANTHROPIC_API_KEY has credit (Phase 20 was blocked on this — check before burning time)
    2. Run: `bash scripts/accuracy_gate.sh` (expected cost: single-digit USD; Crow+Bob gating incl. reproducibility double-run; Chelsea+Moxy tracked)
    3. Review the summary table: geometry/text accuracy ≥97% Crow+Bob, overall ≥90%, item-found ≥95%, zero silent misses, repro ≤5%
    4. If a gate fails, the per-layer design localizes it: scale wrong → layer 2 (21-07), wall/room quantity wrong → layer 1 (21-10), provenance leakage → layer 3 (21-12/13). Report the failing table rows.
  </how-to-verify>
  <resume-signal>Type "gate passed" with the summary numbers, or paste the failing rows for gap-closure planning</resume-signal>
</task>

</tasks>

<verification>
- Offline scoring tests green; gate script syntax-clean
- Human-verified gate run on Crow+Bob (the only API-spending step in the phase's test architecture)
</verification>

<success_criteria>
- vision_only_benchmark is the CI-invocable plans-only gate with provenance splits, zero-silent-miss, and reproducibility assertions (V3-PROD-01)
- Crow+Bob hard gate run and reviewed; Chelsea+Moxy tracked
</success_criteria>

<output>
After completion, create `.planning/phases/21-accuracy-learning-engine/21-17-SUMMARY.md`
</output>

---
phase: 21-accuracy-learning-engine
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [V3-STRUCT-01]
files_modified:
  - pyproject.toml
  - src/bobbytailor/** (git mv of 30 root modules)
  - "*.py root shims (one per moved module)"
  - tests/conftest.py
autonomous: true

must_haves:
  truths:
    - "All 348 existing deterministic tests pass after the package move"
    - "Every old root import path (e.g. `import geometry_takeoff`) still resolves via shim"
    - "The package installs editable and `python -c 'import bobbytailor'` succeeds"
  artifacts:
    - path: "pyproject.toml"
      provides: "src-layout package definition, editable install"
      contains: "bobbytailor"
    - path: "src/bobbytailor/__init__.py"
      provides: "package root"
    - path: "src/bobbytailor/deterministic/geometry_takeoff.py"
      provides: "moved module (byte-identical to pre-move geometry_takeoff.py)"
    - path: "geometry_takeoff.py"
      provides: "root shim re-exporting bobbytailor.deterministic.geometry_takeoff"
      contains: "from bobbytailor.deterministic.geometry_takeoff import"
  key_links:
    - from: "root shims"
      to: "src/bobbytailor/*"
      via: "star re-export plus explicit private-name re-exports"
      pattern: "from bobbytailor\\..* import \\*"
---

<objective>
Restructure the 30 flat root modules into the `src/bobbytailor/` package (V3-STRUCT-01) using the migration strategy from 21-RESEARCH.md §7: one pure `git mv` commit, root import shims, `pyproject.toml` with src-layout, all 348 tests green with zero content edits to moved modules.

Purpose: every subsequent Phase 21 plan creates new modules (`deterministic/`, `learning/`, `vision/`, `web/blueprints/`) directly in their final package homes — no double-moves later.
Output: `src/bobbytailor/` package tree, root shims, `pyproject.toml`, green test suite.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-accuracy-learning-engine/21-RESEARCH.md  (§7 Package Restructure — the migration strategy is LOCKED)
@.planning/phases/21-accuracy-learning-engine/21-PATTERNS.md  (No Analog section: pyproject + shims)
@tests/conftest.py
@main.py
</context>

## Artifacts this phase produces

New symbols created by this plan:
- **File** `pyproject.toml` — `[project] name = "bobbytailor"`, `[tool.setuptools] package-dir = {"" = "src"}` (or equivalent hatchling config)
- **Package tree** `src/bobbytailor/` with subpackages: `pipeline/`, `vision/`, `deterministic/`, `scrape/`, `stores/`, `devtools/`, `web/` — each with `__init__.py`
- **Moved modules** (byte-identical, `git mv` only):
  - `src/bobbytailor/config.py` ← `config.py`
  - `src/bobbytailor/pipeline/` ← `takeoff_pipeline.py`, `sheet_pass_matrix.py`, `calculator.py`, `aggregator.py`, `reporter.py`, `cross_references.py`, `accuracy_config.py`, `page_text_enrichment.py`, `pdf_analyzer.py`
  - `src/bobbytailor/vision/` ← `claude_analyzer.py`
  - `src/bobbytailor/deterministic/` ← `geometry_takeoff.py`, `scale_extraction.py`, `scale_utils.py`, `scale_recalc.py`, `footprint_takeoff.py`, `schedule_extraction.py`, `plan_deterministic_legends.py`, `takeoff_measurements.py`
  - `src/bobbytailor/scrape/` ← `scraper.py`, `browser.py`, `stackct_sync.py`, `sheet_preview.py`, `capture_manifest.py`, `linked_sheets.py`
  - `src/bobbytailor/stores/` ← `stackct_store.py`, `job_store.py`, `project_cache.py`
  - `src/bobbytailor/devtools/` ← `companion_takeoff.py`, `object_manifest.py`
  - `src/bobbytailor/web/` ← `app.py`, `auth.py`, `settings.py`, `seed_admin.py`
- **Root shim files** — one per moved module, same old filename at repo root (e.g. root `geometry_takeoff.py` containing `from bobbytailor.deterministic.geometry_takeoff import *` plus explicit re-exports of private names that tests import)
- No new classes/functions/CLI flags/DB tables.

<threat_model>
ASVS L1. This plan changes zero behavior — threats are regression-shaped:
- **Auth/CSRF preservation (HIGH if broken):** `app.py`, `auth.py` move but must not be edited. Flask-Login `@login_required`, CSRF token wiring, and rate limiting must be byte-identical post-move. Mitigation: `git mv` only; `git diff HEAD~1 --stat -M100%` must show 100% renames for moved files. Blocking check in Task 3.
- **Secret handling:** `config.py` `.env` discovery uses `Path(__file__).resolve().parent / ".env"` — after the move `__file__` is `src/bobbytailor/config.py`, so the project-root `.env` would NOT be found at `parent`. The existing fallback (`Path.cwd() / ".env"`) covers app-run-from-root, but the shim task must verify env loading still resolves the repo-root `.env` (walk up parents or keep fallback). This is the ONE permitted content edit, isolated to a follow-up commit, never in the move commit.
- No new attack surface introduced.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create pyproject.toml + package skeleton + git mv all 30 modules</name>
  <files>pyproject.toml, src/bobbytailor/** (all subpackage __init__.py files), git mv of all root modules listed in Artifacts</files>
  <read_first>
    - .planning/phases/21-accuracy-learning-engine/21-RESEARCH.md (§7 — exact layout and migration steps)
    - config.py (note the `.env` path resolution that will break — do NOT fix in the move commit)
    - main.py (imports `from scraper import ...`, `from config import ...` — stays at root, relies on shims)
    - scripts/vision_only_benchmark.py lines 40-50 (sys.path.insert(0, ROOT) pattern — shims keep scripts working)
  </read_first>
  <action>
    1. Write `pyproject.toml`: `[project] name = "bobbytailor"`, `requires-python = ">=3.10"`, dependencies mirroring requirements.txt (playwright, anthropic, apscheduler, python-dotenv, flask, flask-login, flask-bcrypt, flask-wtf, flask-limiter, pymupdf>=1.26, pillow). Build backend setuptools with `[tool.setuptools] package-dir = {"" = "src"}` and `packages.find where = ["src"]`.
    2. Create `src/bobbytailor/__init__.py` and subpackage `__init__.py` for: pipeline, vision, deterministic, scrape, stores, devtools, web (all empty or docstring-only).
    3. `git mv` each root module to its destination per the Artifacts mapping above. ONE commit, content byte-identical (RESEARCH §7 step 1). `main.py`, `tests/`, `scripts/`, `templates/`, `static/` do NOT move.
    4. Note: `pymupdf` floor raised to `>=1.26` in pyproject AND requirements.txt (RESEARCH §1 — table-finder and drawings fixes needed by later plans).
  </action>
  <verify>git diff --stat -M100% HEAD~1 shows only renames for moved modules; `pip install -e .` exits 0; `python3 -c "import bobbytailor.deterministic.geometry_takeoff"` exits 0</verify>
  <done>All 30 modules live under src/bobbytailor/ as pure renames; package imports resolve</done>
  <acceptance_criteria>
    - `test -f src/bobbytailor/deterministic/geometry_takeoff.py && test -f src/bobbytailor/pipeline/takeoff_pipeline.py && test -f src/bobbytailor/web/app.py` exits 0
    - `git log --follow --oneline src/bobbytailor/deterministic/geometry_takeoff.py` shows pre-move history (rename tracked)
    - `pyproject.toml` contains `package-dir` and `"bobbytailor"`
    - requirements.txt contains `pymupdf>=1.26`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Root import shims for every moved module + config .env fix</name>
  <files>30 root shim .py files (one per moved module, original filenames), src/bobbytailor/config.py (env-path fix only), tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (the `_ensure_real` pre-registration and env pinning — shim names must keep these imports working)
    - tests/test_pipeline_parity.py (sys.modules.setdefault stubbing — shims must not break the stub mechanism)
    - src/bobbytailor/config.py (post-move; the `.env` resolution to fix)
    - src/bobbytailor/deterministic/geometry_takeoff.py (identify private names tests import, e.g. `_segments`, `_calibrate_from_dimensions` — check `rg "from geometry_takeoff import|import geometry_takeoff" tests/` for the exact needed names)
  </read_first>
  <action>
    1. For each moved module create a root shim with the ORIGINAL filename, e.g. root `geometry_takeoff.py` = `from bobbytailor.deterministic.geometry_takeoff import *  # noqa: F401,F403` PLUS explicit re-imports of every private/underscore name any test or script references (find with `rg "from <mod> import" tests/ scripts/ *.py`). Do the same for all 30 modules (app.py shim: `from bobbytailor.web.app import *` plus `from bobbytailor.web.app import app`).
    2. Fix `src/bobbytailor/config.py` `.env` resolution: `_env_path` must check, in order: `Path(__file__).resolve().parents[2] / ".env"` (repo root from src/bobbytailor/), then `Path.cwd() / ".env"`. Separate commit from the move commit.
    3. Add to tests/conftest.py top (before existing content): `sys.path.insert(0, str(Path(__file__).resolve().parents[1]))` guarded to only apply if `bobbytailor` is not importable — the fallback path insert RESEARCH §7 step 3 requires during transition.
    4. Internal imports inside moved modules stay UNCHANGED (they say `from scale_extraction import ...` and resolve through the root shims because repo root is on sys.path). Preserve all lazy in-function imports exactly (RESEARCH §7 risk: hidden cycles).
  </action>
  <verify>python3 -m pytest tests/ -q -m "not golden" exits 0 with 348 passed (Validation layer: full deterministic suite)</verify>
  <done>Every legacy import path works; env loading finds repo-root .env; suite green</done>
  <acceptance_criteria>
    - Root `geometry_takeoff.py` contains `from bobbytailor.deterministic.geometry_takeoff import *`
    - `python3 -c "import app; assert hasattr(app, 'app')"` exits 0 (Flask object reachable through shim)
    - `python3 -m pytest tests/ -q -m "not golden"` output contains `348 passed` (or the current green count if it moved) and `0 failed`
    - `python3 -c "from bobbytailor.config import OUTPUT_DIR"` exits 0 from repo root
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Regression sweep — scripts, web boot, rename integrity</name>
  <files>none (verification only; fix-forward any breakage found)</files>
  <read_first>
    - scripts/vision_only_benchmark.py (sys.path bootstrap)
    - main.py (entry imports)
  </read_first>
  <action>
    Run the full regression sweep: (a) full pytest suite; (b) `python3 -c "from scripts import ..."`-equivalent smoke of each script's import block via `python3 scripts/vision_only_benchmark.py --help` (must print usage, not ImportError); (c) Flask boot smoke `python3 -c "import app; app.app.test_client().get('/login')"` expecting HTTP 200; (d) rename-integrity: `git diff HEAD~2 --stat -M100%` on the move commit shows no content changes to moved modules. Fix any breakage by adjusting SHIMS only — moved module content is frozen except the config env fix.
  </action>
  <verify>python3 -m pytest tests/ -q -m "not golden" green; vision_only_benchmark.py --help prints usage; login page returns 200</verify>
  <done>Package move is complete, invisible to tests, scripts, and the web app</done>
  <acceptance_criteria>
    - `python3 -m pytest tests/ -q -m "not golden"` exits 0
    - `python3 scripts/vision_only_benchmark.py --help` exits 0
    - `python3 -c "import app; rv = app.app.test_client().get('/login'); assert rv.status_code == 200"` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Full deterministic suite green (VALIDATION.md quick command: `python3 -m pytest tests/ -q -m "not golden"`)
- `git log --follow` shows history preserved on at least 3 spot-checked moved modules
- No content diff on moved modules in the move commit (100% renames)
</verification>

<success_criteria>
- src/bobbytailor/ package exists with all 30 modules relocated per the RESEARCH §7 layout
- 348 tests green, scripts and Flask app functional through shims
- pyproject.toml editable install works
</success_criteria>

<output>
After completion, create `.planning/phases/21-accuracy-learning-engine/21-01-SUMMARY.md`
</output>

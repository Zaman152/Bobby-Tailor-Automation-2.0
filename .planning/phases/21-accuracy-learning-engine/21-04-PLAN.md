---
phase: 21-accuracy-learning-engine
plan: 04
type: execute
wave: 2
depends_on: ["21-01"]
requirements: [V3-LEARN-01, V3-LEARN-02]
files_modified:
  - src/bobbytailor/learning/__init__.py
  - src/bobbytailor/learning/store.py
  - src/bobbytailor/learning/distill.py
  - src/bobbytailor/config.py
  - tests/test_learning_store.py
autonomous: true

must_haves:
  truths:
    - "A human correction recorded via record_correction() survives deletion of the run folder and is retrievable by (project_type, sheet_type, scope, item_pattern)"
    - "Distill is idempotent: running it twice over the same corrections log produces identical vocabulary/assumption/manifest rows"
    - "A contradicting correction decays the old value (weight × 0.5) and the new value wins retrieval"
  artifacts:
    - path: "src/bobbytailor/learning/store.py"
      provides: "learning_corrections/vocabulary/assumptions/manifests tables + CRUD"
      exports: ["record_correction", "get_vocabulary", "get_assumptions", "get_manifest", "init_learning_schema"]
      min_lines: 150
    - path: "src/bobbytailor/learning/distill.py"
      provides: "append-only log → distilled tables rebuild"
      exports: ["distill"]
    - path: "tests/test_learning_store.py"
      provides: "layer-4 round-trip, conflict/decay, idempotency tests"
      min_lines: 80
  key_links:
    - from: "src/bobbytailor/learning/store.py"
      to: "output/stackct.db (or LEARNING_DB_PATH)"
      via: "sqlite3 with parameterized queries"
      pattern: "execute\\(\\s*[\"'].*\\?"
    - from: "src/bobbytailor/learning/distill.py"
      to: "learning_corrections"
      via: "rebuild from append-only log"
      pattern: "learning_corrections"
---

<objective>
Create the durable learning store (V3-LEARN-01) and the distillation job (V3-LEARN-02): four SQLite tables (`learning_corrections` append-only log; `learning_vocabulary`, `learning_assumptions`, `learning_manifests` distilled state) with the exact schema from 21-RESEARCH.md §6, plus conflict/decay policy and idempotent rebuild.

Purpose: human verifications must survive run-folder deletion and accumulate what manifests used to provide. Retrieval/injection (21-16) and endpoint capture (21-09) both build on this module.
Output: `learning/store.py`, `learning/distill.py`, layer-4 tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-accuracy-learning-engine/21-RESEARCH.md  (§6 Learning Store Design — schema SQL is authoritative)
@.planning/phases/21-accuracy-learning-engine/21-PATTERNS.md  (store.py assignment — lock/Row/upsert/migration patterns)
@src/bobbytailor/stores/stackct_store.py
@src/bobbytailor/stores/job_store.py
</context>

## Artifacts this phase produces

New symbols created by this plan:
- **Module** `src/bobbytailor/learning/store.py` with:
  - `init_learning_schema()` — lazy double-checked schema init (job_store pattern) guarded by `learning_schema_version` metadata key
  - `record_correction(run_id, project_key, project_type, sheet_type, scope, item_pattern, original_value, corrected_value, context) -> int`
  - `get_vocabulary(project_type) -> list[dict]`, `get_assumptions(project_key, project_type) -> list[dict]`, `get_manifest(project_type) -> list[dict]`
  - module-level `_write_lock`, `get_connection()`
- **DB tables**: `learning_corrections` (+ index `ix_corr_lookup`), `learning_vocabulary`, `learning_assumptions`, `learning_manifests` — column definitions exactly per RESEARCH §6 SQL
- **Module** `src/bobbytailor/learning/distill.py` with `distill(full_rebuild: bool = False) -> dict[str, int]` (stats dict return, migrate_from_json_caches pattern)
- **Config**: `LEARNING_DB_PATH` in `config.py` (env `LEARNING_DB_PATH`, default `Path(OUTPUT_DIR) / "learning.db"`)
- **Scope enum values** (string constants in store.py): `'item' | 'scale' | 'wall_height' | 'name' | 'missed_item' | 'unit'`

<threat_model>
ASVS L1 — SQLite injection via learned strings is a named phase surface:
- **SQL injection through corrected_value/item_pattern (HIGH — blocking):** every value a human types flows into these tables and back out into prompts/aggregation. Mitigation: 100% parameterized queries (`?` placeholders) — Task 3 acceptance greps for f-string/`%`/`+` SQL construction and must find none; store module is the ONLY module allowed to touch `learning_*` tables (PATTERNS shared-pattern rule).
- **Stored-payload reflection (MEDIUM):** learned strings are later injected into prompts and UI. Store JSON values via `json.dumps` only; retrieval returns parsed values, never raw string interpolation into SQL. Length cap: `corrected_value`/`item_pattern` truncated at 2,000 chars on write (defense against payload stuffing).
- **DB path traversal (LOW):** `LEARNING_DB_PATH` comes from env, not user input; resolve and mkdir parents like STACKCT_DB_PATH.
- Decision: dedicated `learning.db` (not stackct.db) — isolates migration risk per RESEARCH §6 parenthetical; CONTEXT grants discretion.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: store.py — schema, init, record_correction</name>
  <files>src/bobbytailor/learning/__init__.py, src/bobbytailor/learning/store.py, src/bobbytailor/config.py</files>
  <read_first>
    - .planning/phases/21-accuracy-learning-engine/21-RESEARCH.md §6 (the CREATE TABLE statements — copy verbatim into SCHEMA_SQL)
    - src/bobbytailor/stores/stackct_store.py (lines 20-21, 81-149: _write_lock, get_connection with Row factory + WAL, schema-version metadata pattern)
    - src/bobbytailor/stores/job_store.py (lines 57-70: lazy double-checked _ensure_schema)
  </read_first>
  <action>
    1. Add `LEARNING_DB_PATH = Path(os.getenv("LEARNING_DB_PATH", "")) if os.getenv("LEARNING_DB_PATH") else Path(OUTPUT_DIR) / "learning.db"` to config.py.
    2. store.py: SCHEMA_SQL = the four CREATE TABLE + CREATE INDEX statements from RESEARCH §6 verbatim, plus a `learning_metadata(key, value, updated_at)` table holding `learning_schema_version = "1"`. `get_connection()` per stackct_store pattern (Row factory, foreign_keys ON, WAL journal mode). `_ensure_schema()` per job_store double-checked pattern.
    3. `record_correction(...)`: named-parameter INSERT (job_store.save_job_run pattern); `original_value`/`corrected_value`/`context` serialized with json.dumps; truncate serialized values at 2000 chars; `created_at` = UTC ISO; returns lastrowid. All writes under `_write_lock`.
    4. Read APIs: `get_vocabulary(project_type)` returns rows where `project_type = ? OR project_type IS NULL` ordered by weight DESC; `get_assumptions(project_key, project_type)` returns rows with specificity precedence (exact project_key first, then project_type, then global — RESEARCH §6 conflict policy) as a deduped-by-key list keeping the most specific max-weight row; `get_manifest(project_type)` returns manifest rows with `seen_in_runs/verified_runs_total` expectation strength.
  </action>
  <verify>python3 -c "from bobbytailor.learning import store; store.init_learning_schema(); import sqlite3; from bobbytailor.config import LEARNING_DB_PATH; tabs = [r[0] for r in sqlite3.connect(LEARNING_DB_PATH).execute(\"SELECT name FROM sqlite_master WHERE type='table'\")]; assert set(['learning_corrections','learning_vocabulary','learning_assumptions','learning_manifests']) <= set(tabs)" exits 0</verify>
  <done>Schema exists; corrections persist; reads honor specificity precedence</done>
  <acceptance_criteria>
    - `sqlite3 output/learning.db ".tables"` (after init) lists learning_corrections, learning_vocabulary, learning_assumptions, learning_manifests
    - store.py contains zero SQL built with f-strings/percent-format: `rg "execute\(f\"|execute\(.*%s|execute\(.*\" \+ " src/bobbytailor/learning/` returns no matches
    - config.py contains `LEARNING_DB_PATH`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: distill.py — log → vocabulary/assumptions/manifests with conflict/decay</name>
  <files>src/bobbytailor/learning/distill.py</files>
  <read_first>
    - src/bobbytailor/learning/store.py (Task 1 state — schema and helpers)
    - src/bobbytailor/stores/stackct_store.py (lines 540-585: migrate_from_json_caches idempotent-batch + stats-dict pattern; lines 180-192 ON CONFLICT upsert)
    - .planning/phases/21-accuracy-learning-engine/21-RESEARCH.md §6 (Conflict/decay policy: +1 weight confirming, ×0.5 decay contradicting, max-weight wins, ties → most recent, staleness decay)
  </read_first>
  <action>
    `distill(full_rebuild=False)`: iterate `learning_corrections` (all rows if full_rebuild, else rows newer than a `last_distilled_id` metadata watermark) and update distilled tables in one transaction:
    - scope `'name'`/`'unit'` → upsert `learning_vocabulary` (UNIQUE(project_type, raw_pattern), ON CONFLICT weight = weight + 1.0, last_seen = now); a row with same key but DIFFERENT canonical_name/unit first decays the existing row (weight × 0.5) then upserts the new mapping
    - scope `'scale'`/`'wall_height'` (and generic assumption corrections) → upsert `learning_assumptions` with key naming convention `'wall_height_ft'` and `'scale_fpi:<sheetname-pattern>'` exactly as RESEARCH §6 shows
    - scope `'item'`/`'missed_item'` on verified runs → update `learning_manifests` (`seen_in_runs` increment, `verified_runs_total` maintained per project_type via distinct run_id count)
    - staleness: rows whose last_seen is older than the 10 most recent verified runs for that project_type decay weight × 0.9 per distill pass
    - update `last_distilled_id` watermark; return stats dict `{"corrections_processed": n, "vocabulary_rows": n, ...}`
    full_rebuild=True wipes distilled tables first and replays the whole log (the auditable-rebuild guarantee).
  </action>
  <verify>python3 -m pytest tests/test_learning_store.py -q -k "distill" exits 0 (Task 3 writes the tests; inline smoke: record two contradicting name corrections, distill, assert new mapping weight > old)</verify>
  <done>Distilled state is a pure function of the append-only log; conflict/decay policy implemented</done>
  <acceptance_criteria>
    - distill.py contains `def distill(` and `ON CONFLICT`
    - Calling distill() twice with no new corrections returns `{"corrections_processed": 0, ...}` on the second call
    - After recording correction A(name X→Y) then B(name X→Z): `get_vocabulary` top row for pattern X has canonical_name Z and the Y row weight is 0.5
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Layer-4 test suite — round-trip, idempotency, decay, survival</name>
  <files>tests/test_learning_store.py</files>
  <read_first>
    - src/bobbytailor/learning/store.py, src/bobbytailor/learning/distill.py (final state)
    - tests/test_job_store.py (tmp_path DB isolation pattern for SQLite tests)
    - .planning/phases/21-accuracy-learning-engine/21-VALIDATION.md (layer 4 contract)
  </read_first>
  <action>
    Write tests (each using a tmp_path LEARNING_DB_PATH via monkeypatch of config):
    1. round-trip: record_correction(scope='item') → row retrievable via direct query by (project_type, sheet_type, scope, item_pattern)
    2. run-folder independence: correction references run_id of a nonexistent folder — retrieval still works (no filesystem dependency)
    3. distill idempotency: distill(); distill() → second stats show 0 processed; full_rebuild=True reproduces identical table contents (compare row dumps)
    4. conflict/decay: contradicting vocabulary corrections → decay + replacement per Task 2 criteria
    5. specificity precedence: assumptions for exact project_key beat project_type beat global in get_assumptions ordering
    6. injection safety: record_correction with item_pattern `"x'; DROP TABLE learning_corrections;--"` → table still exists, value round-trips literally
  </action>
  <verify>python3 -m pytest tests/test_learning_store.py -q exits 0; full suite still green</verify>
  <done>Learning store layer-4 tests green including the injection probe</done>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_learning_store.py -q` exits 0 with ≥ 6 tests
    - Test file contains the literal injection probe string `DROP TABLE`
    - `python3 -m pytest tests/ -q -m "not golden"` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Layer-4 suite green; full deterministic suite green
- `sqlite3 output/learning.db ".schema learning_corrections"` matches RESEARCH §6 columns
</verification>

<success_criteria>
- Corrections persist independently of run folders, keyed for retrieval (V3-LEARN-01)
- Vocabulary/assumptions/manifests distill idempotently from the append-only log with conflict/decay (V3-LEARN-02 storage half; ITEM_NAME_MAP integration lands in 21-16)
</success_criteria>

<output>
After completion, create `.planning/phases/21-accuracy-learning-engine/21-04-SUMMARY.md`
</output>

---
phase: 21-accuracy-learning-engine
plan: 03
type: execute
wave: 2
depends_on: ["21-01"]
requirements: [V3-ACC-01, V3-ACC-02]
files_modified:
  - src/bobbytailor/deterministic/dimensions.py
  - tests/test_dimensions.py
  - tests/fixtures/synthetic/gen_synthetic.py
  - requirements.txt
  - pyproject.toml
autonomous: true

must_haves:
  truths:
    - "Every printed feet-inch dimension string on a fixture sheet is parsed (incl. fractions and stacked fractions) and paired with its grammar-verified dimension line, or dropped — never mispaired"
    - "Crow Cass page 1 yields ≥8 DimensionSamples whose feet/points ratios agree within 2% at the true 1\"=20' main-plan scale"
    - "The 1136' and 350' overall dims are among the associated samples on Crow"
  artifacts:
    - path: "src/bobbytailor/deterministic/dimensions.py"
      provides: "Recipe A dimension-line association engine"
      exports: ["DimensionSample", "extract_dimension_samples", "parse_feet_inches"]
      min_lines: 150
    - path: "tests/test_dimensions.py"
      provides: "layer-1 unit tests on fixtures + synthetics"
      min_lines: 80
    - path: "tests/fixtures/synthetic/gen_synthetic.py"
      provides: "PyMuPDF Shape-drawn synthetic fixture generator with known ground truth"
  key_links:
    - from: "src/bobbytailor/deterministic/dimensions.py"
      to: "shapely.STRtree"
      via: "corridor search over indexed segments"
      pattern: "STRtree"
    - from: "tests/test_dimensions.py"
      to: "tests/fixtures/crow_cass/crow_cass_plans.pdf"
      via: "fixture-driven assertions"
      pattern: "crow_cass_plans"
---

<objective>
Build the dimension-line association engine (RESEARCH Recipe A) — the highest-value deterministic component: harvest every dimension string from the text layer, pair each with its grammar-verified dimension-line geometry (terminators + extension lines), and emit `DimensionSample(feet, points, ...)` records that the scale solver (21-07) consumes.

Purpose: replaces the brute-force nearest-segment calibration whose mispairing caused the 1"=24'-vs-20' Crow failure; provides the raw material for zero-calibration scale solving (V3-ACC-02) and text-layer-authoritative measurement (V3-ACC-01).
Output: `deterministic/dimensions.py`, synthetic fixture generator, layer-1 tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-accuracy-learning-engine/21-RESEARCH.md  (§1 Recipe A — the algorithm spec; Pitfalls 2, 3, 4)
@.planning/phases/21-accuracy-learning-engine/21-PATTERNS.md  (dimensions.py assignment — regex, rotation, stacked-fraction, dataclass patterns)
@src/bobbytailor/deterministic/footprint_takeoff.py
@src/bobbytailor/deterministic/scale_extraction.py
</context>

## Artifacts this phase produces

New symbols created by this plan:
- **Dataclass** `DimensionSample` — fields: `feet: float`, `points: float`, `text_bbox: tuple`, `line_bbox: tuple`, `rotation: tuple`, `score: float`, `page_index: int`, plus `to_dict()` via `asdict`
- **Function** `parse_feet_inches(text: str) -> Optional[float]` — feet-inch parser incl. fractions (`25'-4 1/2"`), bare feet (`60'`), stacked-fraction char recovery
- **Function** `extract_dimension_samples(page, exclude_rects: list = None) -> list[DimensionSample]` — full Recipe A pipeline on one page
- **Function** `_harvest_dimension_texts(page)` (internal) — rawdict harvesting with `line["dir"]` rotation
- **Function** `_visible_drawings(page, doc)` (internal) — get_drawings() filtered by OCG hidden-layer state (Pitfall 3); shared later by walls/rooms/symbols via import
- **Module** `tests/fixtures/synthetic/gen_synthetic.py` — CLI: `python3 tests/fixtures/synthetic/gen_synthetic.py --out <dir>`; generates PDFs with known dims/walls/symbols truth JSON alongside
- **New dependency**: `shapely>=2.0`, `numpy>=1.26`, `networkx>=3.2` added to requirements.txt + pyproject.toml (installed here, consumed by 21-06/07/10 too)

<threat_model>
ASVS L1 — PDF parsing of untrusted files surface:
- **Malicious/degenerate PDF input (MEDIUM):** dimension harvesting runs regexes and geometry loops over attacker-supplied PDFs. Mitigations: reuse the lazy-fitz open/close-with-try/finally pattern (footprint_takeoff lines 221-264); cap processed drawing items per page (skip association if > 500k segments — log + return empty with `needs_review` semantics rather than hang); all parsing wrapped so an exception on one page returns `[]` for that page, never crashes the run.
- **ReDoS (LOW):** dimension regex must be linear — no nested quantifiers; extend the anchored `footprint_takeoff._DIM_RE` style.
- No network, no SQL, no file writes in this module.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: parse_feet_inches + dimension text harvesting (rotation-aware)</name>
  <files>src/bobbytailor/deterministic/dimensions.py, requirements.txt, pyproject.toml</files>
  <read_first>
    - src/bobbytailor/deterministic/footprint_takeoff.py (lines 29-94: _DIM_RE, _parse_feet, _axis_dims rotation handling — the base patterns to extend)
    - src/bobbytailor/deterministic/scale_extraction.py (lines ~102-119: _paper_inches_from_chars stacked-fraction char-split to reuse for fractional inches)
    - .planning/phases/21-accuracy-learning-engine/21-RESEARCH.md §1 Recipe A step 1 (formats: `1136' - 0"`, `350'-0`, `25'-4 1/2"`, stacked `1'-6¹/₂"`, bare `60'`; reject elevations/GL/INV, slopes, tags)
  </read_first>
  <action>
    1. Add `shapely>=2.0`, `numpy>=1.26`, `networkx>=3.2` to requirements.txt and pyproject dependencies; `pip install -e .` to pull them.
    2. Create `dimensions.py` with `parse_feet_inches(text)` supporting: `NNN' - N"`, `NNN'-N`, `NN'-N F/D"` (fraction), bare `NN'`, returning float feet or None. Plausibility bounds 0.5–2000 ft (dimension strings, unlike overall dims, go down to inches-scale — do NOT copy the 50-ft floor from footprint_takeoff). Reject tokens preceded/followed by `GL`, `INV`, `EL`, `T.O.`, `B.O.`, `%`, `/FT` context (elevation/slope guard — same exclusion class as ACCURACY-08).
    3. `_harvest_dimension_texts(page)`: iterate `page.get_text("rawdict")` lines; record `(text, feet, center, bbox, dir)` per parsed hit; handle stacked fractions via per-char y-split (reuse the scale_extraction logic — import or replicate with attribution comment); handle `dir` values (1,0), (0,±1), (-1,0) (Pitfall 4: 180° upside-down dims).
    4. `_visible_drawings(page, doc)`: return `page.get_drawings()` filtered to exclude paths on OFF optional-content layers using `doc.get_ocgs()` / layer config when present (Pitfall 3); on any OCG API error fall back to unfiltered.
  </action>
  <verify>python3 -m pytest tests/test_dimensions.py -q -k "parse" exits 0 (written in Task 3; during this task use a quick inline check: python3 -c "from bobbytailor.deterministic.dimensions import parse_feet_inches as p; assert p(\"1136' - 0\\\"\")==1136.0; assert p(\"25'-4 1/2\\\"\")==25.375; assert p(\"60'\")==60.0; assert p('3%') is None")</verify>
  <done>Parser handles all RESEARCH formats; harvesting returns rotation-tagged dimension texts</done>
  <acceptance_criteria>
    - `python3 -c "from bobbytailor.deterministic.dimensions import parse_feet_inches as p; assert p(\"25'-4 1/2\\\"\") == 25.375"` exits 0
    - dimensions.py contains `def parse_feet_inches(` and `def _harvest_dimension_texts(` and `get_ocgs`
    - requirements.txt contains `shapely>=2.0`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: STRtree corridor search + terminator verification → DimensionSample</name>
  <files>src/bobbytailor/deterministic/dimensions.py</files>
  <read_first>
    - src/bobbytailor/deterministic/dimensions.py (Task 1 state)
    - src/bobbytailor/deterministic/geometry_takeoff.py (lines 50-66 `_segments` harvest to reuse; lines 124-159 `_calibrate_from_dimensions` — the ANTI-PATTERN being replaced, do not copy its nearest-segment logic)
    - .planning/phases/21-accuracy-learning-engine/21-RESEARCH.md §1 Recipe A steps 2-4 (corridor parameters: direction within ~3°, perpendicular offset 2-15 pt, longest candidate; terminators: ≥2 short segments < 6 pt within ~4 pt of endpoint, or perpendicular extension line; chained dims: drop ambiguous sub-segments)
  </read_first>
  <action>
    Implement `extract_dimension_samples(page, exclude_rects=None)`:
    1. Build `shapely.STRtree` over LineStrings from `_visible_drawings` straight segments (filter out segments inside `exclude_rects` — title block/legend masks supplied by viewports module later; parameter defaults to None = no masking).
    2. For each harvested dim text: corridor query per Recipe A step 2 (direction match within 3° of text `dir`, perpendicular distance of segment midpoint to text bbox 2-15 pt, pick the longest candidate).
    3. Terminator verification per step 3: at each candidate endpoint search within 4 pt for ≥2 segments shorter than 6 pt (arrowhead/tick cluster) OR a perpendicular segment crossing near the endpoint (extension line). Require at least one signal per end; compute `score` = number of satisfied signals (0-4); drop candidates scoring < 2.
    4. Chained-dimension handling: if two dim texts pair to the SAME segment, attempt sub-segmentation at extension-line crossings; if ambiguous, drop those samples (Recipe A: "drop the sample rather than guess").
    5. Emit `DimensionSample` list. Guard: if page has > 500k segments, log warning and return [].
  </action>
  <verify>python3 -c quick fixture probe: extract_dimension_samples on tests/fixtures/crow_cass/crow_cass_plans.pdf page 0 returns ≥ 8 samples, and at least 60% of their feet/points ratios lie within 2% of one consensus value</verify>
  <done>Grammar-verified pairing produces consistent samples on Crow; no brute-force nearest-segment logic remains</done>
  <acceptance_criteria>
    - dimensions.py contains `def extract_dimension_samples(` and `STRtree(`
    - dimensions.py does NOT import or call `_calibrate_from_dimensions`
    - On crow_cass_plans.pdf page 0: `len(samples) >= 8` and the largest 2%-tolerance consensus set covers ≥ 60% of samples (asserted in Task 3 test)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Synthetic fixture generator + layer-1 test suite</name>
  <files>tests/fixtures/synthetic/gen_synthetic.py, tests/test_dimensions.py</files>
  <read_first>
    - tests/test_takeoff_generalization.py (lines 24-36, 79-110: fixture + expected-constraints pattern, pinned assertions)
    - tests/fixtures/crow_cass/ and tests/fixtures/bobs_discount/ (existing fixture PDFs to assert against)
    - .planning/phases/21-accuracy-learning-engine/21-VALIDATION.md (Wave 0: synthetic geometry fixture generator requirement)
  </read_first>
  <action>
    1. `gen_synthetic.py`: use PyMuPDF `Shape` to draw test pages with KNOWN truth — a horizontal dim (line + tick terminators + extension lines + text "50'-0\""), a vertical dim (rotated text), a chained dim row, a fraction dim (`12'-6 1/2"`), and a decoy (text with no dimension line). Write `truth.json` beside each generated PDF: `{page: [{text, feet, expected_associated: bool}]}`. Idempotent CLI writing into `tests/fixtures/synthetic/`.
    2. `tests/test_dimensions.py` (validation layer 1):
       - parse tests: the exact format table from Recipe A incl. rejects (`3%`, `GL 850'`, `EL 12'-0"`)
       - synthetic association tests: run generator (or commit generated PDFs), assert each `expected_associated: true` dim yields a sample with correct feet and the decoy yields none
       - Crow fixture test: page 0 of crow_cass_plans.pdf → ≥8 samples, consensus set ≥60%, and samples include feet values 1136.0 and 350.0 (the proven overall dims)
       - Bob fixture smoke: bobs_discount_plans.pdf page 0 → extraction returns without error (287 paths — sparse vector page)
       - degenerate guard test: blank one-page PDF → []
  </action>
  <verify>python3 -m pytest tests/test_dimensions.py -q exits 0; python3 -m pytest tests/ -q -m "not golden" still green</verify>
  <done>Layer-1 dimension tests green on synthetics + both hard-gate fixtures</done>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_dimensions.py -q` exits 0 with ≥ 10 tests collected
    - test file contains assertions for feet values `1136.0` and `350.0`
    - `python3 tests/fixtures/synthetic/gen_synthetic.py --out tests/fixtures/synthetic` exits 0 and writes ≥ 1 PDF + truth.json
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/test_dimensions.py tests/ -q -m "not golden"` green (layer 1 + regression)
- Spot check: Crow samples' consensus ratio ≈ 20 ft/in equivalent (fpp ≈ 0.2778) — recorded in test output
</verification>

<success_criteria>
- Recipe A implemented with grammar verification; mispairing class of failure eliminated by construction (score < 2 dropped)
- DimensionSample stream ready for the 21-07 scale solver; synthetic fixture harness in place (VALIDATION Wave 0 item)
</success_criteria>

<output>
After completion, create `.planning/phases/21-accuracy-learning-engine/21-03-SUMMARY.md`
</output>

---
phase: 21-accuracy-learning-engine
plan: 15
type: execute
wave: 7
depends_on: ["21-13"]
requirements: [V3-STRUCT-02, V3-LEARN-04]
files_modified:
  - src/bobbytailor/scrape/scraper.py
  - src/bobbytailor/pipeline/pdf_analyzer.py
  - src/bobbytailor/devtools/companion_takeoff.py
  - src/bobbytailor/deterministic/plan_deterministic_legends.py
  - tests/test_pipeline_parity.py
autonomous: true

must_haves:
  truths:
    - "The production path never auto-discovers companion take-off PDFs: uploading a plans PDF next to a `*takeoff*.pdf` sibling ignores it; benchmark scripts pass ground truth explicitly"
    - "StackCT and PDF-upload paths construct identical RunSettings and fire the same deterministic + learning call-sites (companion_present=False hardcode gone because the parameter is gone from the production path)"
    - "StackCT runs retain per-page PDF blobs so the deterministic engine gets vector data, with a runtime vector/raster probe per page"
  artifacts:
    - path: "src/bobbytailor/pipeline/pdf_analyzer.py"
      provides: "plans-only production path (no companion auto-discovery)"
    - path: "src/bobbytailor/scrape/scraper.py"
      provides: "blob retention + RunSettings parity"
    - path: "tests/test_pipeline_parity.py"
      provides: "extended parity assertions incl. deterministic + learning call-sites"
  key_links:
    - from: "scraper.py + pdf_analyzer.py"
      to: "takeoff_pipeline.RunSettings.from_env()"
      via: "identical construction in both entry points"
      pattern: "RunSettings"
    - from: "devtools/companion_takeoff.py"
      to: "scripts/vision_only_benchmark.py only"
      via: "explicit --takeoff-pdf argument (21-17); no production import"
      pattern: "find_companion_takeoff_pdf"
---

<objective>
Enforce the plans-only production input contract (V3-LEARN-04) and entry-point parity (V3-STRUCT-02): kill sibling `*takeoff*.pdf` auto-discovery and manifest requirements on the production path, demote companion/manifest modules to devtools-only imports, make StackCT retain per-page vector PDF blobs, and extend the parity test to prove both entry points fire identical deterministic + learning call-sites.

Purpose: LOCKED user directive — "the take-off PDF must NEVER be discoverable by the runtime pipeline"; StackCT asymmetry (companion_present=False hardcode, screenshot-only rasters) eliminated.
Output: plans-only entry points, blob retention with vector probe, extended parity tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-accuracy-learning-engine/21-CONTEXT.md  (Input contract LOCKED; Benchmark Protocol LOCKED — auto-discovery kill)
@.planning/phases/21-accuracy-learning-engine/21-RESEARCH.md  (§7 entry-point parity; Open Question 1 — blob retention + vector probe)
@src/bobbytailor/scrape/scraper.py
@src/bobbytailor/pipeline/pdf_analyzer.py
</context>

## Artifacts this phase produces

New symbols created by this plan:
- **Removed from production path**: `find_companion_takeoff_pdf` call in pdf_analyzer.py (line ~268) and the `companion_present` parameter plumbing; `companion_present=False` hardcodes at scraper.py lines ~787 and ~1146 (calls updated to the new plans-only signature)
- **Changed signature**: `build_deterministic_legends_from_run_dir(...)` in plan_deterministic_legends.py — `companion_present` and required-manifest behavior become optional dev-only kwargs (`dev_manifest=None`, default production path uses learned/auto manifest data only)
- **Function** `probe_page_vector(pdf_path_or_page) -> dict` in scraper.py (or a small shared helper) — `{is_vector: bool, drawing_items: int, words: int}` using the is_vector_sheet heuristic; logged per page
- **Behavior**: scraper's per-page blob PDFs persisted under the run dir (`blobs/{page_id}.pdf`) and passed to TakeoffPipeline for the deterministic engine (StackCT path no longer screenshot-only)
- **Test additions** in test_pipeline_parity.py: RunSettings-identical assertion; deterministic call-site parity (both paths reach extract_dimension_samples/fuse_sheet via stubs); learning call-site parity; companion-isolation test (sibling takeoff PDF present → never opened)

<threat_model>
ASVS L1:
- **Ground-truth leakage into production output (design HIGH — the phase's core integrity rule):** if the pipeline can see the human take-off, "accuracy" is fake. Blocking acceptance: isolation test places `X-Take offs.pdf` beside the plans PDF and asserts (via patched open/discovery spy) it is never read; `find_companion_takeoff_pdf` has zero call sites outside devtools/ and scripts/.
- **Blob storage (LOW):** per-page PDFs written under the run dir with existing path conventions (no user-controlled names — page_id ints).
- Auth/endpoints unchanged.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Kill companion auto-discovery + demote manifest requirements</name>
  <files>src/bobbytailor/pipeline/pdf_analyzer.py, src/bobbytailor/deterministic/plan_deterministic_legends.py, src/bobbytailor/devtools/companion_takeoff.py</files>
  <read_first>
    - src/bobbytailor/pipeline/pdf_analyzer.py (line ~268 find_companion_takeoff_pdf call and lines 282+ companion_present plumbing)
    - src/bobbytailor/deterministic/plan_deterministic_legends.py (lines 19-106 — companion_present/manifest parameters and their consumers)
    - src/bobbytailor/devtools/companion_takeoff.py (find_companion_takeoff_pdf — stays, but only devtools/scripts may import it)
    - tests/test_companion_takeoff.py, tests/test_plan_deterministic_legends.py (tests to update for the new contract)
  </read_first>
  <action>
    1. pdf_analyzer: delete the find_companion_takeoff_pdf call and companion plumbing; production `run_pdf_analysis` takes plans PDF only. Deterministic legends now come from the 21-13 pipeline flow (tables/schedules/footprint) — plan_deterministic_legends' companion/manifest branches become dev-only: rename params to `dev_manifest=None` and gate the companion-dependent logic behind explicit dev kwargs with a docstring stating "production is plans-only; see benchmark scripts for ground-truth scoring".
    2. companion_takeoff.py keeps `find_companion_takeoff_pdf` for dev/benchmark use with a module docstring "DEV/BENCHMARK ONLY — never imported by production entry points".
    3. Update affected tests: companion tests now target the devtools import path; plan_deterministic_legends tests use dev kwargs explicitly.
  </action>
  <verify>rg "find_companion_takeoff_pdf" src/bobbytailor/ --files-with-matches shows ONLY devtools/companion_takeoff.py; python3 -m pytest tests/test_companion_takeoff.py tests/test_plan_deterministic_legends.py -q exits 0</verify>
  <done>Production path structurally cannot see the take-off PDF</done>
  <acceptance_criteria>
    - `rg -l "find_companion_takeoff_pdf" src/bobbytailor/pipeline src/bobbytailor/scrape src/bobbytailor/web` returns no files
    - plan_deterministic_legends.py signature no longer contains `companion_present` (renamed dev kwarg)
    - Isolation test (Task 3) green
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: StackCT blob retention + RunSettings parity</name>
  <files>src/bobbytailor/scrape/scraper.py</files>
  <read_first>
    - src/bobbytailor/scrape/scraper.py (lines 755-825 + 1130-1160: the two companion_present=False call sites; blob download path — where per-page PDFs are fetched then discarded vs screenshots kept; the _pipeline singleton run_sheet calls)
    - src/bobbytailor/pipeline/takeoff_pipeline.py (RunSettings + run_sheet page-source contract from 21-13)
    - .planning/phases/21-accuracy-learning-engine/21-RESEARCH.md §7 + Open Question 1 (blob retention task; vector/raster probe recommendation)
  </read_first>
  <action>
    1. Retain per-page PDF blobs: save each downloaded page blob to `{run_dir}/blobs/{page_id}.pdf` (respecting REUSE_SCREENSHOTS-style caching); pass blob path (or fitz page) into the pipeline run_sheet calls so the deterministic engine runs on vector data — screenshots remain for UI/preview only.
    2. `probe_page_vector` per page at capture time; log and store `is_vector` in the capture manifest; raster pages route to the ensemble fallback exactly like raster uploads (identical behavior contract).
    3. Replace both `companion_present=False` call sites with the new plans-only legends flow (same code path pdf_analyzer uses post-Task-1). Construct `RunSettings.from_env()` once and pass to the pipeline — identical construction to pdf_analyzer.
  </action>
  <verify>python3 -m pytest tests/test_scraper_pipeline.py tests/test_scraper_two_phase.py -q exits 0</verify>
  <done>StackCT path is vector-capable and settings-identical to the PDF path</done>
  <acceptance_criteria>
    - scraper.py contains `blobs/` path writing and `probe_page_vector`
    - `rg "companion_present" src/bobbytailor/scrape/scraper.py` returns no matches
    - Both entry points contain `RunSettings.from_env()` (grep both files)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Extended parity + isolation tests</name>
  <files>tests/test_pipeline_parity.py</files>
  <read_first>
    - tests/test_pipeline_parity.py (existing parity assertions + stub mechanism)
    - tests/conftest.py (_ensure_real pre-registration — new stubbed modules may need entries)
  </read_first>
  <action>
    Extend parity tests:
    1. RunSettings parity: patch RunSettings.from_env, run both entry-point code paths (stubbed I/O), assert both received the same settings object/values
    2. Deterministic call-site parity: patch takeoff_pipeline deterministic entry names; assert both paths trigger them for a vector page
    3. Learning call-site parity: patch the learning retrieval hook (lands fully in 21-16 — assert the seam exists and is invoked identically; use hasattr guard so this test strengthens after 21-16)
    4. Companion isolation: tmp dir with `plans.pdf` + `plans-Take offs.pdf`; spy on companion_takeoff module import/open; run the production pdf path (stubbed pipeline) → the takeoff file is never opened; also assert `find_companion_takeoff_pdf` absent from production module namespaces
  </action>
  <verify>python3 -m pytest tests/test_pipeline_parity.py -q exits 0; full suite green</verify>
  <done>Parity and plans-only isolation locked by tests</done>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_pipeline_parity.py -q` exits 0 with ≥ 4 new assertions/tests
    - Isolation test contains a sibling filename matching `*Take offs.pdf`
    - `python3 -m pytest tests/ -q -m "not golden"` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Parity suite green; scraper suites green; full suite green
- Grep audit: companion discovery confined to devtools/ + scripts/
</verification>

<success_criteria>
- Production input contract is plans-only by construction (V3-LEARN-04 independence half)
- Entry points behave identically with a parity test covering deterministic + learning call-sites (V3-STRUCT-02)
</success_criteria>

<output>
After completion, create `.planning/phases/21-accuracy-learning-engine/21-15-SUMMARY.md`
</output>

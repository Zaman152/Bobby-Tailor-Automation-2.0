---
phase: 13-stackct-data-persistence
plan: 2
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - stackct_sync.py
  - project_cache.py
  - app.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Only one Playwright StackCT login runs at a time across the app"
    - "Project list sync writes to SQLite and records sync_runs row"
    - "Plan list sync for a project writes pages to SQLite and updates sheet_count"
    - "Stale catalog can refresh in background without blocking UI thread"
  artifacts:
    - path: stackct_sync.py
      provides: "sync_projects, sync_project_plans with browser lock"
      contains: "_browser_lock"
    - path: project_cache.py
      provides: "Delegates live fetch to stackct_sync"
      contains: "stackct_sync"
  key_links:
    - from: stackct_sync.py
      to: browser.py
      via: "StackCTBrowser login + get_all_projects/get_all_page_ids"
      pattern: "StackCTBrowser"
    - from: stackct_sync.py
      to: stackct_store.py
      via: "upsert_projects|upsert_plans"
      pattern: "upsert_projects|upsert_plans"
---

<objective>
Centralize StackCT browser ingest into a sync service that populates SQLite under the existing global browser lock, with TTL checks and optional background refresh.

Purpose: Eliminate scattered asyncio loops in `project_cache.py`; enable bulk warming of sheet counts; reduce concurrent login races.

Output: `stackct_sync.py`, refactored `project_cache.py` fetch paths, APScheduler hook in `app.py`.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-stackct-data-persistence/13-RESEARCH.md
@.planning/phases/13-stackct-data-persistence/13-01-SUMMARY.md
@project_cache.py
@browser.py
@app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement stackct_sync.py with serialized browser access</name>
  <files>stackct_sync.py</files>
  <action>
1. Create `stackct_sync.py`:
   - Move `_browser_lock` from `project_cache.py` here (single source of truth)
   - `sync_projects(force: bool = False) -> dict` — if not force and `stackct_store.is_projects_fresh()`, return cached list from DB with `from_cache: True`
   - Under lock: reuse pattern from `_fetch_from_stackct` / `_fetch_pages_for_project` (new event loop per call — match existing)
   - On success: `upsert_projects`, `record_sync_run` type `projects` status success
   - On failure: log error, `finish_sync_run` error; if DB has rows, return stale DB data with `stale: True` (mirror current JSON fallback)

2. `sync_project_plans(project_id: int, force: bool = False) -> dict`:
   - TTL check via `is_plans_fresh(project_id)`
   - Under lock: double-check cache after acquiring lock (existing pattern in `get_project_plans`)
   - `browser.get_all_page_ids(project_id)` → `upsert_plans`
   - Return `{plans, project_id, from_cache, fetched_at}` matching current API shape

3. Do NOT add Option B API intercept in this task — DOM path only.

4. Export `get_browser_lock()` for scraper if needed later (document only; no scraper changes in this plan).
  </action>
  <verify>python -c "from stackct_sync import sync_projects; r=sync_projects(force=True); print(len(r.get('projects',[])), r.get('from_cache'))" — requires valid .env StackCT creds OR mock in dev</verify>
  <done>sync_projects and sync_project_plans write to stackct.db; sync_runs table has rows; lock prevents parallel sync tests from overlapping</done>
</task>

<task type="auto">
  <name>Task 2: Wire background refresh and thin project_cache facade</name>
  <files>project_cache.py, app.py</files>
  <action>
1. Refactor `project_cache.py`:
   - `get_projects` → read DB via `stackct_store.list_projects()`; if empty or stale, call `stackct_sync.sync_projects`
   - `get_project_plans` → read DB plans; if missing/stale, `stackct_sync.sync_project_plans`
   - `get_all_sheet_counts` → `stackct_store.get_sheet_counts()` only (no disk glob)
   - `prefetch_in_background` → thread calling `sync_projects(force=False)` if stale
   - Remove direct JSON `save_cache` / `save_plans_cache` from hot path (keep functions as deprecated no-ops or redirect to store for one release)
   - Import lock from `stackct_sync` only

2. In `app.py` (after Flask app created):
   - Use existing `apscheduler` dependency: register `IntervalTrigger(hours=STACKCT_CACHE_TTL_HOURS)` job `sync_projects_if_stale` (daemon-safe)
   - Keep `prefetch_in_background()` on startup

3. Add `POST /api/projects/<int:project_id>/sync-plans` (optional force query) calling `sync_project_plans` — for manual warm-up without opening preview panel.

4. README snippet (1 paragraph): single browser lock; concurrent refresh + preview may queue.
  </action>
  <verify>grep -E "stackct_sync|stackct_store" project_cache.py; curl -s http://localhost:5050/api/projects/sheet-counts | head -c 200</verify>
  <done>Background scheduler registered; project_cache no longer writes JSON on normal path; sheet-counts served from DB</done>
</task>

</tasks>

<verification>
- Second sync within TTL does not launch browser (check logs for "from cache" / skip)
- sync_runs shows success row after manual refresh
- `_browser_lock` exists only in stackct_sync.py
</verification>

<success_criteria>
- All browser-based catalog fetches go through stackct_sync.py
- TTL and stale fallback behavior preserved from Phase 1/4
</success_criteria>

<output>
After completion, create `.planning/phases/13-stackct-data-persistence/13-02-SUMMARY.md`
</output>

---
phase: 13-stackct-data-persistence
plan: 4
type: execute
wave: 4
depends_on: ["13-03"]
files_modified:
  - static/app.js
  - templates/index.html
  - static/style.css
autonomous: false
user_setup: []

must_haves:
  truths:
    - "Project list shows sheet counts on load when DB has synced plans (not only after Preview)"
    - "Preview Plans loads instantly from cache when DB is fresh"
    - "User can trigger Sync/Refresh and see syncing state in cache status line"
    - "Login failure hint still shown when live sync fails"
  artifacts:
    - path: static/app.js
      provides: "DB-aware loadSheetCounts and fetchPlans"
      contains: "syncing"
    - path: templates/index.html
      provides: "Sync plans control if added"
      contains: "cacheStatus"
  key_links:
    - from: static/app.js:loadProjects
      to: /api/projects/sheet-counts
      via: "fetch on project load"
      pattern: "sheet-counts"
    - from: static/app.js:fetchPlans
      to: /api/projects/.*/plans
      via: "refresh query param"
      pattern: "/plans"
---

<objective>
Update Projects workspace UI to consume DB-backed sheet counts and cached plans, with clear sync/refresh feedback per Master §8.3.

Purpose: User-visible fix for "Preview for count" and slow/failing preview when data already in DB.

Output: Updated `static/app.js` (+ minimal HTML/CSS for sync controls).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-stackct-data-persistence/13-03-SUMMARY.md
@Master.md (§8.3 Projects page)
@static/app.js
@templates/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sheet counts and project list from DB</name>
  <files>static/app.js</files>
  <action>
1. `loadSheetCounts()` — after fetch, if `data.counts` has entries, update `projectSheetCounts` and call `renderProjectList(allProjects)` immediately (already partial — ensure re-render happens even when counts arrive after projects)

2. `loadProjects()` cache status line:
   - Show `Syncing…` when `data.syncing === true`
   - Show `Stale · refreshing` when `data.stale && data.from_cache`
   - Keep existing Cached/Live labels

3. Replace copy "Preview for count" when `sheet_count` is null:
   - Show `—` or "Sync plans" with muted style instead of implying preview is required
   - If count is 0 from API, show "0 sheets"

4. Optional: on project select, if `projectSheetCounts[id]` missing, call `POST /api/projects/{id}/sync-plans` in background (fire-and-forget) to warm DB — do not block selection

Do NOT change plan checkbox / run selected logic (Phase 4/9).
  </action>
  <verify>Manual: load Projects page with warm DB — counts visible without clicking Preview</verify>
  <done>Project list displays numeric sheet counts when DB has data; status line reflects syncing/stale</done>
</task>

<task type="auto">
  <name>Task 2: Preview UX for cached plans and refresh controls</name>
  <files>static/app.js, templates/index.html, static/style.css</files>
  <action>
1. `fetchPlans(projectId)`:
   - First request: `GET /api/projects/{id}/plans` (DB-first)
   - Add "Force refresh" path: hold Shift+Preview or secondary button `refreshPlansBtn` → `?refresh=1`
   - If response `from_cache && !stale`: change loading message to "Loaded from cache" briefly
   - If `syncing`: show spinner "Syncing with StackCT…" and poll same endpoint every 3s up to 120s

2. `templates/index.html` — near existing Refresh projects button:
   - Add `Sync all plan counts` button OR tooltip on Refresh: "Updates project list from StackCT"
   - Wire to `POST /api/projects/refresh` (existing) — no new endpoint required for projects

3. Keep login failure hint block in fetchPlans (lines ~964-966 pattern) for `data.error` matching /login/i

4. CSS: `.sheet-count-unknown { opacity: 0.6 }` for missing counts

Match Master §8.3: searchable list with sheet counts — counts are first-class, not preview-gated.
  </action>
  <verify>Manual: Preview on warmed project — plan list appears in &lt;2s; force refresh shows live fetch</verify>
  <done>Preview uses DB when fresh; force refresh available; syncing polling works</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>DB-backed sheet counts on project list; cached plan preview; sync status indicators</what-built>
  <how-to-verify>
1. Start app with existing `output/stackct.db` or run refresh once
2. Open Projects → confirm sheet counts appear without clicking Preview on each row
3. Select project → Preview Plans → list loads quickly if previously synced
4. Click Refresh / force refresh → status shows Syncing then Cached/Live
5. Confirm no regression: Run Selected still works with checked page_ids
  </how-to-verify>
  <resume-signal>Type "approved" or describe UI issues</resume-signal>
</task>

</tasks>

<verification>
- UI never requires preview solely to see sheet count when sync has run
- Concurrent preview + refresh does not break UI (may queue — acceptable)
</verification>

<success_criteria>
- Master §8.3 success criterion: searchable project list **with sheet counts** without per-click scrape
- Preview login errors reduced when DB warm
</success_criteria>

<output>
After completion, create `.planning/phases/13-stackct-data-persistence/13-04-SUMMARY.md`
</output>

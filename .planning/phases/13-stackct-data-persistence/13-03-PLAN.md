---
phase: 13-stackct-data-persistence
plan: 3
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - app.py
  - project_cache.py
  - stackct_store.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "GET /api/projects returns projects from DB with from_cache and fetched_at metadata"
    - "GET /api/projects/<id>/plans returns plans from DB when fresh without requiring preview click first"
    - "GET /api/projects/sheet-counts returns counts for all projects that have synced plans"
    - "Refresh endpoints trigger sync then return updated DB snapshot"
  artifacts:
    - path: app.py
      provides: "DB-first project/plan routes"
      contains: "syncing"
  key_links:
    - from: app.py:/api/projects
      to: stackct_store.py
      via: "project_cache.get_projects"
      pattern: "get_projects"
    - from: app.py:/api/projects/sheet-counts
      to: stackct_store.py
      via: "get_sheet_counts"
      pattern: "get_all_sheet_counts|get_sheet_counts"
---

<objective>
Refactor Flask project/plan API routes to be DB-first with stale-while-revalidate semantics and explicit sync status in JSON responses.

Purpose: API consumers (UI, scripts) never depend on JSON files; preview can hit cached plans instantly when DB is warm.

Output: Updated routes in `app.py`, response contract documented in docstrings.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-stackct-data-persistence/13-02-SUMMARY.md
@app.py
@project_cache.py
@Master.md (API table §3.9)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize API response contract</name>
  <files>project_cache.py, stackct_store.py</files>
  <action>
1. Ensure all catalog endpoints return consistent shape:
   - Projects: `{projects: [{id, name, sheet_count?}], fetched_at, from_cache, stale?, syncing?, error?}`
   - Plans: `{plans: [{page_id, sheet_name}], project_id, fetched_at, from_cache, stale?, warning?, error?}`
   - Sheet counts: `{counts: {project_id: int}, synced_at?}`

2. Implement stale-while-revalidate in `get_project_plans`:
   - If DB plans exist but TTL expired: return DB plans immediately with `stale: True`, spawn daemon thread `sync_project_plans(force=True)` (set `syncing: True` on first response only if thread started)
   - If DB empty: blocking sync (current UX) unless `?background=1` query → return empty + syncing

3. `get_projects(force_refresh)`:
   - `refresh=1` → blocking `sync_projects(force=True)`
   - else DB-first + background project sync if stale (mirror plans pattern)

4. Preserve Phase 1 rule: never return raw exception strings — use generic error + log details.

Do NOT change `/api/run/stackct` or scraper in this plan.
  </action>
  <verify>python -m py_compile app.py project_cache.py stackct_store.py</verify>
  <done>Response shapes documented in module docstrings; stale-while-revalidate implemented for plans</done>
</task>

<task type="auto">
  <name>Task 2: Update app.py routes and query params</name>
  <files>app.py</files>
  <action>
1. Update route docstrings for:
   - `GET /api/projects` — supports `?refresh=1` (existing), document `from_cache`, `syncing`
   - `GET /api/projects/<id>/plans` — add `?refresh=1` passthrough to force sync; `?background=1` for non-blocking
   - `GET /api/projects/sheet-counts` — note DB source

2. Wire `POST /api/projects/refresh` to `sync_projects(force=True)` via stackct_sync (not legacy JSON)

3. Add integration test script or `tests/test_stackct_store.py` minimal:
   - Mock store: list_projects returns data without browser
   - Optional: skip live StackCT in CI

4. Deprecation comment in app.py: `projects_cache.json` legacy, removed in v1.2.

Do NOT add authentication changes (Phase 12 scope).
  </action>
  <verify>curl -s http://localhost:5050/api/projects | python -m json.tool | head -20; curl -s http://localhost:5050/api/projects/sheet-counts | python -m json.tool</verify>
  <done>All three project routes read DB; refresh forces sync; errors sanitized</done>
</task>

</tasks>

<verification>
- With warm DB, GET plans responds in &lt;500ms (no browser)
- force_refresh=1 updates fetched_at in response
</verification>

<success_criteria>
- JSON cache files not read on normal API path (grep confirms)
- API backward compatible for UI fields: projects, plans, counts, from_cache, error
</success_criteria>

<output>
After completion, create `.planning/phases/13-stackct-data-persistence/13-03-SUMMARY.md`
</output>

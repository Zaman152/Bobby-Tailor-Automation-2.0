# Plan 11-03: README VPS Deployment Section

**Phase:** 11 — PDF Selection & Production Docs
**Requirements:** DEPLOY-02
**Depends on:** None
**Reference:** Master.md §11

---

## Objective

README.md documents complete Hostinger VPS setup: gunicorn, Playwright system deps, `.env` configuration, headless Chrome flags, systemd service, and nginx reverse proxy.

---

## Tasks

### 1. Review Current README Coverage

Current README.md includes:
- ✓ Basic setup (pip, playwright install)
- ✓ VPS deployment section (apt deps, gunicorn command)
- ✓ Chromium launch flags
- ✓ `.env` tuning (CANVAS_STABILITY_TIMEOUT)
- ✓ Docker alternative
- ✓ Troubleshooting section

**Gaps to fill:**
- Systemd service file example
- Nginx reverse proxy config
- Complete environment variables table (match Master.md §11.3)
- Security notes (firewall, HTTPS)

### 2. Add Systemd Service File

Add section with complete service file:

```markdown
### Systemd Service (Auto-start on Boot)

Create `/etc/systemd/system/bobby-tailor.service`:

\`\`\`ini
[Unit]
Description=Bobby Tailor Estimation Automation
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/home/ubuntu/bobby-tailor
Environment="PATH=/home/ubuntu/bobby-tailor/.venv/bin"
ExecStart=/home/ubuntu/bobby-tailor/.venv/bin/gunicorn -w 2 -b 127.0.0.1:5050 app:app --timeout 300
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
\`\`\`

Enable and start:

\`\`\`bash
sudo systemctl daemon-reload
sudo systemctl enable bobby-tailor
sudo systemctl start bobby-tailor
sudo systemctl status bobby-tailor
\`\`\`
```

### 3. Add Nginx Reverse Proxy Config

Add section for nginx:

```markdown
### Nginx Reverse Proxy (Optional)

If using nginx to serve on port 80/443:

\`\`\`nginx
server {
    listen 80;
    server_name your-domain.com;
    
    location / {
        proxy_pass http://127.0.0.1:5050;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;
    }
    
    client_max_body_size 100M;  # For large PDF uploads
}
\`\`\`

For HTTPS, add SSL certificate via Let's Encrypt:

\`\`\`bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
\`\`\`
```

### 4. Expand Environment Variables Table

Replace current `.env` mentions with comprehensive table matching Master.md §11.3:

```markdown
### Environment Variables

| Variable | Description | Default |
|----------|-------------|---------|
| `STACKCT_EMAIL` | StackCT login email | (required) |
| `STACKCT_PASSWORD` | StackCT login password | (required) |
| `ANTHROPIC_API_KEY` | Anthropic API key (sk-ant-...) | (required) |
| `CLAUDE_MODEL` | Model for general drawings | `claude-haiku-4-5` |
| `CLAUDE_MODEL_SCHEDULES` | Model for schedule/panel sheets | `claude-sonnet-4-6` |
| `HEADLESS` | Run browser headless | `true` |
| `OUTPUT_DIR` | Report output directory | `./output` |
| `CANVAS_STABILITY_TIMEOUT` | Max seconds waiting for canvas render | `15` |
| `CANVAS_STABILITY_CHECKS` | Consecutive stable hashes before capture | `2` |
```

### 5. Add Security Notes

```markdown
### Security Notes

**Firewall:** Only expose port 80/443 if using nginx; keep 5050 internal:

\`\`\`bash
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 22/tcp
sudo ufw enable
\`\`\`

**Credentials:** Never commit `.env` to git. The `.gitignore` already excludes it.

**Updates:** Periodically update Playwright and Chromium:

\`\`\`bash
pip install --upgrade playwright
playwright install chromium
\`\`\`
```

### 6. Reorganize VPS Section Structure

Final README VPS section structure:

```
## VPS Deployment (Ubuntu)

### System Requirements
- Ubuntu 20.04+ or Debian 11+
- Python 3.10+
- 2 GB RAM minimum (4 GB recommended)

### Installation
(existing content)

### Environment Variables
(new comprehensive table)

### Running with Gunicorn
(existing content)

### Systemd Service (Auto-start on Boot)
(new content)

### Nginx Reverse Proxy (Optional)
(new content)

### Chromium Launch Flags
(existing content, already good)

### Docker (Alternative)
(existing content)

### Security Notes
(new content)

### Troubleshooting
(existing content)
```

---

## Acceptance Criteria

- [ ] README has systemd service file example with enable/start commands
- [ ] README has nginx reverse proxy config with SSL note
- [ ] README has complete environment variables table
- [ ] README has security notes (firewall, credentials, updates)
- [ ] README structure matches logical deployment order

---

## Files Modified

| File | Change |
|------|--------|
| `README.md` | Add systemd, nginx, env table, security sections |

---

## Estimated Complexity

**Low** — documentation only, no code changes

---

*Plan created: 2026-05-26*

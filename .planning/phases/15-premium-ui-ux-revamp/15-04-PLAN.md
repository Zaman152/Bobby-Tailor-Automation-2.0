---
phase: 15-premium-ui-ux-revamp
plan: 4
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified:
  - static/js/projects-flow.js
  - static/app.js
  - templates/index.html
  - static/style.css
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Projects flow shows visual stepper: Project → Plan set → Sheets → Run"
    - "Plan set and sheet panels use consistent cards, cache pills, skeleton loaders"
    - "Sticky run bar shows selected count and plan set name"
  artifacts:
    - path: static/js/projects-flow.js
      provides: "stepper state machine"
      contains: "renderProjectStepper"
  key_links:
    - from: static/js/projects-flow.js
      to: "/api/projects/*/plan-sets"
      via: "fetchPlanSets"
---

<objective>
Upgrade Projects page to a guided, premium flow for plan-set → sheet selection (Phase 14 APIs), reusing drawer/modal patterns from 15-02.

Purpose: Multi-set projects feel intentional, not a wall of checkboxes.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/15-premium-ui-ux-revamp/15-RESEARCH.md
@design-system/bobby-tailor/pages/projects.md
@static/app.js (fetchPlanSets, fetchPlans)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stepper + layout</name>
  <files>static/js/projects-flow.js, templates/index.html, static/style.css</files>
  <action>
1. Optional **21st.dev MCP** for "multi-step wizard stepper, dark industrial, 4 steps" — vanilla output.

2. Add horizontal stepper UI above project list when mode=specific: `① Project  ② Plan set  ③ Sheets  ④ Run`.

3. Extract plan-set + sheet logic from `app.js` into `projects-flow.js`; `app.js` calls `ProjectsFlow.init()`.

4. Auto-advance: 1 plan set → skip to sheets; show breadcrumb "Plan set: {name}".

5. Skeleton loaders during `syncing` instead of plain spinner text.

6. Sticky footer on sheet step: `Run N sheets from {planSet}` + disabled state when 0 selected.
  </action>
  <verify>grep "renderProjectStepper" static/js/projects-flow.js</verify>
  <done>Stepper visible; steps match Phase 14 flow</done>
</task>

<task type="auto">
  <name>Task 2: Polish + toasts</name>
  <files>static/js/projects-flow.js, static/app.js</files>
  <action>
1. On sync error (network/login), `ui.toast.error` with actionable message (not only inline HTML).

2. On successful plan-set sync, subtle toast "Plan sets updated".

3. Improve plan-set cards: selected border, sheet count badge, keyboard selectable.

4. Sheet list: virtualized scroll if &gt;100 items (CSS max-height + scroll ok for v1; document if &gt;200 slow).
  </action>
  <verify>Manual: Morehouse shows 2 sets in stepper; v2 loads 180 sheets</verify>
  <done>Human verify checklist in SUMMARY</done>
</task>

</tasks>

<success_criteria>
- Projects UX matches design-system/pages/projects.md
- No regression to folder_id run payload
</success_criteria>

<output>
After completion, create `.planning/phases/15-premium-ui-ux-revamp/15-04-SUMMARY.md`
</output>

---
phase: 15-premium-ui-ux-revamp
plan: 3
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified:
  - static/js/reports-workspace.js
  - static/js/data-grid.js
  - static/app.js
  - static/style.css
  - templates/index.html
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Report cards have one clear Open Preview CTA; downloads grouped as Export"
    - "Preview workspace: run list + tabs + download rail; Esc closes"
    - "Calculations tab uses Grid.js (or Tabulator) with sort, filter, export filtered rows"
    - "URL reflects open run and tab (?run= & tab=)"
  artifacts:
    - path: static/js/reports-workspace.js
      provides: "full-screen preview shell"
      contains: "openReportWorkspace"
    - path: static/js/data-grid.js
      provides: "vanilla data grid wrapper"
  key_links:
    - from: static/js/reports-workspace.js
      to: "/api/reports/*/preview/*"
      via: "fetch preview APIs"
---

<objective>
Replace inline card accordion preview with a **Report Preview Workspace**: obvious preview vs download, persistent navigation, and analyst-grade calculation tables.

Purpose: Address the user's core complaint — preview/export confusion and weak calculation analysis.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/15-premium-ui-ux-revamp/15-RESEARCH.md
@design-system/bobby-tailor/pages/reports.md
@static/app.js (renderReportCard, loadPreviewTab)
@.planning/phases/05-report-preview-apis/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Report workspace shell</name>
  <files>static/js/reports-workspace.js, static/app.js, static/style.css, templates/index.html</files>
  <action>
1. **Use 21st.dev MCP** (`21st_magic_component_builder`) with prompt: "Full-screen dark SaaS report viewer drawer: left run list, center tabs, right export rail, vanilla HTML/CSS, industrial dark, Lucide icons" — adapt output to project tokens (no React).

2. Implement `openReportWorkspace(runFolder)`, `closeReportWorkspace()`, `setWorkspaceTab(tab)`.

3. Refactor `renderReportCard`:
   - Primary: `Open preview` (icon + text)
   - Secondary: `Export ▾` dropdown → Calculations CSV, Raw CSV, JSON, Summary TXT (calls existing `downloadReportFile`)

4. Remove inline `renderPreviewPanel` accordion; delegate to workspace.

5. Parse/update URL: `history.replaceState` with `?run=&tab=` on open/tab change; on Reports page load, open workspace if params present.

6. Keyboard: Esc close, 1-4 tabs, optional ↑↓ run list.
  </action>
  <verify>grep "openReportWorkspace" static/js/reports-workspace.js; manual: open Morehouse report, switch tabs, close with Esc</verify>
  <done>Preview only via workspace; downloads only via Export menu/rail</done>
</task>

<task type="auto">
  <name>Task 2: Data grid + analysis UX</name>
  <files>static/js/data-grid.js, static/js/reports-workspace.js, app.py (optional cap note only)</files>
  <action>
1. Integrate **Grid.js** (CDN) in `data-grid.js` — `mountGrid(container, { headers, rows, filters, onExport })`.

2. Calculations tab: columns per API headers; confidence column cell class if `confidence` field exists; `formula_applied` in expandable row detail or title tooltip.

3. Footer row: totals for `qty` grouped by `item_type` (visible filtered rows).

4. Export button: download CSV of **currently filtered** rows (client-side Blob).

5. Summary tab: improved typography (headings, monospace blocks for numbers).

6. JSON tab: add search/filter input above `renderJsonTree`; keep collapsible tree.

7. Show cap banner when API returns `capped: true` with link text "Download full CSV to analyze all rows".
  </action>
  <verify>grep "Grid" static/js/data-grid.js; open calculations tab on a real report — sort column works</verify>
  <done>Grid replaces renderDataTable for workspace; export filtered works</done>
</task>

</tasks>

<verification>
- [ ] User can answer "where do I preview?" → one button
- [ ] User can answer "where do I download?" → Export menu/rail only
- [ ] Switching tabs does not close workspace
- [ ] Switching runs inside workspace keeps drawer open
</verification>

<success_criteria>
- Report preview is a first-class workspace, not a card expando
- Calculations analyzable without downloading (for capped subset)
</success_criteria>

<output>
After completion, create `.planning/phases/15-premium-ui-ux-revamp/15-03-SUMMARY.md`
</output>

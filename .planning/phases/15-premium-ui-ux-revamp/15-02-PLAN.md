---
phase: 15-premium-ui-ux-revamp
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - static/js/ui/modal.js
  - static/js/ui/drawer.js
  - static/js/ui/toast.js
  - static/js/ui/focus-trap.js
  - static/ui-motion.js
  - templates/index.html
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Modal and drawer open/close with focus trap and Esc"
    - "Toast notifications work for sync errors and job complete"
    - "prefers-reduced-motion disables non-essential animation"
  artifacts:
    - path: static/js/ui/modal.js
      provides: "dialog primitive"
    - path: static/js/ui/drawer.js
      provides: "slide-over panel"
  key_links:
    - from: static/ui-motion.js
      to: static/js/ui/modal.js
      via: "animate enter/exit"
---

<objective>
Build reusable UI primitives (modal, drawer, toast, focus trap) and extend Motion helpers — the building blocks for report workspace and stepper flows.

Purpose: Every Phase 15 surface uses the same interaction patterns.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/15-premium-ui-ux-revamp/15-RESEARCH.md
@static/ui-motion.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: UI primitives</name>
  <files>static/js/ui/*.js, templates/index.html</files>
  <action>
1. `modal.js` — `openModal({ title, bodyHtml, size })` / `closeModal()` using `&lt;dialog&gt;` or div+aria-modal; backdrop click optional.

2. `drawer.js` — `openDrawer({ side: 'right', width, content })` for full-screen report workspace host.

3. `toast.js` — `toast.success|error|info(message)` stack top-right, auto-dismiss 5s.

4. `focus-trap.js` — trap Tab inside modal/drawer; restore focus on close.

5. Mount `#ui-modal-root` and `#ui-toast-root` in `index.html` before app scripts.

6. Export `window.ui = { modal, drawer, toast }`.
  </action>
  <verify>Manual: open/close modal from browser console `ui.modal.openModal({title:'Test', bodyHtml:'ok'})`</verify>
  <done>Primitives work; Esc closes; focus returns to trigger</done>
</task>

<task type="auto">
  <name>Task 2: Motion extensions</name>
  <files>static/ui-motion.js</files>
  <action>
1. Add `modalEnter(el)`, `drawerSlide(el, from)`, `fadeOut(el)` using Motion CDN.

2. Respect `matchMedia('(prefers-reduced-motion: reduce)')` — skip animation, instant show.

3. `pulseButton` used on primary CTAs (preview open, run start).
  </action>
  <verify>grep "prefers-reduced-motion" static/ui-motion.js</verify>
  <done>Motion helpers exported on window.uiMotion</done>
</task>

</tasks>

<success_criteria>
- Modal/drawer/toast usable from app.js without duplicating DOM logic
</success_criteria>

<output>
After completion, create `.planning/phases/15-premium-ui-ux-revamp/15-02-SUMMARY.md`
</output>

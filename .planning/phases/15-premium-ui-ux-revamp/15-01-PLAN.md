---
phase: 15-premium-ui-ux-revamp
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - design-system/bobby-tailor/MASTER.md
  - design-system/bobby-tailor/pages/projects.md
  - design-system/bobby-tailor/pages/reports.md
  - static/style.css
  - static/ui-polish.css
  - templates/index.html
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Dark industrial tokens are documented and applied consistently"
    - "Lucide icons load; no emoji used as UI icons"
    - "Page-level design overrides exist for Reports and Projects"
  artifacts:
    - path: design-system/bobby-tailor/MASTER.md
      provides: "global design source of truth"
    - path: static/ui-polish.css
      provides: "glass surfaces, elevation, focus rings"
  key_links:
    - from: templates/index.html
      to: static/ui-polish.css
      via: link tag
---

<objective>
Establish design system v2: persist dark industrial palette (override ui-ux-pro-max light default), typography scale, spacing, and glass/elevation layers on the existing shell.

Purpose: Foundation for all Phase 15 UI work — one visual language before components.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-premium-ui-ux-revamp/15-RESEARCH.md
@Master.md (§8.1 tokens)
@static/style.css
@~/.cursor/skills/ui-ux-pro-max/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MASTER.md dark industrial + page overrides</name>
  <files>design-system/bobby-tailor/MASTER.md, design-system/bobby-tailor/pages/*.md</files>
  <action>
1. Edit `design-system/bobby-tailor/MASTER.md` to **keep** existing CSS variables from Master.md §8.1 (not the light #F8FAFC palette from auto-generate). Document: primary, surfaces, accents, fonts (DM Mono display, Inter body, JetBrains data).

2. Create `pages/reports.md` — preview workspace layout, button hierarchy (primary preview vs secondary export), tab labels.

3. Create `pages/projects.md` — stepper steps, plan-set card density, cache pill colors.

4. Run ui-ux-pro-max supplemental searches only if needed:
   `python3 skills/ui-ux-pro-max/scripts/search.py "data table dashboard dark" --domain ux`
   `python3 skills/ui-ux-pro-max/scripts/search.py "modal drawer" --domain ux`
  </action>
  <verify>test -f design-system/bobby-tailor/MASTER.md && test -f design-system/bobby-tailor/pages/reports.md</verify>
  <done>MASTER + 2 page overrides committed; documents primary vs export actions</done>
</task>

<task type="auto">
  <name>Task 2: Tokens v2 in CSS + Lucide</name>
  <files>static/style.css, static/ui-polish.css, templates/index.html</files>
  <action>
1. Add token aliases: `--surface-glass`, `--shadow-lg`, `--radius-xl`, `--motion-fast`, `--motion-normal`.

2. `ui-polish.css`: glass cards (backdrop-filter), focus-visible rings, skeleton shimmer, elevation scale.

3. Load Lucide via CDN script or inline sprite helper in `static/js/ui/icons.js` — replace text-only buttons where trivial (sidebar nav).

4. Ensure contrast ≥ 4.5:1 on `--text-primary` / `--text-secondary` against surfaces.
  </action>
  <verify>grep -n "surface-glass" static/ui-polish.css && curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:5050/static/ui-polish.css</verify>
  <done>Polish CSS linked from index.html; tokens used in at least sidebar + cards</done>
</task>

</tasks>

<success_criteria>
- Design system files exist and match dark industrial brand
- New CSS does not break existing layout
</success_criteria>

<output>
After completion, create `.planning/phases/15-premium-ui-ux-revamp/15-01-SUMMARY.md`
</output>

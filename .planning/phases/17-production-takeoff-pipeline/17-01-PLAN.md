---
phase: 17-production-takeoff-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scraper.py
  - sheet_preview.py
  - config.py
  - .env.example
autonomous: true

must_haves:
  truths:
    - "Scraper skips blob download when a matching screenshot exists from a prior run"
    - "Reuse is controlled by REUSE_SCREENSHOTS env var (default true)"
    - "Job log shows 'Using cached screenshot' per reused sheet"
  artifacts:
    - path: "scraper.py"
      contains: "find_screenshot_paths"
    - path: "config.py"
      contains: "REUSE_SCREENSHOTS"
---

<objective>
Wire screenshot reuse from sheet_preview into the scraper capture step so re-runs skip StackCT blob downloads when a fresh file already exists on disk.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Config flag REUSE_SCREENSHOTS</name>
  <files>config.py, .env.example</files>
  <action>
Add `REUSE_SCREENSHOTS = os.getenv("REUSE_SCREENSHOTS", "true").lower() in ("1", "true", "yes")` to config.py.
Document in .env.example under StackCT section.
  </action>
  <verify>grep REUSE_SCREENSHOTS config.py .env.example</verify>
  <done>Flag loads default true; documented</done>
</task>

<task type="auto">
  <name>Task 2: Integrate find_screenshot_paths in scraper capture</name>
  <files>scraper.py, sheet_preview.py</files>
  <action>
Before `_capture_sheet_screenshot`:
1. At start of sheet loop (once), if REUSE_SCREENSHOTS: `cached = find_screenshot_paths(project_id, project_name, pages)`
2. If `page_id in cached` and file size > 1000 bytes:
   - Copy cached file to current run's screenshot_path (shutil.copy2)
   - Log "Using cached screenshot for {sheet_name}"
   - Skip browser download entirely
3. Export `_safe_sheet_filename` from scraper or move to shared `path_utils.py` if sheet_preview needs it for consistency

Ensure copied files use sanitized names even when source had legacy bad names.
  </action>
  <verify>pytest tests/test_scraper_reuse.py — mock cache hit skips download</verify>
  <done>Second run on same project uses cached files when REUSE_SCREENSHOTS=true</done>
</task>

</tasks>

<success_criteria>
- Re-run on project with existing screenshots completes capture phase in seconds (log proof)
- REUSE_SCREENSHOTS=false forces fresh download
</success_criteria>

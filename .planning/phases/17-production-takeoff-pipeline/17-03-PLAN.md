---
phase: 17-production-takeoff-pipeline
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - scraper.py
  - capture_manifest.py
  - app.py
autonomous: true

must_haves:
  truths:
    - "Analyze-only mode re-runs Claude on existing manifest without browser login"
    - "Failed analysis sheets can be retried without re-capture"
    - "API accepts analyze_only flag on /api/run/stackct"
  artifacts:
    - path: "app.py"
      contains: "analyze_only"
    - path: "scraper.py"
      contains: "run_analyze_from_manifest"
---

<objective>
Enable resume and analyze-only runs from disk manifest — recover from mid-run crashes and re-run Claude without StackCT browser time.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: run_analyze_from_manifest()</name>
  <files>scraper.py, capture_manifest.py</files>
  <action>
Add function accepting `screenshots_dir` or `manifest_path`:
- Load manifest
- Skip pages with analysis_status=ok unless force=True
- Run analyze pass only
- Generate report at end (partial rules from hotfix)

Add optional cache: save `{page_id}_analysis.json` beside screenshot for skip on re-analyze.
  </action>
  <verify>pytest with fixture manifest + fake analyze_drawing mock</verify>
  <done>Analyze-only produces report without browser.start()</done>
</task>

<task type="auto">
  <name>Task 2: API analyze_only + resume from manifest path</name>
  <files>app.py, static/app.js</files>
  <action>
Extend POST `/api/run/stackct` body:
- `analyze_only: bool` — skip capture, require `manifest_dir` or latest run folder
- `manifest_dir: str` optional — explicit path under output/screenshots

Job dict stores `mode_detail: "full" | "analyze_only"`.

UI: optional "Re-analyze last run" button on job monitor (stretch — log-only OK for v1).
  </action>
  <verify>curl POST with analyze_only + valid manifest_dir completes</verify>
  <done>API documented in README</done>
</task>

</tasks>

<success_criteria>
- Crash after capture pass → analyze_only completes report from existing JPEGs
</success_criteria>

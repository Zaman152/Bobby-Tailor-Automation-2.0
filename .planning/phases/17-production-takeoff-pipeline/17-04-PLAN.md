---
phase: 17-production-takeoff-pipeline
plan: 04
type: execute
wave: 4
depends_on: ["17-02"]
files_modified:
  - app.py
  - static/app.js
  - static/style.css
  - scraper.py
autonomous: true

must_haves:
  truths:
    - "Job monitor shows current phase: Capturing / Analyzing / Reporting"
    - "Weighted progress bar reflects phase (not just sheet index)"
    - "Cancel flag checked between sheets — job stops cleanly"
    - "Partial completion shows warning and still opens Reports"
  artifacts:
    - path: "static/app.js"
      contains: "current_phase"
    - path: "scraper.py"
      contains: "_cancel"
---

<objective>
Production job UX: phase-aware progress, cooperative cancellation, and clear partial-success messaging for client demos.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Weighted progress + current_phase in job API</name>
  <files>app.py, scraper.py</files>
  <action>
Job dict fields: `current_phase`, `phase_progress`.

Progress helper in scraper:
- capturing: 0–40% = (idx/total)*40
- analyzing: 40–90% = 40 + (idx/total)*50
- reporting: 95–100%

Expose in `/api/status/<job_id>`.
  </action>
  <verify>Monitor shows phase label during run</verify>
  <done>Progress no longer misleading during long Claude calls on sheet 1</done>
</task>

<task type="auto">
  <name>Task 2: Cooperative cancel between sheets</name>
  <files>scraper.py, app.py</files>
  <action>
Pass `cancel_check: Callable[[], bool]` from app reading `jobs[job_id].get("_cancel")`.
Between sheets: if cancel_check(): log "Cancelled by user", return partial report if any success else cancelled status.

app.py cancel endpoint already sets `_cancel` — wire it through _stackct_job.
  </action>
  <verify>Cancel mid-run → status cancelled, partial report if ≥1 sheet done</verify>
  <done>Cancel works during 45-sheet run</done>
</task>

<task type="auto">
  <name>Task 3: Monitor UI phase badge + error styling</name>
  <files>static/app.js, templates/index.html, static/style.css</files>
  <action>
Show `job.current_phase` in monitor header.
Error state: red banner with job.error text (already partially done — polish).
Partial done: amber warning badge with job.warning.
  </action>
  <verify>Manual demo walkthrough</verify>
  <done>Client-visible status is never ambiguous</done>
</task>

</tasks>

<success_criteria>
- Demo operator understands what the job is doing at all times
- Cancel does not leave zombie browser sessions
</success_criteria>

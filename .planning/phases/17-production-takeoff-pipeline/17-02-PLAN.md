---
phase: 17-production-takeoff-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - scraper.py
  - capture_manifest.py
autonomous: true

must_haves:
  truths:
    - "Run completes capture for all sheets before any Claude analyze_drawing call"
    - "manifest.json written to screenshots run folder listing every page and capture status"
    - "Progress callback reports phase=capturing during bulk capture"
  artifacts:
    - path: "capture_manifest.py"
      provides: "RunManifest read/write"
    - path: "scraper.py"
      contains: "phase=\"capturing\""
---

<objective>
Split scraper into explicit capture and analyze passes with a manifest file so demos show all screenshots landing first, then Claude processing begins.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create capture_manifest.py</name>
  <files>capture_manifest.py</files>
  <action>
New module with:
- `RunManifest` dataclass (project_id, project_name, folder_id, pages list)
- `PageEntry` (page_id, sheet_name, screenshot_rel, capture_status, analysis_status)
- `save(path)` / `load(path)` JSON serialization
- `manifest_path(screenshots_dir) -> screenshots_dir / "manifest.json"`
  </action>
  <verify>pytest tests/test_capture_manifest.py</verify>
  <done>Manifest round-trips JSON</done>
</task>

<task type="auto">
  <name>Task 2: Refactor scraper into capture pass + analyze pass</name>
  <files>scraper.py</files>
  <action>
Restructure `run_project_scrape`:

**Pass 1 — Capture** (browser required):
- Loop all pages → capture or reuse → update manifest capture_status
- progress_callback(..., phase="capturing")
- Close browser after pass 1 (release resources before long Claude phase)

**Pass 2 — Analyze** (no browser):
- Loop manifest pages where capture_status=ok
- analyze_drawing → update analysis_status
- progress_callback(..., phase="analyzing")

**Pass 3 — Report** (unchanged)

Save manifest after each page state change for crash recovery foundation (17-03).
  </action>
  <verify>Log shows all "Screenshotting" lines before any "Analyzing" line</verify>
  <done>Two-phase ordering verified in integration test or manual 3-sheet run</done>
</task>

</tasks>

<success_criteria>
- manifest.json exists in every run folder
- Browser closed before first Claude API call
</success_criteria>
